\documentclass[tikz,border=8pt]{standalone}
\usepackage{pifont}
\usepackage{amssymb}
\usetikzlibrary{positioning,calc,arrows.meta}
\definecolor{accB}{HTML}{1769AA}
\definecolor{fillB}{HTML}{DCEAF5}
\definecolor{accO}{HTML}{C85A00}
\definecolor{fillO}{HTML}{FCE8D5}
\definecolor{ink}{HTML}{18212B}
\definecolor{slate}{HTML}{66727D}
\definecolor{chipgray}{HTML}{D9DEE3}
\newcommand{\frozenmark}{\textcolor{slate}{\ding{100}}}
% note (luojiaxuan): 与 Figure 1 同语言的事件卡:虚线灰=纯摘要,蓝/橙实线=带图,
% 底部灰条=摘要条。#3: 0=summary 1=recent(蓝) 2=distant(橙)
\newcommand{\evcard}[3]{%
  \ifnum#3=0
    \draw[slate, dashed, rounded corners=1.2pt, fill=white]
      (#1-0.19,#2-0.26) rectangle (#1+0.19,#2+0.26);
  \fi
  \ifnum#3=1
    \draw[accB, very thick, rounded corners=1.2pt, fill=fillB]
      (#1-0.19,#2-0.26) rectangle (#1+0.19,#2+0.26);
  \fi
  \ifnum#3=2
    \draw[accO, very thick, rounded corners=1.2pt, fill=fillO]
      (#1-0.19,#2-0.26) rectangle (#1+0.19,#2+0.26);
  \fi
  \fill[chipgray] (#1-0.13,#2-0.20) rectangle (#1+0.13,#2-0.10);
}
\begin{document}
\begin{tikzpicture}[
  font=\small,
  arr/.style={-{Stealth[length=2mm]}, ink},
  oarr/.style={-{Stealth[length=2.2mm]}, accO, very thick},
  lbl/.style={font=\scriptsize, text=ink},
  slbl/.style={font=\scriptsize, text=slate},
]
\def\dx{0.55}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Panel (a): HGKV — 只动被恢复的历史图 token
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}[shift={(0,0)}]
  \node[lbl, font=\small\bfseries, anchor=west] at (-0.2, 3.55)
    {(a) HGKV: gated K/V, trained; policy frozen};

  % attention 盒
  \node[draw=ink, thick, rounded corners=2pt, minimum width=6.55cm, minimum height=0.8cm,
        anchor=west] (attn) at (-0.2, 2.9)
    {self-attention, last 8 layers\; \frozenmark};

  % ΔK,ΔV 盒(橙 = 被训练的部件)
  \node[draw=accO, very thick, rounded corners=2pt, fill=fillO, font=\small,
        minimum height=5mm] (dkv) at (2.42, 1.7) {$\Delta K,\Delta V$ (trained)};
  \node[lbl, text=accO, anchor=west, font=\scriptsize] at ($(dkv.east)+(0.06,0.2)$) {$\times\,g$};

  % prompt 卡片行:3 摘要 + [蓝 蓝 橙 蓝] + 当前屏 + 响应
  \foreach \i in {1,...,3} {\evcard{\dx*\i}{0}{0}}
  \evcard{\dx*4}{0}{1} \evcard{\dx*5}{0}{1} \evcard{\dx*6}{0}{2} \evcard{\dx*7}{0}{1}
  \node[draw=ink, thick, rounded corners=1.2pt, fill=chipgray, minimum width=0.38cm,
        minimum height=0.52cm, inner sep=0pt] (curc) at (\dx*8.35, 0) {};
  \node[draw=ink, rounded corners=1.2pt, fill=white, minimum width=0.68cm,
        minimum height=0.52cm, inner sep=0pt, font=\scriptsize] (resp) at (\dx*9.9, 0) {act};
  % 分组说明
  \node[slbl, anchor=north, align=center] at (\dx*2, -0.42) {summaries};
  \node[slbl, anchor=north, align=center, text=ink] at (\dx*5.5, -0.42) {restored images};
  \node[slbl, anchor=north, align=center] at (\dx*8.45, -0.42) {current};
  \node[slbl, anchor=north, align=center] at (\dx*10.0, -0.42) {resp.};

  % gate 行
  \foreach \i/\v in {1/0,2/0,3/0} \node[slbl] at (\dx*\i, -1.05) {\v};
  \foreach \i in {4,5,7} \node[font=\scriptsize, text=accB] at (\dx*\i, -1.05) {1};
  \node[font=\scriptsize, text=accO] at (\dx*6, -1.05) {1};
  \node[slbl] at (\dx*8.35, -1.05) {0};
  \node[slbl] at (\dx*9.9, -1.05) {0};
  \node[lbl, anchor=east] at (0.05, -1.05) {gate $g$};

  % 箭头:带图卡片 → ΔKV → attention;其余列直接向上(冻结)
  \foreach \i in {4,5,6,7} \draw[oarr] (\dx*\i, 0.30) -- ($(dkv.south)+(\dx*\i-2.42,0)$);
  \draw[oarr] (dkv.north) -- (dkv.north |- attn.south);
  \draw[arr] (\dx*2, 0.30) -- (\dx*2, 1.55) -- (\dx*2, 1.55 |- attn.south);
  \draw[arr] (\dx*8.35, 0.30) -- (\dx*8.35 , 2.5);
  \draw[arr] (\dx*9.9, 0.30) -- (\dx*9.9, 2.5);

  % 底注
  \node[slbl, anchor=north west, align=left] at (-0.2, -1.5)
    {gate is $1$ only on restored history-image tokens;\\
     $B{=}0 \Rightarrow g\equiv 0 \Rightarrow$ bitwise-exact frozen policy};
\end{scope}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Panel (b): 条件二次决策(pass 1 / selector / pass 2)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\begin{scope}[shift={(8.3,0)}]
  \node[lbl, font=\small\bfseries, anchor=west] at (-0.3, 3.55)
    {(b) conditional re-decision: 2nd pass only on displacement};

  % pass 1 行
  \node[slbl, anchor=east] at (0.16, 2.75) {pass 1};
  \foreach \i in {1,...,5} {\evcard{0.47*\i}{2.75}{0}}
  \foreach \i in {6,...,9} {\evcard{0.47*\i}{2.75}{1}}
  \node[draw=ink, rounded corners=2pt, inner sep=2.5pt, font=\scriptsize, anchor=west]
    (pol1) at (4.75, 2.75) {policy \frozenmark};
  \draw[arr] (0.47*9+0.24, 2.75) -- (pol1.west);
  \node[lbl, anchor=west] (a1) at ($(pol1.east)+(0.18,0)$) {$a_1$ proposal};
  \draw[arr] (pol1.east) -- (a1.west);

  % selector 盒(橙 = 被训练的部件)
  \node[draw=accO, very thick, rounded corners=2pt, fill=fillO, align=center,
        font=\scriptsize, inner sep=3.5pt] (selb) at (2.6, 1.45)
    {selector (trained):\\scores all events given $a_1$, recency, chosen set};
  \draw[arr] (1.9, 2.45) -- ($(selb.north)+(-0.7,0)$);
  \draw[arr] (a1.south) |- ($(selb.east)+(0.35,0.1)$) -- ($(selb.east)+(0,0.1)$);

  % 分数条 + S*
  \node[draw=ink, rounded corners=2pt, inner sep=3pt, anchor=north] (ss) at (2.6, 0.55) {%
    \begin{tikzpicture}[baseline]
      \foreach \i/\h in {1/0.12, 2/0.32, 3/0.12, 4/0.10, 5/0.16, 6/0.28, 7/0.13, 8/0.26, 9/0.30}
        \draw[ink] (0.30*\i,0) rectangle (0.30*\i+0.18,\h);
      \fill[accO] (0.30*2,0) rectangle (0.30*2+0.18,0.30);
      \foreach \i/\h in {6/0.26, 8/0.24, 9/0.28}
        \fill[accB] (0.30*\i,0) rectangle (0.30*\i+0.18,\h);
    \end{tikzpicture}};
  \draw[arr] (selb.south) -- (ss.north);
  \node[lbl, anchor=west] at ($(ss.east)+(0.08,0)$) {$S^{*}$, $|S^{*}|{=}B$};

  % 分支
  \node[lbl, anchor=north] (br) at (2.6, -0.35) {$S^{*}$ keeps recent tail?\; yes $\to$ emit $a_1$};
  \draw[arr] (ss.south) -- (br.north);

  % pass 2 行
  \node[slbl, anchor=east] at (0.16, -1.45) {pass 2};
  \foreach \i in {1,3,4,5,7} {\evcard{0.47*\i}{-1.45}{0}}
  \evcard{0.47*2}{-1.45}{2}
  \foreach \i in {6,8,9} {\evcard{0.47*\i}{-1.45}{1}}
  \node[draw=ink, rounded corners=2pt, inner sep=2.5pt, font=\scriptsize, anchor=west]
    (pol2) at (4.75, -1.45) {policy \frozenmark};
  \draw[arr] (0.47*9+0.24, -1.45) -- (pol2.west);
  \node[lbl, anchor=west] (a2) at ($(pol2.east)+(0.18,0)$) {$a_2$ final};
  \draw[arr] (pol2.east) -- (a2.west);
  \draw[arr] (br.south) -- node[right, font=\scriptsize]{no: re-render, same weights} (2.6, -1.05);

  % 图例
  \node[slbl, anchor=north west, align=left] at (-0.3, -2.1)
    {\textcolor{accB}{$\blacksquare$}\,recent w/ pixels\quad
     \textcolor{accO}{$\blacksquare$}\,restored distant\quad
     \tikz{\draw[slate, dashed, rounded corners=1pt] (0,0) rectangle (0.24,0.18);}\,summary only\quad
     \frozenmark\,frozen};
\end{scope}
\end{tikzpicture}
\end{document}
