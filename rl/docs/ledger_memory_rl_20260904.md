# CausalCache 记忆控制器 RL 线 实验台账(截至 2026-09-06 18:55 PT)

本文件是 RL 线的**唯一现行判断清单**。历史日志(`cua_lite_integration_20260825.md` §4.x、
`summary_retrieval_design_20260831.md` §1–28、`rl_recipe.md`、`audit_ledger_20260809.md`)只作
证据仓库,**与本文件冲突时以本文件为准**;本文件每次判断变更都要写进 §4 并注明推翻了谁。

## 0. 一句话现状

**当前主线:Stage I(动态 B)**——文本推理历史恒定保留,控制器学『何时该存图、存哪几张』,平时 B=0
(用户 2026-09-04 11:10 PT 定案,J11;J7 撤销)。executor = 冻结的 UI-Venus-2-9B(hyper00 GPU1/GPU3 两台
vLLM),标注中(1,726 状态 × 23 上下文,约 4 小时)。可得区间已量化:B=0 已有 90.4%、∃正确对 97.7%,
且 72% 的步对上下文完全不敏感。历史四条 RL 线的判死/停线结论见 §2 J1–J9。下一个决策点 = Stage I 的
未见模板闸门(§5 11:10 PT 条目)。

## 1. 三十秒背景

问题:冻结的 GUI executor 之上,能否**学**出一个记忆控制器(每步从历史截图里选 B 张喂给
executor),使第三方记忆 benchmark(MemGUI-Bench,128 题闭环,LLM 判分)显著提升、常规 benchmark
(MobileWorld / AndroidWorld)不退化。成功 = MemGUI Pass@1 显著高于同一 executor 的默认历史窗,
且方法是学出来的(RL 或密集反馈的 bandit),不是手写规则。

## 2. 当前生效的判断(冲突消解后的唯一版本)

| # | 判断 | 证据(可 grep) | 推翻了什么 |
|---|---|---|---|
| J1 | **iter_59 能利用非连续历史帧**。oracle 双帧−recency 的 gold 动作 logprob:底座 +0.187,iter_59 +0.255,配对差 +0.068 显著(80 状态);行为口径 300 状态:recency 35.5%、仅当前帧 31.4%、oracle 对 53.2% | summary §10 表、§17 表 | 09-04 02:30 PT 我说的"executor 用不动非连续帧" |
| J2 | **随机远帧有害**:随机对 29.6% < recency 35.5% < oracle 53.2%;executor 只在选得准时受益 | summary §17 | — |
| J3 | **selector 精度跨模板不泛化**:密集行为标签监督预训练,宽口径(域内)+5.1pp 胜 recency,严口径(跨模板)+0.2pp 持平;训练数据减半不掉 → 数据量不是瓶颈,表征是 | summary §19–20 | "再标注扩量"的路线 |
| J4 | **终局 0/1 回报的 selector RL 在冻结 executor 上无信号**:RLOO 组 70% 零对比;22 轮策略熵 4.05≈均匀 4.19;KL(final‖init)=0.026;argmax 一致 61.5%;heldout 贪心 RL=init=recency 15.8% | summary §25–26.6 | §25"heldout 连续上行"(配对复测后判噪声) |
| J5 | **任务级地板**:MobileWorld heldout-20 有 11 题四次评测全零;MemGUI(GUI-Owl-1.5-8B,50 步口径)两臂各 7/128,118 题两臂同败,82% episode 跑满上限 | summary §26.1、§27.9 | — |
| J6 | **random-S 格式 SFT 不做**:它针对"executor 不会读非连续帧",J1 证明该前提对 iter_59 不成立 | 由 J1 推出 | 09-04 02:30 PT 我提的"先 SFT"(用户已批,本条撤回并已告知) |
| J7 | ~~Stage I 暂停~~ **已由用户 2026-09-04 11:10 PT 推翻:先做 Stage I**。原搁置理由(同数据的监督预训练没泛化)不成立——监督模仿 oracle 标签学的是模板捷径,与 RL 在自身分布上优化反事实回报不是一回事;外审也把『oracle 余量大而学出的 selector 崩』列为『该重做 selector』的分支 | 用户裁定;reviews/two_stage §3;§19–20 | 我 09-04 02:30 PT 的 J7 |
| J8 | **MemGUI 对外基线一律引用论文 11.7% / 15.6%**;我们的 5.5% 是 50 步上限口径,只作内部配对 | §27.9–27.10;用户裁定 | — |
| J9 | **判分只保留 Pass@1 所需调用**(逐步描述 + 终判),IRR/BadCase 关闭;终判模型 gemini-3.1-pro-preview 替代 2.5-pro 须脚注 | §27.10;开关已入库 | — |
| J10 | **下一步 = 换 executor 为 UI-Venus-2-9B**(MemGUI 62.6、AndroidWorld 80.2、MobileWorld 65.8;权重公开 18.8GB,Qwen3.5-9B 底座,vLLM 镜像已支持;权重许可证"待确认")。**闸门按外审改判并预注册**(reviews/executor_swap_review_20260904.md §3):G0 闭环地板 recency-2 ≥ 40%(117 题);G1 剂量/选择臂 N_IMG 0/2/8 + 启发式非连续 B=2 只作诊断;**G2 主闸门 = heldout-39 轨迹上离线 oracle-2 − recency-2 ≥ +5pp 且 random-2 ≤ recency-2 + 2pp**;MemGUI 不参与任何决策 | 外审 2026-09-04 03:40 PT;HF 仓核验 03:00 PT | 03:30 PT 版 J10 的"满历史 − 最近两帧 ≥ 3pp"闸门(外审指出不是命题的必要条件) **[08:50 PT 结果]** G0 ✓(recency-2 45.7%);G1 剂量 42.2→45.7→47.4,启发式净负 6 但救 6;G2 形式 ✓(+5.8pp)但赢家诅咒对照显示视觉记忆特有可救状态仅约 4%。**去留待用户裁决(§8)** |
| J11 | **控制对象 = 动态 B(用户 2026-09-04 11:10 PT 定案)**:文本推理历史恒定保留,平时 B=0 不存图,只在需要视觉记忆时才取 B 张。取代我提的『文本与帧的预算分配』(用户判:记忆方向已拥挤,且探针本身表明保留文本即够,分配问题不成立) | 用户裁定;§5 10:40 PT 与 08:50 PT 的数据 | 我 09-04 10:45 PT 的 §8 推荐 |
| J12 | **UI-Venus-2 的 MemGUI 62.6 / MobileWorld 65.8 是他方自报,不进我们的任何基线表**(只可标注为『他方自报』)。理由:(a) MemGUI 仓库 `site/leaderboard.json` 只有 8 条、最高 M3A 32.8,Qwen3-VL-235B 仅 23.4,62.6/77.3 只出现在 README 新闻栏的第三方自报;(b) 我们按官方协议、官方 mobile 示例逐条移植,同 117 题 50 步测得 45.7%,比其自报低 20pp,复现不出;(c) 时间线上 MemGUI 2026-02-09 公开,UI-Venus-2 2026-08 才发布(Qwen3.5-9B 底座),GUI-Owl-1.5 2026-02-15 与 benchmark 同期——两者不在同样的『benchmark 是否已公开』条件下。**我们对外只用自测的配对数**(同为 MobileWorld heldout 口径:Venus recency-2 39.5%/38 题;GUI-Owl 三臂 9/36、9/35、12/35 = 25–34%,来自 §4.15 期的联合训练 checkpoint) | 本地 leaderboard.json;§5 07:05 PT;arXiv 2609.00028 / 2602.06075 | 此前把 62.6 当作『换 executor 的理由之一』的表述 |
| J13 | **主张按外审弱化(2026-09-04 12:50 PT)**:不是『训练造出了利用非连续帧的能力』(冻结模型本来就会用 oracle 对:Venus 去文本 +30.0pp、GUI-Owl +17.7pp),而是**『配对的历史采样训练会改变 agent 从检索到的截图中获得的收益;因此冻结 agent 的比较不必然预测联合适配系统的排名与表现』**。核心实验为三臂配对 SFT + 固定选择器交互效应 | reviews/history_layout_sft_review_20260904.md | 我 09-04 11:40 PT 的『训练造出能力』表述 |

## 3. 当前生效的假设与口径

| 假设/口径 | 取值 | 谁定的 | 依据 | 若错会怎样 |
|---|---|---|---|---|
| 记忆预算 B | 2 张历史帧 + 当前帧 | 外审+用户(09-01) | 可枚举 C(n,2),可变 B 排在固定 B 之后 | 结论只对 B=2 成立 |
| 研究范围 | 文本动作/推理历史之上的**视觉情景记忆选择**(Venus N_IMG=0 仍保留 assistant 文本历史) | 外审(09-04) | Venus 官方协议 | 不得表述为"记忆 vs 无记忆" |
| 探针考场 | MobileWorld GUI-only 全部 117 题(train 78 + heldout 39;executor 探针不涉及 selector 训练故可全用),heldout-39 另报;50 步上限 | 外审(09-04) | 39 题一题 = 2.56pp,分辨不了 3pp | — |
| 解码参数 | temperature 0.0 + repetition_penalty 1.05 + frequency_penalty 0.3(官方离线示例脚本原配);`CC_VENUS_TEMP` 可改 | 官方示例 + 我方实测 | temp 1.0 实测 19.8%(动作解析崩坏),已排除 | 绝对分与论文差 20pp 的原因仍未定位 |
| MobileWorld 切分 | 按主 app 分层 2:1,train 78 / heldout 39,seed 20260825 | 我选的 | `cc_recipe/fixtures/mw_split_v1.json` | heldout 含训练模板则泛化数字虚高 |
| 最小值得效应 MDE | +3pp 绝对 | 外审功效论证 | rl_recipe §6 | 更小效应视为未证 |
| "可控上下文"定义 | recency 对错、∃某帧对使贪心解码对(等价类匹配) | 我选的 | summary §26.2:1,853 中 452 个(24.4%) | ∃ 带赢家诅咒,真可控率偏低 |
| MemGUI 分数口径 | 成功/128,缺失计失败;判错另列 | 我选的 | `memgui_tally.py` | 与榜单 Pass@1 定义一致 |
| MemGUI 步数预算 | **官方 golden×2.5+1**(脚本默认);09-03 两臂用的固定 50 已判偏差 | 官方 | `core/runner.py:87` | 固定上限会截断 90/128 题 |
| 判分模型 | 逐步 gemini-2.5-flash;终判 gemini-3.1-pro-preview | 用户裁定(2.5-pro 账号门控) | §26.7 | 与榜单判分不完全同源,须脚注 |
| 判分费用 | **硬停止**:未获预算不发起任何 judge 调用 | 用户(09-04 01:00 PT) | §27.9 事故 | — |
| selector 特征 | 冻结视觉塔均值池化 + 文本 embedding 均值 | 我选的 | summary §12/§19 | 已判为泛化瓶颈(J3) |
| 榜单对照 | **官方榜单**(leaderboard.json):M3A 32.8、Qwen3-VL-235B 23.4、GUI-Owl-1.5-8B 11.7/15.6。**他方自报**(README 新闻栏,不入基线表):UI-Venus-2-9B 62.6、Qwen-UI-Agent 77.3 | 第三方 | `memgui/site/leaderboard.json` 与 README | 见 J12 |
| 主机/算力 | hyper00,账号 4 卡上限;executor 单卡 vLLM | 全局规则 | — | — |

## 4. 已剪枝与未做的事

| 被剪掉的 | 方式 | 为什么 | 风险(偏向哪边) |
|---|---|---|---|
| MemGUI 官方步数口径重跑 | 不做 | 用户裁定,费用与价值不匹配 | 我们的 5.5% 偏悲观,不对外用 |
| AndroTMem 离线探针线 | 全部删除 | 用户裁定:非闭环 | 失去"oracle 记忆上界"的第三方度量 |
| oracle 记忆的闭环任务级上界 | 未测 | 无 oracle 帧标注的闭环考场 | 项目余量未知,靠 J10 探针间接判 |
| IRR / BadCase | 关闭 | Pass@1 不需要 | 无榜单 IRR/FRR 列 |
| per-step 状态依赖 baseline | 未做 | 联合线停线 | 联合线方差偏大 |
| 非法动作(double_tap)训练侧惩罚 | 未做 | 联合线停线 | 联合线绝对分偏低 |
| 难度优先采样真跑验证 | 未做 | 联合线停线 | 该机制效果未知 |
| 可变 B | 推迟 | 固定 B 未过闸 | — |
| 池 p00–p31 | 已停(CPU 让 MemGUI 后端) | 探针需重启 8–16 台 | 重启约十分钟 |

## 5. 实验台账(时间倒序,只列改变判断的条目;细节指向源文档)

> 时间标签更正(2026-09-06 17:10 PT 写):本节 2026-09-06 标为 **16:55 PT 至 18:55 PT** 的条目,实际写于 UTC 23:05–23:56(= 16:05–16:56 PT),
> 标签比真实时刻快约 50–60 分钟;各处引用("16:55 PT 条目"等)以标签为键,顺序与内容不受影响,标签不改。机器日志一律 UTC,以日志为准。


### 2026-09-06 18:55 PT ★ PartMatch v3(Documents,六候选网格)GUI-Owl 自然前缀:方向同前,n=12、gold 58%,只作辅助
- GUI-Owl 自然前缀(候选列表第一次上屏为决策步),12 个有效 checkpoint:text_only 0.250、rec2 0.083、irr2 0.083、ctrl_keep 0.167、**src_keep 0.417**、
  swap_keep 0.333、gold_text 0.583。src − ctrl +25.0 [+7.1, +46.2],src − rec2 +33.3 [+9.1, +60.0](8 对聚类)。方向与 Pictures 版(16:10 PT)一致,
  但 gold 58% 远低于 85% 有效性门槛(六候选网格底行被切,GUI-Owl 也常看不到 cand_e/f 的文件名),只作辅助;外部效度主臂等 v4(四候选)。
- Venus 在该布局上先滑动再点,矩阵不可用(已记);v4 链已接力(装任务 → Venus 采集+矩阵 → GUI-Owl 采集+矩阵)。

### 2026-09-06 18:30 PT ★★★ 交互对照(外审点名):同格式的一行历史只差"事实留/删",图的价值只在事实被删时出现——交互项 +46.7pp
- Venus,Mail 30 checkpoint,历史文本 = 只留 action;"留"= 证据轮加一句 `Noted: <事实>`(同长度同风格,只差这句);所有图条件带请求帧。

| 一行历史里的事实 | 不给图 | 同龄对照轮 | **证据轮** | 文本直接给答案 | src − ctrl |
|---|---|---|---|---|---|
| **删**(只留 action) | 0.000 | 0.000 | **0.567** | 0.933 | **+56.7** |
| **留**(action + "Noted: 事实") | 0.933 | 0.800 | 0.900 | 0.967 | +10.0 |

  交互项 [(src−ctrl)_删 − (src−ctrl)_留] = **+46.7pp,95% CI [+28.1, +65.5]**(按 (族, 对) 16 簇聚类 bootstrap;按 8 簇更保守的区间同量级)。
- 读法:(1) 审稿人攻击"你只是把答案删了"被正面回答:格式、长度、风格、提示形态全部不变,只差一句事实——**事实在文本里时图几乎没用
  (+10,且老对照帧还略有害:0.933 → 0.800),事实不在时证据帧救回 57%**;(2) 这就是外审要的主图交互项;(3) 一句 "Noted: <事实>" 只要
  约 20 token,就把 93% 的决策带到位——"按信息类型分配模态"的效率结论再次成立:能写成文字的事实,写一句最省。
- 至此准确率制式的证据链:充分性闸(证据留存 100/97/10% ↔ 源帧收益 +3/+7/+53)、交互对照(+46.7)、三个模型-协议组合复现(53/43/37.5 vs 0/0/12.5)、
  孪生反事实(共享请求版 40% 翻转)。剩余:自然分布外部效度(PartMatch v4 ≥30 有效 checkpoint、gold ≥85%)与延后揭示版(设计稿 §7,待外审)。

### 2026-09-06 18:05 PT ★★ 第二轮外审(`reviews/fidelity_axis_review_20260906.md`):主张改口径为"证据是否留在文本里"的充分性闸;补交互对照
- 它同意:机制成立、教师强制对条件机制有效、效率结果保留但改为模态分配。它反对且我改判:(1) "保真度轴"名不副实——首句模式的目标值留存
  29/30 与整段几乎相同,数据只区分"充分 / 不充分"两档,横轴应画实测证据留存率而非文本长度;(2) 缺一个对照:full/首句/action 之间压缩、风格、
  提示形态、事实删除四件事一起变,审稿人可说"这只是把答案删了";要**同长度同风格的一行历史,只差目标事实留/删,交叉源帧/对照帧**,估计交互项
  [(src−ctrl)_删 − (src−ctrl)_留] > 0;(3) 教师强制要叫"受控回放复现",不得说"自然一行摘要 agent 获得 +43";(4) 最强的非平凡对照是延后揭示。
- 落地:评测器加 `--text-mode action_fact`(只留 action,证据轮加一句 "Noted: <事实>"——与 `action` 模式同长度同风格,只差事实),链
  `pilot_venus_fact_interaction.sh` 已发射(Venus,Mail 30,text_only / ctrl_at_turn / src_at_turn × {删, 留});主张与故事措辞改;延后揭示任务
  进入设计队列(需用户模拟器端点)。

### 2026-09-06 17:50 PT ★★ 预算曲线第二段(缩放检索帧):对文本可写的事实,"以图换文"在效率上输给压缩文本;帧本身很便宜
- Venus,Mail 30 checkpoint,只留 action + 请求帧 + 证据轮,请求帧与证据帧同比例缩放;对照三种纯文本配置。prompt_tokens 含当前屏(≈2.5k)与系统提示。

| 配置 | 命中率 | prompt_tokens | 相对"整段文本不给图" |
|---|---|---|---|
| 整段文本,不给老图 | **1.000** | 6,048 | — |
| think 首句,不给老图 | 0.867 | 4,542 | −25% |
| 只留 action + 请求帧 + 证据轮,原尺寸 | 0.567 | 9,091 | +50% |
| 同上,边长 0.5 | 0.533 | 5,283 | −13% |
| 同上,边长 0.35 | 0.500 | 4,615 | −24% |
| 同上,边长 0.25 | 0.333 | 4,295 | −29% |

- 读法:(1) 缩到 0.35 时两张历史帧只要约 700 token(每帧约 350),命中率仍有 50%——**检索帧本身可以很便宜**;但同样约 4.5k 的预算下,
  "think 首句"纯文本答 87%,整段文本 6.0k 答 100%。**对数字、地址这类文本可写的事实,压缩文本比缩放图更省也更准**,"以图换文"的效率主张在
  这一族上不成立。(2) 成本地板由当前屏 + 系统提示(≈3.9k)决定,历史帧在 0.35 档只占 15%,所以"扔掉最近两帧"仍是最大单项节省(−46%)。
  (3) 效率制式的准确表述改为**按指代物类型分配模态**:文本可写的事实 → 压缩文本最优,只在摘要丢失时才回看帧;视觉指代物(零件图)→ 文本
  描述是否够用待 PartMatch v4 出数(Venus 会把形状写成文字,"dark gray triangle with a circle",这一族是否仍需图,正是 v4 要回答的)。
- 主张定型(准确率制式不变):**帧的价值 = 文本没记住的那部分;要保留的是请求轮 + 证据轮,不是最近两轮**。效率制式改写为上述模态分配,
  不主张"图比文本省"。

### 2026-09-06 17:35 PT ★ QuoteRecall 共享请求版(孪生只差数字):干净的孪生翻转 40%
- Venus,只留 action + 请求帧,n=11(16 题中 11 个有效 checkpoint):text_only 0 / rec2 0 / ctrl 0 / **src_at_turn 0.455** / swap→孪生答案 0.400 /
  gold_text 0.636;src − ctrl = +45.5 [+9.1, +83.3](7 对聚类);**孪生翻转联合概率 0.400**(源帧→本版答案且换成孪生源帧→孪生答案),
  此前请求随 TWIN 变的版本只有 0.0–0.18。读法:请求相同、只换证据图,答案跟着图上的数字走——这是"决策随证据帧走"的干净反事实。
  限定:n=11、7 对;gold_text 64% 低于 85% 门槛(该族 Compose 屏上给了答案也只六成会打字),绝对值偏低但方向一致。
- PartMatch v3(Documents 列表视图)Venus 矩阵全部 0 含 gold_text → 端点(点击行号映射)与实际布局不符,重标定后离线重判(decodes 已存,不重跑)。

### 2026-09-06 17:30 PT ★★★ GUI-Owl 教师强制第二轮:同样两帧的预算下,"请求轮 + 证据轮"43% vs 默认"最近两轮"0%
- 同 16:55 PT 的 30 个 checkpoint(GUI-Owl 沿 Venus 轨迹自写一行 conclusion,部署协议保留文本),加"保留哪几轮"的组合条件:

| text_only | rec2(最近两轮) | req_keep(仅请求轮) | ctrl_req_keep(请求轮 + 同龄无证据轮) | src_keep(仅证据轮) | **src_req_keep(请求轮 + 证据轮)** | swap_keep | gold_text |
|---|---|---|---|---|---|---|---|
| 0.000 | 0.000 | 0.000 | 0.000 | 0.200 | **0.433** | 0.214 | 1.000 |
| 3,919 tok | 9,099 tok | 6,512 | 9,084 | 6,515 | **9,108** | 6,497 | 3,932 |

  闸门(16 对聚类):src_req − ctrl_req = src_req − rec2 = src_req − text_only = **+43.3 [+26.7, +59.4]**;src_req − swap = +21.4 [+3.6, +39.3]。
  分族:OrderAddressJoin 0.43,QuoteRecall **0.44**(上一轮 0.06——补上请求轮后与订单-地址族持平,证实 QuoteRecall 的瓶颈是丢了"要哪一项")。
- 读法:**在完全相同的上下文预算(两张历史图,约 9.1k token)下,控制器保留"请求轮 + 证据轮"做对 43%,默认保留"最近两轮"做对 0%**;
  只保留请求轮或只保留证据轮各自不够(0% / 20%)——两张图必须是"对的两张"。文本直接给答案 100%,能力不是瓶颈。这是准确率制式下最直接的
  一句话结论,也是"视觉不该默认最近图"的部署形态:B 不变,换的是选哪几帧。
- 三个模型-协议组合上的同一形态已齐:Venus 只留 action(请求 + 证据轮 53% vs 其他 0)、GUI-Owl 教师强制 Mail(43% vs 0)、GUI-Owl PartMatch
  自然前缀(37.5% vs 12.5%)。

### 2026-09-06 17:25 PT ★★ 预算曲线第一段(Venus,Mail 30):文本详细时扔掉最近两帧省 46% token 且精度不变;原尺寸检索帧比整段文本贵
- prompt_tokens(vLLM,含图像与当前屏)中位 × 命中率:

| 配置 | 命中率 | prompt_tokens |
|---|---|---|
| 整段文本 + 最近两帧(Venus 官方默认) | 1.000 | 11,160 |
| **整段文本,不给老图** | **1.000** | **6,048** |
| think 首句,不给老图 | 0.867 | 4,542 |
| 只留 action + 请求帧 | 0.000 | 6,523 |
| 只留 action + 请求帧 + 证据轮(原尺寸) | 0.567 | 9,091 |
| 只留 action + 请求帧 + 最近两帧 | 0.000 | 11,635 |

- 读法:(1) **详细文本制式下,默认的最近两帧是纯浪费**:去掉后 100% 不变,token 从 11.2k 降到 6.0k(−46%),这是模型无关、不需要缩图的节省,
  动态 B 最稳的那一半。(2) 一张全分辨率截图 ≈ 2,500 token,而整段 think 一轮只有 100–150 token,二十几步的全文加起来仍不到两张图——
  **原尺寸检索帧替代不了详细文本**(9.1k 答 57% vs 6.0k 答 100%)。"以图换文"只可能在缩图后成立(0.25 边长 ≈ 1/16 像素 ≈ 160 token/帧),
  或在更长轨迹上(文本每步 +100–150,帧是常数)。(3) 缩放档(0.5 / 0.35 / 0.25)本轮未产出(评测器缩放条件未触发,排查中),补跑排队。
- 用户追问"全量思考是否反而比 1–2 张图省":按实测,是——在原尺寸、二十几步的轨迹上。效率主张的准确表述:详细文本 + 最近两帧的常见配置里,
  最近两帧是纯浪费;要用检索帧替代详细文本,必须是精简帧,可行与否取决于缩到多小还能读出信息。

### 2026-09-06 17:15 PT ★★★ 只留 action + 请求帧(Venus,Mail 30):保留证据轮 53% vs 其他任何帧策略 0%,能力对照 93%——目前最干净的一组
- 同 16:00 PT 的 action 口径,但所有图条件都带上短信请求帧(模型知道"要哪一项")。

| text_only(仅请求帧) | rec2(请求 + 最近两帧) | ctrl_at_turn(请求 + 同龄无证据轮) | irr_at_turn | **src_at_turn(请求 + 证据轮)** | swap→孪生答案 | gold_text |
|---|---|---|---|---|---|---|
| 0.000 | 0.000 | 0.000 | 0.033 | **0.533** | 0.357 | **0.933** |

  闸门(16 对聚类):src − ctrl = src − rec2 = src − text_only = **+53.3 [+39.3, +67.9]**;gold_text 93.3% ≥ 85% 有效性前提**首次满足**;
  分族:OrderAddressJoin src 0.57 / swap 0.58(请求共享的干净孪生,决策完全跟证据帧走),QuoteRecall src 0.50 / swap 0.19(该批孪生请求不同,
  共享请求版 QR2 另跑)。上下文成本(prompt_tokens 中位):仅请求帧 6,523;请求 + 证据轮 9,091;请求 + 最近两帧 11,635——Venus 一帧约 2,500 token。
- 读法:文本只记动作时,"要哪一项"和"值是多少"都只在两张老截图上;**把这两轮保留下来,一半以上的决策做对,其他任何帧策略(最近两帧、同龄对照、
  无关帧)都是 0,而文本直接给答案 93%**。这就是"控制器该保留哪几轮"问题的完整形态:答案是请求轮 + 证据轮,不是最近两轮。三个预注册闸门
  ①②(原生文本口径的 source − ctrl / source − rec2)在"只留 action"这一制式下以 +53pp 通过,⑤ 孪生翻转在请求共享的 OrderAddressJoin 上
  58% 跟孪生走。
- 预算读数(为效率制式铺垫):同一决策,整段文本不给图 ≈ 4k token 答 100%;只留 action + 两张老帧 ≈ 9k token 答 53%——原尺寸截图比整段文本贵,
  效率主张要靠缩放帧(预算曲线链正在跑:0.5 / 0.35 / 0.25 边长)。
- QR2(QuoteRecall 共享请求版)的无文本口径同样 gold=0(协议分布外),不引用;其 action + 请求帧版排在 GUI-Owl 第二轮之后。

### 2026-09-06 16:55 PT ★★★ GUI-Owl 教师强制复现(一行摘要 agent,Mail 两族 30 checkpoint):不给图 0% → 保留证据轮 23%,能力对照 100%
- 做法:沿 Venus 跑出的 30 条 Mail 轨迹逐屏让 GUI-Owl 按自己的部署协议(最近两帧 + 每步一行 conclusion)写回复,动作按 Venus 原路执行
  (`pilot_owl_tf.py`),得到"一行摘要 agent"在同一串屏幕上的因果前缀;再在同一批决策步上跑"保留哪几轮"矩阵(`eval_owl_tf_mail`)。
  它写的 conclusion 果然只记动作("Click on the email titled …"),`leak.hist = 3/30`。

| text_only | rec2 | ctrl_keep | irr2 | **src_keep** | src_pickimg | src_hybrid | swap_keep(按孪生答案) | gold_text |
|---|---|---|---|---|---|---|---|---|
| **0.000** | 0.000 | 0.000 | 0.000 | **0.233** | 0.267 | 0.267 | 0.250 | **1.000** |

  闸门(16 对聚类):src − ctrl = src − rec2 = src − text_only = **+23.3 [+6.7, +42.9]**,四个原生文本闸全部过线(这是保留文本的部署协议!);
  分族:OrderAddressJoin src 0.43 / swap 0.42,QuoteRecall src 0.06 / swap 0.12。上下文成本(prompt_tokens 中位):text_only 3919,+1 帧
  (src_keep)6515,+2 帧(rec2)9099——GUI-Owl 一帧约 2,600 token。
- 读法:(1) **在保留文本的部署协议下,一行摘要 agent 记不住值,不给图 / 最近两帧 / 同龄对照轮 / 无关帧全部 0;把当年看证据那一轮连图带回复
  保留,救回 23%;文本直接给答案 100%——能力不是瓶颈,缺的只是信息**。这是 13:35 PT "文本够用"结论的对立面,同一批屏幕、只换 agent 的
  文本记法。(2) QuoteRecall 只有 6%:它的 conclusion 同样丢掉了短信里"要哪一项"(OrderAddressJoin 的请求写在 Compose 主题行上,所以 43%)。
  控制器该保留的是**请求轮 + 证据轮**(B=2~3);已加 `req_keep` / `src_req_keep` / `ctrl_req_keep` 条件,第二轮排在预算曲线之后。
  (3) 换成孪生的证据轮 25% 跟孪生答案走,与 src 23% 对称。
- 至此三条独立证据指向同一形态(Venus 只留 action 33%、GUI-Owl PartMatch 37.5%、GUI-Owl 教师强制 Mail 23%,对照全 0 或近 0)。

### 2026-09-06 16:45 PT ★ 用户定案:论文空间 = "各家 GUI agent 的记忆管理不同"这一轴上的两个制式
- **用户的话**:各个 GUI agent 的 memory 管理不一样,有的 action summary 非常详细,有的非常简略。简略的那类,视觉回看补信息(准确率);
  **详细的那类,要做的是看能不能用精简的 vision token 在效率上超过详细的 summary 文本**。
- 落成实验(默认推进;实验设计与下一批结果一起送外审):**预算曲线**——同一批 Mail 30 checkpoint、同一个 Venus,横轴 = 上下文 token
  (vLLM `usage.prompt_tokens`,含图像 token,逐条件记录),纵轴 = 命中率。臂:整段文本不给图(详细摘要基线,100%);think 首句不给图;
  只留 action + 请求帧 + 源帧,源帧边长缩放 1 / 0.5 / 0.35 / 0.25(像素数 1 / 1/4 / 1/8 / 1/16,图像 token 随之降);只留 action + 请求帧 +
  最近两帧;只留 action + 对照帧。判据:若某个缩放档的"只留 action + 精简源帧"在 token 数低于整段文本时命中率不显著低于它,则"精简视觉
  token 胜过详细文本"成立;若必须原尺寸才追得上,则效率主张只对图像 token 便宜的模型成立。评测器已加 `--scales` / `--conds` / `ptoks`。
- 与两个制式对应的两条主张:(a) 简略摘要制式(GUI-Owl 一行 conclusion、只留 action 的 Venus):值会丢,老源帧优于最近帧、决策跟证据走
  (准确率主张,已有证据);(b) 详细摘要制式(Venus 官方):文本冗长而截图冗余,用精简的检索帧替换冗长文本可在同精度下省 token(效率主张,
  预算曲线待出)。控制器(动态 B)在两个制式下的触发条件不同:前者按"摘要缺什么"取帧,后者按"预算"以帧换文。
- 队列(hyper00,本线 ≤2 卡):GUI-Owl 教师强制(GPU1,进行中)→ 只留 action + 请求帧重跑 → 预算曲线;并行:PartMatch v3 / QuoteRecall 共享请求版(GPU2)。

### 2026-09-06 16:00 PT ★★★ 文本保真度扫描(Venus,Mail 30 checkpoint):视觉余量随文本保真度下降而出现,曲线成形
- 同一批 checkpoint、同一个模型,只改历史文本的保真度(`--text-mode`):full = 官方协议整段 think+action;oneline = 每轮只留 think 首句 + action;
  action = 只留 action;none = 无文本口径(带请求帧)。帧条件按"保留哪几轮"。

| 文本保真度 | 值仍在文本里 | text_only | rec2 | ctrl_at_turn | **src_at_turn** | swap→孪生答案 | gold_text | src − ctrl(16 对聚类) |
|---|---|---|---|---|---|---|---|---|
| full(整段) | 30/30 | 1.000 | 1.000 | 0.967 | 1.000 | 0.143 | 1.000 | +3.3 [0, +10.3] |
| oneline(think 首句) | 29/30 | 0.867 | 0.900 | 0.867 | 0.933 | 0.214 | 0.767 | +6.7 [0, +17.2] |
| **action(只留动作)** | **3/30** | **0.000** | **0.000** | **0.000** | **0.333** | 0.286 | **0.967** | **+33.3 [+15.6, +53.3]** |
| none(无文本 + 请求帧) | 0 | 0.000 | 0.000 | 0.000 | 0.267 | 0.071 | 0.233 | +26.7 [+10.7, +43.8] |

- 读法:(1) **只要文本里还有值(full / Venus 的 think 首句),图没有余量**;(2) **文本只剩动作时,值全丢(3/30),不给图 / 最近两帧 / 同龄对照帧全部 0,
  源帧放回原轮救回 1/3,+33.3pp、区间不含 0 且过 +10 门槛;gold_text 96.7% 说明该口径下模型完全有能力,缺的只是信息**——这是"视觉通道在部署里
  被需要"的第一组干净证据(无文本口径 gold 只有 23%,是协议分布外;action 口径不是);(3) 换成孪生源帧,28.6% 跟着孪生答案走,与源帧 33.3% 几乎对称:
  模型跟它拿到的那张证据图走。(4) 该轮 action 口径**没带请求帧**(评测器只在无文本口径附请求帧),模型只能从当前屏(Compose 主题行)猜"要哪一项",
  33.3% 是下界;已改为 action 口径也附请求帧,重跑排在 GUI-Owl 教师强制之后。
- 与 GUI-Owl PartMatch(16:10 PT 条目,一行 conclusion 天然不含所见)互为印证:**帧的价值 = 文本没记住的那部分**。论文主图即此曲线:x = 文本保真度,
  y = 源帧轮相对最近两帧 / 对照轮的净救回;"该保留哪一轮"只在曲线右侧成为问题。
- 另:QuoteRecall 共享请求版重采完成,原生文本口径 text_only 1.000(n=11),与预期一致;其无文本口径与 PartMatch v3、GUI-Owl 教师强制在跑。

### 2026-09-06 16:10 PT ★★★ 用户指正:"text 够用"是 Venus 全文转录协议的产物,一行摘要的 agent 记不住;GUI-Owl 在 PartMatch 上首次出现"源帧 > 最近帧 + 决策跟证据走"
- **用户的点**:常见 GUI agent 的文本历史是每步一行摘要,没道理一行摘要能保留全部信息。**核实成立**:Venus 官方协议保留每轮完整 `<think>+<action>`
  (Mail 前缀中位 234 字符/轮,均值 556),它读到证据当即把值写进 think 首句——30 个 checkpoint 里 full 30/30、只留 think 首句 29/30 仍含答案,
  只留 action 才降到 3/30;**GUI-Owl 的一行 conclusion(中位 45 字符)写的是动作不是所见**("Click on the email titled …"),同批 Mail 任务
  32 条里只有 3 条的 conclusion 含目标值。所以 13:35 PT 的"文本把值全带到决策步"限定于**全文转录型**文本历史;一行摘要型 agent 的文本通道
  是有损的,视觉回看的余量就回来了。
- **首个正向证据(GUI-Owl,PartMatch,Pictures 网格布局,自然前缀 16 checkpoint,部署协议 = 最近两帧 + 一行 conclusion,`eval_owl_pm2`)**:

| text_only | rec2 | ctrl_keep(同龄无证据轮) | **src_keep(源帧轮)** | src_pickimg | irr2 | **swap_keep(整轮换孪生;按另一版答案判)** | gold_text |
|---|---|---|---|---|---|---|---|
| 0.125 | 0.188 | 0.125 | **0.375** | 0.250 | 0.250 | **0.562** | 0.750 |

  闸门(按 8 对聚类):src − ctrl **+25.0 [+6.2, +43.8]**(过线);src − rec2 +18.8 [−6.2, +43.8](EXPAND 级);src − text_only +25.0 [0, +43.8];
  `leak.hist = 0/16`(conclusion 里没有形状信息);gold_text 75%(有效性略低于 85% 门槛)。读法:**一行摘要记不住形状 → 不给图 12.5%;最近两帧
  (根目录/Pictures 列表)不救;把看过样品的那一轮连图带回复保留,命中翻三倍;整轮换成孪生的样品轮,56% 跟着孪生答案走**——决策随证据帧走,
  不是布局效应(ctrl_keep 同结构只有 12.5%)。限定:n=16、8 对;GUI-Owl 闭环只成功 1/16(它能选对但后续复制常失败);Pictures 网格底行被切
  (GUI-Owl 按文件名点击,端点可用,但 cand_e/f 可能看不见);孪生翻转联合概率只 6%(源帧命中与孪生命中几乎不重叠,样本小)。
- **对论文的含义**:主张不必退到"效率"——回到用户原题"记忆余量 + 视觉不该默认最近图",但加一个决定性的条件变量:**文本历史的保真度**。
  全文转录(Venus)→ 视觉冗余、余量 0;一行摘要(GUI-Owl,业界常态)→ 值丢失,老源帧 > 最近帧、决策跟证据走。这是可控实验:同一批 checkpoint、
  同一个模型,只改文本保真度(full / think 首句 / 仅 action / 无)× 帧策略(无 / 最近两帧 / 源帧轮 / 对照轮 / 孪生轮)。预期曲线:帧价值随文本
  保真度下降而上升;"该保留哪一轮"的问题只在有损摘要处出现——这就是控制器(动态 B)的触发条件,也是效率叙事的机制解释。
- **已发射**:(1) Venus 文本保真度扫描(Mail 30 checkpoint × {oneline, action}),`pilot_venus_textmode.sh`;(2) **GUI-Owl 教师强制**:沿 Venus 的
  30 条 Mail 轨迹逐屏让 GUI-Owl 按自己的部署协议写回复(动作按 Venus 原路执行),得到"一行摘要 agent"的因果前缀,再跑同一批 checkpoint 的
  "保留哪几轮"矩阵,`pilot_owl_tf.py` / `pilot_owl_tf_chain.sh`;(3) QuoteRecall 共享请求版重采与 PartMatch v3(Documents 列表视图)链照跑。
  外审:方向已变(从效率叙事回到保真度条件下的余量主张),下一轮外审在 TF 与扫描出数后发。

### 2026-09-06 15:40 PT ★★ B-pilot 第二轮:Mail 无文本口径(带请求帧)源帧有特异效应但绝对值低;PartMatch 端点与孪生设计两处返工
- **Mail 两族,Venus 无文本口径(重做,所有图条件带请求帧),n=30**:text_only(只有请求帧)0.0;rec2 0.0;ctrl_at_turn 0.0;irr_at_turn 0.0;
  **src_at_turn 0.267**;swap_at_turn 0.071;**gold_text 0.233**。闸门:source − ctrl = +26.7 [+10.7, +43.8],source − rec2 同,source − swap = +17.9
  [+7.1, +28.6](按 16 对聚类)。读法:(1) 去掉文本后,**只有源帧能救回决策,同龄对照帧/最近帧/无关帧一个都救不回**——视觉通道存在且对内容
  特异,效应显著;(2) 但 gold_text 只有 23%:即使把答案以文本行给它,无文本口径下 Venus 在 Compose 屏也多半不会去打字——该口径对 Venus
  严重分布外,绝对值被协议压低,**预注册的有效性前提(gold_text ≥ 85%)不满足**,所以这是"通道能力"的证据,不是"部署需要"的证据;
  (3) 分族:OrderAddressJoin src 0.36 / gold 0.43 / swap 0.17;QuoteRecall src 0.19 / gold 0.06 / swap 0.0。
- **QuoteRecall 孪生设计缺陷**:目标供应商与属性由 TWIN 种子抽取 → 孪生的**请求**也不同,换成孪生源帧时请求与证据不匹配,翻转率 0 是构造使然
  (OrderAddressJoin 的请求由 PAIR 给出,是干净反事实)。已改为由 PAIR 种子抽取(共享请求,只有数字变),重采 Venus 前缀 + 重跑矩阵(链已发射)。
- **PartMatch(Pictures 布局)端点失效**:模型点缩略图不报文件名,文本端点下 gold_text 也只有 3/16;改按点击坐标落格后仍不可用——Pictures 是
  两列网格、底行被切掉,Venus 决策屏上的动作有约一半是先 Swipe 再点(如 `PartMatchTask01B` 各条件全为 Swipe),决策不再是一步,且滚动后
  坐标→文件映射失效。两轮 pm2 矩阵数字(原生 0.31–0.375、无文本 0.19–0.56)**不作引用**。改到 Documents/Candidates(列表视图,六行全可见,
  按点击行号判定,行心 317/397/…/717),v3 全链排在 QuoteRecall 重采之后自动跑(Venus 采集+矩阵 → GUI-Owl 采集+矩阵)。GUI-Owl 在 Pictures 布局
  上 16 题只成功 1 题;其 pm2 矩阵(16 checkpoint,原生文本)在跑,同样只作参考。
- 现状判断不变:部署条件(保留文本)下构造任务也无余量(13:35 PT 条目);视觉通道的"能力"在无文本口径下可见但被协议压低;"需要"只能来自
  指代物不可文本化(PartMatch v3 待出)或延后揭示 + 有损摘要(待设计、待外审)。

### 2026-09-06 13:45 PT 无文本口径首轮按构造无效(请求帧缺失)→ 重做;Venus 在新布局 PartMatch 上 14/14;占卡与收尾纪律两处修补
- **无文本口径首轮**(`eval_venus_mail_notext.jsonl`,n=30):全部条件 ≈0,`gold_text` 也是 0 → checkpoint 在该口径下**无效**,不是结果。
  原因按构造:去掉文本后,"要哪一项"(短信内容)只存在于短信那一屏,而源帧集合只含证据邮件,不含请求屏;`gold_text` 的事实又是塞在被去掉的
  文本里。修正:规格构建器增加 `request_frames`(打开短信线程后的帧,须下一步文本含短信内容词确认),无文本口径下所有图条件一律带上请求帧;
  `gold_text` 在无文本口径下把事实作为文本行附在当前屏前;另加 `irr_at_turn`(源帧位置换成无关帧)。Mail 无文本重做中;首轮文件不作引用。
- **Venus × PartMatch(候选目录挪到 Pictures)**:16 题重采,已出结果的 14/14 全部做对——视觉绑定任务对 Venus 不构成障碍;矩阵(原生文本 + 无文本)
  排在 Mail 无文本之后同一台执行器上跑。
- 两处工程修补:(1) `cc_container_lib.sh` 在 `source` 时重新生成 `OWN`,接管容器的手动脚本传入的 OWN 被覆盖,`rm_own` 判"not mine"跳过删除
  (Venus 执行器 jaxan-2 因此多活了一轮,正好复用);本线创建的容器改为按名显式删除 + 清 map 行。(2) 账号占卡对账:20:37Z 另一条 session 占
  GPU0/3/4 三张、本线 GPU1/2 两张,合计 5 张超过 hyper00 的 4 卡上限——已通报对方;本线后续 GUI-Owl 重采先删自己的 Venus 采集容器再起,不增卡。

### 2026-09-06 13:35 PT ★★★ B-pilot 主臂(Venus × Mail 两族,原生文本口径):text_only 100%——文本通道把值全带到了决策步
- 数据:`eval_venus_mail_text.jsonl`,30 个 checkpoint(QuoteRecall 16 + OrderAddressJoin 14),16 对孪生(28 个有反事实)。条件按"保留哪几轮"
  主干预(13:25 PT 条目)。

| 条件 | text_only | rec2 | ctrl_at_turn(同龄无证据轮) | src_at_turn(源帧轮) | src_pickimg | irr2 | swap_at_turn(整轮换孪生;按另一版答案判) | gold_text |
|---|---|---|---|---|---|---|---|---|
| V | **1.000** | 1.000 | 0.967 | 1.000 | 1.000 | 1.000 | 0.143 | 1.000 |

- 读法:(1) **保留自写文本时,不给任何图也 100% 答对**;源帧、无关帧、对照帧全部 1.0,`source − rec2 = 0`,`source − ctrl = +3.3 [0, +10.3]`
  ——原生文本口径的四个闸门没有任何余量,**按预注册是 NO-GO(不是效应小,是天花板)**。(2) 机制直接可见:`leak.hist = 30/30`,Venus 在读到证据的
  那一步就把值抄进自己的 `<think>`(例:`OrderAddressJoinTask01A` 第 17 步 "Found it. Halcyon Plastics' shipping address: 5148 Lantern St,
  Fresno, CA",第 24 步再复述一次),决策步只是把文本里的值打出来。(3) 整轮换成孪生对应轮后只有 14% 跟着另一版走:**图与该轮文本被换掉,
  但后续各轮的自写文本仍是本版的值,模型跟文本不跟图**——文本通道优先于图像通道,与自然状态上"文本在时图几乎无用"的读数同源。
- 这正是用户 13:10 PT 的担忧的实证:**只要 executor 会把看到的东西写进文本历史,"选哪张老图"就没有价值可言**,自己造的任务也改变不了这一点。
  构造任务里视觉通道要有余量,必须同时满足两条之一:(a) 指代物不可文本化(PartMatch 类,重采中);(b) **相关性延后揭示 + 摘要有损**——agent
  读证据时不知道之后要什么,因而没把值抄下来。本 pilot 的短信步 1 可见 + Venus 逐字转录,两条都不满足,所以 Mail 两族在原生文本口径下必然
  天花板。无文本口径(跑数中)只回答"视觉通道**能不能**带",不回答"部署里**需不需要**"。
- 对论文主张的含义(待外审后定):"视觉帧选择的价值"作为主张在现有与构造任务上都立不住;可立的是**记忆的模态分配**——文本承载几乎全部记忆,
  历史图 token 大多是浪费(动态 B、默认 B=0 的效率主张,可在第三方 benchmark 上以"精度持平 + 图 token 大减"验证),视觉回看只在"指代物不可
  文本化"或"相关性延后揭示且摘要有损"两种情形有价值(构造任务作为诊断探针给出触发条件)。
- 下一步(默认推进,发外审):(1) 无文本口径与 PartMatch 重采出数后合并判定;(2) 设计"延后揭示 + 有损摘要"版本的 Mail 任务:浏览阶段的
  目标是模糊的("看看新邮件"),请求经 ask_user 在浏览结束后才到——需要用户模拟器端点(41002)或把请求放进需导航才可见的载体;(3) MemGUI-Bench
  上 Venus B=0/2/8 三臂(保留文本)以取第三方对照——判分预算待用户批。

### 2026-09-06 13:25 PT 用户指正:主干预改为"保留哪几轮"单一控制变量;Venus 前缀收官;两条自动链接管重跑
- **指正**:"保留某张老图"在部署布局里必然连带保留那一轮的原始回复并从折叠文本抽走——这不是要拆掉的混杂,**这就是控制变量**。采纳:主终点
  改为 `src_keep`(保留源帧所在轮)vs `ctrl_keep`(保留同轨迹同龄的无证据轮,源帧前一帧)vs `rec2` vs `text_only`;反事实 = `swap_keep`
  (源帧所在轮整轮换成孪生另一版的对应轮,判定按另一版答案);Venus 对应 `*_at_turn`。`pickimg` / `hybrid` 降为机制诊断。设计稿 §5 已改,
  `pilot_gates.py` 默认按后端取这组条件。
- **Venus 前缀收官**(20:15Z):QuoteRecall 16/16、OrderAddressJoin 15/16 做对(约 17–37 步),PartMatch 只跑出 5 题(重采覆盖)。规格构建器
  的源帧规则改回"点击含身份关键词的邮件后打开的帧"(须正文词或离开动作确认打开),因为收件箱主题行也含供应商名/订单号,按观察文本匹配会把
  收件箱列表误判为源帧(拼图核对发现);PartMatch 用观察文本里的形状/颜色描述定位查看器帧。Mail 两族 30 个 checkpoint、28 对孪生,源帧距决策步
  ≥10 步、不落最近两帧,源/对照帧拼图逐张核对(源 = 打开的目标邮件正文,对照 = 同龄收件箱列表)。
- **两条自动链接管重跑**:主臂链在规格修正前点火(错规格)→ 停掉、重建规格、接管其 Venus 容器跑 Mail 矩阵(原生文本 + 无文本);PartMatch
  重采链的安装脚本路径写错、任务类未装即开跑 → 停掉、装好新任务类(p12/p13 确认 `Pictures/Candidates`)、接管其容器重跑(Venus → GUI-Owl)。
  与另一条 session 对表:本线只用模拟器 p12/p13,GPU1/GPU2;对方 p00–p11、p14–p33 与 GPU0。

### 2026-09-06 13:05 PT ★ B-pilot 第一批读数:Venus 自然到达决策步;GUI-Owl 在 PartMatch 上的干预矩阵(旧布局,带两条保留意见)
- **Venus 前缀**(`prefix_venus/`,官方协议最近两帧):前 4 道报价题全部做对、各约 20 步——Venus 自然走到"读完短信去写邮件"的决策步,
  Mail 两族的 checkpoint 有了(GUI-Owl 48 题 3 成功、Mail 两族 0 决策步,只有 PartMatch 7/16 到决策步)。
- **评测器 bug 与修复**:file 类(PartMatch)判定被"必须是 type/answer 动作"的门挡住,冒烟全 0;解码本身正确。改为 file 类看整段输出
  是否指名目标文件;单源帧族的无关对照允许一帧;加 `--rescore` 用已存解码重判(冒烟与 16 态矩阵均离线重判,未重跑)。
- **GUI-Owl × PartMatch 16 个结构化 checkpoint**(决策步 = 看过样品后候选列表第一次上屏,不按结果选态;`eval_owl_partmatch.jsonl`):

| 条件 | text_only | rec2 | src_pickimg | src_keep | src_hybrid | swap_pickimg | swap_hybrid | gold_text |
|---|---|---|---|---|---|---|---|---|
| V(命中) | 0.188 | 0.250 | 0.250 | 0.375 | **0.500** | 0.250 | 0.375 | 1.000 |

  读法:附带 PAST 标记把样品图附进去(`src_hybrid`)命中翻倍(0.50 vs rec2 0.25),换成孪生的样品图后 0.375 跟着另一版答案走——模型确实在读
  附入的那张图;换进最近槽位(`pickimg`)则无效。这与自然状态上"hybrid 最差"相反:**在必须回看的状态上,附老帧有用**。
  **两条保留意见,数字不作闸门引用**:(1) 拼图核对发现决策步的最近两帧含 Approved 文件夹列表,其缩略图已透露样品形状,rec2 不是无证据
  对照 → 候选目录已挪到 Pictures(另一棵目录树),Venus 全量前缀跑完后自动重采 PartMatch(Venus + GUI-Owl 各 16 题);(2) text_only
  命中的恰好是 expected=cand_d 的 3 题、其余 0——GUI-Owl 对第 4 行有位置先验,text_only 的 0.188 不是记忆。
- 已排队自动链:`pilot_partmatch_recollect.sh`(等 Venus 前缀结束 → 装新任务类 → Venus/GUI-Owl 重采 PartMatch)、`pilot_eval_venus_run.sh`
  (等 Venus 前缀结束 → Mail 两族规格 → Venus 原生协议矩阵,原生文本 + 无文本)。闸门计算脚本 `pilot_gates.py`(按对聚类 bootstrap、孪生翻转、
  leak 分层)。

### 2026-09-06 12:10 PT ★★ 部署布局重测四模型齐:四个模型同构,三臂之间无可分辨差别
- 数据:`rl_v2/deploy2_{base,recency_s0,random_s0,older_s0}.jsonl`(同一批 337 个自一致状态 × 9 上下文;`results/deploy2_four_models_20260906.txt`)。
  全部按 GUI-Owl **部署布局**(交错多轮)构造,`pickimg` = 结构固定最近两轮、只把图换成裁判老帧;`hybrid` = 最近两轮原样 + 裁判老帧带 PAST 标记附入;
  裁判 = Qwen3-VL-30B / GLM-4.6V / Qwen3.8-27B 直接读全史选两帧。净值 = 相对 text-only 的逐状态救回 − 伤害(pp)。

| 模型 | text-only | 最近两帧 | 无关两帧 | pickimg 30B / GLM / Qwen3.8 | hybrid 30B / GLM / Qwen3.8 |
|---|---|---|---|---|---|
| 底座 | 70.9 | **+22.3** | +13.1 | +11.3 / +8.3 / +14.8 | +1.5 / +1.8 / +3.0 |
| RECENCY 臂 | 75.4 | +16.0 | +11.9 | +11.6 / +9.2 / +15.1 | +7.4 / +5.3 / +5.9 |
| RANDOM 臂 | 74.2 | +16.9 | +12.5 | +12.8 / +10.4 / +16.3 | +6.8 / +5.0 / +5.0 |
| OLDER 臂 | 74.2 | +17.5 | +11.9 | +12.8 / +11.3 / +16.3 | +6.8 / +5.3 / +5.6 |

- 读法:(1) **四个模型同构**——最近两帧最高;无关两帧拿走其中一半以上(格式效应);裁判老帧换进最近槽位比 recency 低 0.5–7pp(Qwen3.8 的
  选帧最接近,差 0.5–1.2pp);在 recency 之上再附老帧最差。(2) **三臂 vs 底座**:SFT 把 text-only 抬 +3.3~4.5pp,把对最近两帧的依赖从
  +22.3 降到 +16~17.5,把附加老帧的伤害从 16% 降到 10–13%(hybrid 净值 +1.5 → +5~7);但 **RANDOM / OLDER 与 RECENCY 臂之间没有可分辨的
  差别**(pickimg 差 ≤1.2pp,hybrid 差 ≤0.6pp,均远小于 ±6–9pp 的配对区间)——"训练时见过非连续帧"没有让裁判老帧在部署布局下胜过 recency。
  (3) 对论点的含义:在自然状态分布上,无论哪个臂,"选哪张老图"在部署布局下**没有余量**,与 1/337 记忆依赖态一致;论点只剩两处可立:
  Venus 无文本协议(J20)与构造任务(B-pilot)。(4) 11:10 PT 条目里 recency 臂的读数(76.1 / +16.5 / +11.4)是中途汇总,以本表为准。
- 判断更新:J22(三臂差异)在部署布局下的对应陈述 = **三臂无差别**;平铺布局内的 J/H 差异不外推。deploy2 链已自行收尾(容器删、map 清)。

### 2026-09-06 11:50 PT ★★ 外审"下一步"(读钉住 commit)+ 前缀采集诊断:底座 GUI-Owl 到不了决策步
- **外审**(`reviews/next_steps_review_20260906.md`,GPT-6 极高档,读了台账/故事/设计稿/评测器四个文件并按文件+节引用):
  (1) 下一实验 = **Venus 为主、同 checkpoint 同布局的反事实帧内容干预**;deploy2 只作外部效度背景——它的无关图 +11.4 说明部署布局分不清
  "哪张老图";(2) 闸门改硬:`V(source)−V(irrelevant)` 与 `V(source)−V(recency2)` **均 ≥ +10pp 且配对 95% CI 下界 > 0**,原生文本与无文本
  两协议都要过;孪生翻转 ≥70%(下界 >50%);`gold_text ≥85%` 为 checkpoint 有效性前提;按 24 对聚类,不要求逐族显著;**仅无文本过线 =
  对联合 RL NO-GO,但是干净的协议依赖结果**;(3) 三个真问题:评测器把 Venus 也走 GUI-Owl 的 `decode_ctx` 构造器(致命阻断)、
  `swap_hybrid` 改变了干预本身(要 `swap_pickimg`)、无关对照未匹配(主负对照应是孪生另一版的源帧);自写文本含答案不是泄漏而是竞争通道,
  要审计 goal/conclusion/文件名;延后揭示未延后对 RL 主张致命、对冻结因果测试不致命。**全部采纳**,加一档 EXPAND(点估计 ≥+10pp 但
  下界 ≤0 → 扩到 ≥100 对再判),三档预注册进设计稿 §5。
- **前缀采集诊断**(`rl_v2/pilot/prefix_base/`,18/48 完成):**全部 40 步、0 分、从不 Compose**。轨迹形态一致:前 8 步依次打开一封 PO 邮件、
  一封供应商邮件、短信线程(源帧与请求都看到了),然后 Mail↔Home 往返、反复重开同两封邮件直到步数用尽(`OrderAddressJoinTask01A`
  第 9–40 步为周期 6 的循环;`QuoteRecallTask01A` 第 12–40 步为 Mail→Home 周期 2)。Mail 种子数据本身正确(收件箱与正文截图核实)。
  结论:**GUI-Owl 自然前缀里有源帧但没有决策步**,"自然前缀 + 决策步 checkpoint"在 GUI-Owl 上不成立(J5 任务级地板的又一实例)。
- **决定(默认推进,四件套)**:问题——checkpoint 从哪来。默认——**前缀改由 UI-Venus-2 自然生成**(同 48 题、同两台模拟器、Venus 官方协议),
  这与外审"Venus 为主 + Venus 原生协议"同向;deploy2 与 GUI-Owl 前缀跑完释放 GPU 后立刻发射。理由——Venus 闭环强得多(heldout 39.5–47.4%
  vs GUI-Owl 15.8%),最可能自然到达"读完短信后去 Compose"的状态。回滚/备选——若 Venus 同样到不了决策态,退回 **teacher-forced 脚本前缀**:
  adb 脚本化导航(逐封打开证据邮件→短信→Compose 填好收件人/主题、光标在正文),每步让 executor 在被强制的屏幕上自写 conclusion(因果、
  只看已发生的屏幕),决策态 = 正文空白的 Compose 屏,源帧按构造已知、距决策步 ≥4 步。外审——本条决定在外审之后作出,外审"Venus 为主"
  已覆盖其方向;脚本前缀方案未经外审,若启用先补审。
- 评测器改造(本条目落地项):`--backend venus`(复用 `venus_oracle.messages`,含 `--no-text`)、`swap_pickimg`、`src_at_turn`(Venus 原生
  位置干预)、`leak` 三标志、无关对照改为同族另一对同步位截图。

### 2026-09-06 11:10 PT B-pilot 环境问题与载体更换;部署布局重测第一臂出数
- **Mattermost 手机端在 `init_state` 快照里未登录**(截图:"Let's Connect to a Server" → 登录页,agent 乱输凭证 40 步);池子里另一
  session 的 `MattermostCreateChannelTask` episode 同样如此(前 8 步全在连接/登录)。后端种数据是成功的(CLI 登录、消息已发),问题在
  手机端 app 会话。**Mattermost 载体在当前环境不可用**(池子与独立模拟器都一样),族 A/B 改为 **Mail 载体**:报价/订单/供应商记录
  各为一封邮件(自定义 `state.json` 推入 gmail clone,与内置 gmail 任务同法),请求仍由短信给出;族 C(Files)不变。48 个任务
  (3 族 × 8 对 × A/B)在 p12/p13 重新注册通过,前缀采集重启。**已知设计局限**:短信通知在第 1 步就可见(截图证实),"延后揭示"
  未真正延后;真正延后要走 ask_user 通道(用户模拟器端点 41002 当前无服务)或把请求放进需要导航才能看到的载体——pilot 先接受此局限。
- 独立模拟器 p12/p13 是两天前遗留的已退出容器,`docker start` 复用并登记 map;任务注册表在容器内 `/app/service` 拷贝里,
  `install_pilot_tasks.sh` 负责拷入 + 重启任务服务;注册表按文件路径加载模块,**相对导入不可用**(Mail 助手已内联)。
- 部署布局重测(`arms_deploy_chain.sh` → `decode_ctx.py`,`deploy2_*.jsonl`)第一臂 recency_s0(337 态,相对 text-only 的逐状态
  救回/伤害):recency-2 **+16.5**(救 19.5 / 伤 2.9);无关两帧 +11.4;pickimg(结构=最近两轮原文,只换图为裁判老帧):30B +12.5、
  GLM +9.2、Qwen3.8 +15.8;hybrid(最近两轮原样 + 裁判老帧带 PAST 标记附入):+6.3 ~ +8.1(伤害 9–11)。读法:**部署布局下加图是大收益,
  其中大部分是格式效应(无关图也 +11.4),内容贡献约 +5;把裁判老帧换进最近两轮的图位不明显吃亏但也不胜 recency;在 recency 之上
  再附老帧反而更差**。random/older/base 在跑。

### 2026-09-06 11:20 PT ★ 口径更正 + 绝对成功率对账(用户质疑)
- 官方榜(仓库 `site/leaderboard.json`):GUI-Owl-1.5-8B-Instruct GUI-only **38.2%**(3 runs、50 步、agent 默认 history_n=1 = **不带历史图**)。
  本项目一直称 recency-2 为"官方默认"是**错误**的:它是 8-24 P0.5 起的 B=2 约定。J14/J18/J21/J22 中"官方默认最近两帧"一律改为"B=2 约定"。
- 绝对数:8-24 对表 33.9%(同 harness、默认协议);9-05/06 单次运行 26.5%(B=2)/ 25.6%(B=0),比 8-24 低约 8pp,比官方低约 12pp。
  harness 未换(83e7b8f + parity 钩子),差距来源待定(单次方差 / 模拟器 500 / 服务配置)。已排队官方默认协议 ×3 对账。
  **对外报任何绝对成功率前必须完成此对账**;内部相对比较不受影响。

### 2026-09-06 10:40 PT 决定(用户):仓库 `luojiaxuan/CausalCache` 改为 public;外审直接读正本
- 目的:外审(网页 ChatGPT)要基于仓库原文与指定的相对文件才有意义。此前按"仓库私有是共享主机匿名规则的前提"提出脱敏镜像方案
  (`CausalCache-review` + Action 自动同步),用户认为维护成本高,决定直接公开正本;镜像与 Action 停用(导出器保留)。
- 后果与已报告项:公开前扫描——无密钥形态、无主机 IP;301 文件含共享主机名、171 文件含容器命名空间、2 处 critic-hack 引用、
  合作者邮箱(paper/main.tex 与一个审计 JSON)。**共享主机匿名规则的"仓库私有"前提不再成立**,这是用户的决定;仓库内容未擅自改。
- 外审提示词纪律同日更新:附 FORMAT 段(面向 agent、只留技术内容)+ 直接给 `blob/<sha>/<path>` 链接与相对路径清单。

### 2026-09-06 10:20 PT ★★★ 本线认领布局伪影(用户质疑"recency 变差是 bug"——是对的)
- 事实核对:本线全部 GUI-Owl 步级测量(第一章 2×2 的 GUI-Owl 两行、第二章三臂 + seed1、三裁判 54 格、救回/伤害计数)都用
  `guiowl_oracle.messages` 的**平铺布局**:images = [选中的历史帧…] + [当前帧] 连排在一条 user 消息里,再附全部 conclusion 文本。
  部署协议(`sglang_omni_rl/gui_owl/protocol.py`,`GuiOwlHistoryProtocol`)是**交错多轮**:未保留的 turn 折叠成 Step 文本;
  首条 user = 模板 + S[0] 帧;此后每个保留 turn = assistant **原始回复** + user tool_response(下一帧)。两者不是同一个协议。
- 另一 session(harm 线,J21)在部署布局下重测底座:recency-2 自复现 **93.5%**(平铺布局只有 55.8%);真实最近两帧 93.5 vs 无关帧
  84.6 vs 不给图 71.8 ⇒ **最近帧的内容被用上了**(真图比无关图 +9pp,比不给图 +17pp,CI 不含 0)。裁判选的老帧按部署协议塞入
  −3 ~ −40pp(9 格 7 格显著为负)——因为"保留哪几帧"与"哪几轮 assistant 回复保留原文"绑在一起,取老帧就拆掉最近轮的结构。
- **改判**:J14 中"GUI-Owl 视觉通道关着、recency 有害、裁判≈无关"**撤回**,改为:**GUI-Owl 的视觉记忆通道对最近帧是开的,且被
  交错格式锁死——在原生格式里检索老帧的代价是破坏最近轮次的结构**。平铺布局造成的伤害是时序歧义(harm 线:PAST 标记或灰图即可恢复)。
  第二章三臂的 J、H、"训练后 V(真实)≈V(无关)"是**平铺布局内**的陈述(训练与评测同布局,内部一致),不能外推到部署协议;
  三臂在部署布局下的裁判/最近帧重测待排(harm 线已发底座)。**Venus 结果不受影响**(`venus_oracle.messages` 就是 Venus 的
  原生交错协议,§2.7/§2.8 站得住)。
- 对路 B 的影响:checkpoint 评测器必须用 `GuiOwlHistoryProtocol`(或 harm 线的 `decode_ctx.py`)构造上下文,并把"附哪几帧"与
  "哪几轮保留原文"**解耦**成两个自变量(harm 线正在做的 pickimg/hybrid),否则重蹈覆辙。`pilot_checkpoint_eval.py` 停用平铺版。
- 对"记忆余量/不该默认取最近帧"定位的影响:在 GUI-Owl 原生格式下,recency 是**好默认**(+17 vs 不给图);"不该默认取最近帧"
  目前只在 Venus 去文本协议下成立。GUI-Owl 上的问题变成**格式**:能否在不拆最近轮结构的前提下附老帧(解耦格式)——这是 B 之前
  必须先回答的问题,也是路 2(动态 B)在 GUI-Owl 上能否成立的前提。

### 2026-09-06 10:05 PT J22 部署布局下的裁判帧结论(替代 J14 的步级部分)
- 底座 GUI-Owl,部署忠实布局,337 态:真实最近两帧 vs 无关图 = **+17.2pp(CI ±8.8)**——最近帧内容有用;裁判选的老帧作为保留 turn
  **−3 ~ −40pp**(9 格 7 格显著为负)。J14 修订为:GUI-Owl 可用的视觉记忆在其原生格式里**只有最近帧**;检索老帧在交错协议里是有害的,
  因为它拆掉最近 turn 的结构(细节与解耦实验见 harm 台账)。主线第一章"GUI-Owl 通道关着"改述为"通道只对最近帧开、且格式绑定"。

### 2026-09-06 09:45 PT ★★★ 两条更正:布局伪影 + Venus 去文本协议
- **J21 布局伪影**:harm 线按 agent 源码重建部署布局(交错多轮:文本 + 首帧 / assistant 原始回复 / 后续帧 / 当前帧)后,底座 GUI-Owl
  在 recency-2 下自复现 **93.5%**(标注器布局 55.8%)。部署布局下 N=2 真实帧 93.5 vs 无关帧 84.6 vs 不给图 71.8 ⇒ **真实帧比无关帧
  多 9 个点,历史图有益**。此前主线所有步级结论(recency 有害、裁判帧≈无关帧、训练后 V(真实)≈V(无关))都是在标注器布局下测的,
  其中"recency 有害"与"裁判≈无关"**可能是布局伪影**,须在部署布局下重测裁判帧(底座已发射;三臂待排)。J14/J19 加此限定。
  机制细节见 `rl/harm/docs/ledger_harm_20260906.md`(标注器布局的伤害 = 时序歧义,PAST 标记或灰图即可恢复;部署布局 N≥4 = 过早终止)。
- **J20 Venus 去文本协议**(`rl_v2/venus/score_venus_notext.txt`,n=311,不给图会错 68%):裁判帧净信息量 **+20.5 ~ +30.5pp,
  九格 CI 全部不含 0**(n≈140–157);救回率裁判 40–51% vs recency 24% vs 无关 21%。⇒ 去掉文本轨迹后 Venus 的视觉通道大开,
  且**裁判选的老帧远好于最近帧**(+17~+27pp)——"视觉历史不该默认取最近帧"在 Venus 无文本协议下成立。保留文本轨迹时同方向但幅度小。
- 底座四臂闭环首发因端口冲突无效(agent 收 None),已重发(见 harm 台账)。

### 2026-09-06 09:35 PT ★★ 路 C 第二半:UI-Venus-2 去文本轨迹协议——视觉通道大开,裁判帧 ≈ 2× recency,九格全部显著
- 设置同 01:10 PT 条目,协议改为去掉文本推理历史(只靠视觉历史,诊断口径非部署口径)。400 态中 311 可判(89 态 Venus 在无文本
  历史下输出坐标残缺/参考不可解析,按 verdict 规则计为不可判)。不给图会错 68.2%(212 态)。
- 救回率(不给图会错的状态上):无关帧 20.9%,**recency 24.2%**,三裁判九口径 **40.0–50.9%**。净信息量(裁判 − 无关):
  **+20.5 ~ +30.5pp,95% CI ±8–9,九格全部显著为正**。逐状态:recency 净 +9.0(救 16.0 / 伤 7.1),无关帧 +8.2,裁判帧
  **+24.0 ~ +26.5**(救 31–33 / 伤 6–7)。
- 判断 J20:**在 Venus 上,去掉文本轨迹后,精心挑选的历史帧的价值是"最近两帧"的两倍以上,且 recency 与无关帧几乎等价**
  (24.2 vs 20.9)——"视觉历史不该默认取最近帧"在这个协议下是硬结论。与保留文本协议(失败态仅 41,裁判 36–53% vs recency 54%,
  CI 宽)对照:**文本轨迹在时,视觉通道被文本替代;文本一撤,视觉通道的余量立刻显现**。这是"记忆余量的协议依赖性"最干净的一对数。
- 与 GUI-Owl 对照:GUI-Owl 去文本协议此前只有事后 oracle(57.6 vs recency 30.7),未做裁判帧/无关帧;要补同法测量才能说
  "模型属性"。采样偏置限定同样适用(状态来自四策略各自成功的轨迹);不过本条的裁判 vs 无关、裁判 vs recency 都是状态内配对比较。
- 收尾:GPU0 `sglang-omni-jaxan-1`(Venus 服务)已由链尾 rm_own 删除;三裁判 picks 与两协议解码在
  `/data01/jaxan/rl_v2/venus/`(picks_*_*.jsonl、judge_decode_venus_{text,notext}.jsonl、score_venus_*.txt)。
- 下一步(路 B 之外):用同一套裁判 picks 对 **GUI-Owl 去文本协议**补解(GUI-Owl 状态集已有裁判 picks,只需 no_text 版 messages),
  补齐"两模型 × 两协议 × 三裁判"的完整方阵。

### 2026-09-06 01:40 PT 协调记录(两条 session 同时在 hyper00 上工作;对方不在本机 ListAgents 里,靠本台账对账)
- **本 session(jolly-greider,记忆控制器线)在跑 / 在等**:GPU0 `sglang-omni-jaxan-1` = UI-Venus-2 去文本协议对三裁判帧对补解
  (110/400,约 1.5 小时);B-pilot 三个任务族已写好并在宿主 checkout 注册(`QuoteRecallTaskA/B`、`OrderAddressJoinTaskA/B`、
  `PartMatchTaskA/B`,源码在 `rl/cua/mw_tasks/`),**尚未装进模拟器容器**(注册表在容器内 `/app/service` 拷贝里;安装脚本
  `install_pilot_tasks.sh` 会重启容器内任务服务,只能在该容器空闲时跑)。
- **对方 session(底座三臂闭环 recent0/recent8/random2 + history-harm)**:占满 12 台模拟器(6800–6811,concurrency 12)与
  GPU1/GPU3(`jaxan-2`、`jaxan-5`)。本 session 08:15Z 曾在 6800 上跑 1 并发冒烟,两任务在 /task/goal 即 500,未产生 episode,
  可能插入过一次 reset;冒烟服务(`jaxan-3`)已删。
- **本 session 承诺**:对方闭环跑完(ARMS 完成标记)前不碰模拟器池;改台账前先拉最新版再写。**请求**:若可留一台模拟器
  (建议 6811)给 B-pilot 冒烟,在本条下方追加一行即可;否则我等闭环结束。
- 采样偏置(对方 01:30 PT 条目)对本线结论的影响已在汇报稿中标注:状态内配对比较仍成立,总体余量说法加"条件于成功状态"限定,
  "Venus 上 recency 最优"撤回;B-pilot 的 checkpoint 状态由构造定义,不受此偏置。

### 2026-09-06 01:30 PT ★★ 用户指出采样偏置:状态只取自"某策略下成功"的轨迹 → 记忆关键状态被系统性稀释
- 用户原话:"他在 recency B=2 时做对的 episode 里面选一步,那本身你是在测给图能 work 的 case 下替换图会发生什么。但实际上还有一些
  memory-critical,如果换了关键的图才可能做对,这部分原本是最大的提升,被你直接忽略了。"
- 承认:**正确**。GUI-Owl 337 态全部来自 recency-2 成功的轨迹;Venus 400 态来自 recent0/recent2/recent8/change2 四臂的成功轨迹。
  凡是"某策略成功"的状态,该策略在那一步就够用;只有非 recency 策略才做成的任务(记忆关键任务)在样本里要么没有、要么被稀释。
  因此此前所有"余量"都是**条件于成功的下界**,"Venus 上 recency 最优"尤其可疑。
- 分层重算(`tilde_stratify.py`、Venus 分层脚本):
  - GUI-Owl(Tilde 集 2400 态,参考来自各策略后代的成功轨迹):任一策略成功 73 任务,recency-2 成功 42,**只有非 recency 做成 31**。
    这 31 个任务上的 389 态:不给图会错 53%(其余任务 44%);救回率 recency 7.8% / 无关帧 13.6% / 单个真实帧对 12.7% / ∃-over-15 37.4%。
    ⇒ 即使在记忆关键任务上,冻结 GUI-Owl 从真实帧得到的也不比无关帧多;∃ 更高只是"更多可挑"。"通道关着"在这一子集仍成立
    (参考动作来自后代的偏置仍在,但真实 vs 无关是同状态内比较,不受其影响)。
  - Venus:记忆关键任务 12 个,400 态里只落了 47 态、不给图会错仅 7 态,**不可判**。按来源臂分层:来自 recent2 成功轨迹的状态
    recency 救回 75%(同义反复);来自 recent8 的状态 recency 33%、裁判 19–44%、无关 12%;来自 change2 的无关帧 60%(n=10)。
    ⇒ **"Venus 上 recency 最优"是采样造成的,撤回**;"真实帧 > 无关帧"在非 recent2 来源的状态上仍成立(样本小)。
- 无偏的余量口径(不依赖状态采样):**任务级 union-vs-best-single**。Venus 4 臂闭环:union 56.0% vs 最佳单臂 47.4% ⇒ 完美的
  按任务选策略至少值 +8.6pp 任务;GUI-Owl 底座只跑过 recency-2(31/117),需补 recent0 / recent8 / change2 三臂闭环取 union。
- 修正后的处置:(i) 立即补 GUI-Owl 底座三臂闭环(每臂 117 任务,约 40 分钟),得到底座的 union 余量;(ii) 在记忆关键任务上用
  **非 recency 成功轨迹**做参考重标 GUI-Owl 与 Venus(裁判帧 + 无关帧 + recency),这是"记忆关键子集上的余量";
  (iii) B-pilot 的 checkpoint 设计(状态由构造定义)从根上消除该偏置。
- J19 修订:保留"Venus 真实帧 > 无关帧"(通道开),撤回"recency 最优";J14/J15 措辞加"条件于成功采样"的限定。

### 2026-09-06 01:10 PT ★ 路 C 第一半:三裁判选帧搬到 UI-Venus-2(保留文本轨迹协议,395 态)——视觉通道在 Venus 上**没关**
- 设置:oracle_v2 的 400 个 Venus 状态(历史中位 13 帧、最长 50),三裁判(GLM-4.6V / Qwen3-VL-30B / Qwen3.8-27B)全历史选帧
  三口径,Venus 对 judge 帧对 + null / recency / 无关帧补解(`decode_pair_venus.py`,与 venus_oracle 同布局同采样参数)。
- 结果(保留文本轨迹):不给图 89.6%;**不给图会错的只有 41 个(10.4%)**。在这 41 个上,救回率:无关帧 30.0%,
  **recency 53.8%**,裁判帧 35.9–52.5%。净信息量(裁判 − 无关):+6.8 ~ +23.8pp,CI ±14–19(n≈39);GLM caption 口径 +23.8 ±16.8
  单格越过零,其余点估计全部为正但区间含零。
- 与 GUI-Owl 的对照(同一批裁判、同一算法):GUI-Owl 上裁判帧救回率(10–19%)≈无关帧(13.8%),recency 只 6.5–8.9%;Venus 上
  裁判帧与 recency 的救回率都是无关帧的 1.2–1.8 倍。**判断 J19:视觉记忆通道是否打开是模型属性——GUI-Owl 关着(且 recency 有害),
  Venus 开着(且 recency 就是好默认,裁判帧不胜 recency)。**"视觉不该默认取最近帧"在 GUI-Owl 上成立、在 Venus 上不成立,
  这正是余量的模型依赖性,也是第一章"在谁身上做检索实验决定结论"的更干净版本。
- 裁判体检:Venus 状态上三裁判两两完全一致 27–33%、重叠 0.55–0.62/2(与 GUI-Owl 状态上相近);裁判选中 recency 帧的比例
  29–40%(GUI-Owl 状态上 9–28%),因 Venus 轨迹更短。Qwen3.8 caption 口径修补后仍有 44 态 pick 不足 2 张。
- 样本量警告:Venus 部署条件下失败状态太少(41),这一半的 CI 很宽;去文本协议(失败态多得多)在跑(47/400,约 4 小时)。

### 2026-09-05 23:45 PT ★★ 路 C 落地(保留文本轨迹协议):Venus 的视觉通道是开的——与 GUI-Owl 相反
- 设置:UI-Venus-2-9B 冻结,400 个状态(oracle_v2 集,保留文本推理轨迹),三裁判(GLM-4.6V / Qwen3-VL-30B / Qwen3.8-27B)×
  三口径从全历史选两帧,Venus 补解;对照 null / recency / 无关帧。脚本 `decode_pair_venus.py`,输出 `rl_v2/venus/score_venus_text.txt`。
- 结果(n=395;不给图会错的只有 41 个,10.4%):

| 上下文 | 全集 V | 救回率(不给图会错) |
|---|---|---|
| 不给图 | 89.6 | — |
| **recency-2** | **91.8** | **53.8%** |
| 裁判帧(9 格) | 89.0–91.6 | 35.9–52.5% |
| 无关帧 | 87.6 | 30.0% |

  裁判净信息量(救回率 − 无关帧):+6.8 ~ +23.8pp,9 格里 4 格 95% CI 下界 > 0(glm caption +23.8±16.8、30B six +20.0±16.3、
  glm direct +13.6±14.7、30B caption/direct +14.7±15.1/16.8 边缘)。裁判间一致率 27–33%(与 GUI-Owl 集相近)。
- 判断 J19:**"截图无额外可用信息"是 GUI-Owl 特有的,不是普遍的。** Venus 上真实帧确实比无关帧更有用(净 +7~+24pp),
  且 **recency-2 是最好的上下文**(救回 53.8%,高于任何裁判)。合起来:同一考场、同一裁判、同一协议,GUI-Owl 的视觉记忆通道
  关着、Venus 的开着——这正是第一章"记忆余量是模型属性"的最强版本;也说明"视觉不该默认取最近帧"要按模型说:GUI-Owl 上
  recency 有害,Venus 上 recency 最优。
- 局限:Venus 不给图会错的状态只有 41 个(它在这集上 89.6% 就对了),CI 宽;去文本轨迹协议(116 态)在跑,预计视觉依赖更强。
- 对 history-harm 线的含义:伤害现象在 Venus 上**不存在**(所有图上下文 V ≥ 89.0,只有无关帧略低 87.6),普遍性判据 P1 需跨模型验证——
  Venus 就是第一个反例,"为什么 GUI-Owl 会被图伤害而 Venus 不会"成为机制线的核心问题。

### 2026-09-05 22:20 PT 定位(用户拍板):记忆余量 + "视觉历史不该默认取最近帧",不做记忆控制器方法
- 用户:"我们跟 MementoGUI 不太一样,我们探究的还是记忆余量,然后指出视觉并不应该总是最近图。"
- J18:论文定位 = (i) 冻结与训练后 agent 的**视觉记忆余量**如何随协议、模型、训练变化;(ii) 官方默认的 recency-N 历史窗
  **不是安全默认**——GUI-Owl 上 recency-2 比不给历史图更差(63.2 → 55.8,自一致 337 态;heldout 63.0 → 54.6),Venus 上中性。
  与 MementoGUI(插件式记忆控制器、冻结底座、无无关帧对照)的关系:它是方法,我们是余量与协议的测量;我们的无关帧对照与
  训练后再测是它没有的。B 的任务集只作为测余量的载体。
- 配套分析:逐状态 rescue / harm 计数(`harm_rescue.py`),把"recency 有害"从均值差变成逐状态证据。

### 2026-09-05 22:00 PT ★ 路 B 外审 + 先例核实 + "自然状态里几乎没有记忆依赖"的实证
- 外审(GPT-6 Pro,`reviews/memory_critical_tasks_review_20260906.md`):不直接做 B;先做文本×视觉因子通道干预(D);
  "诊断强于证据"——V(真实)≈V(无关)≈V(不给) 不证明文本含记忆;54 格非独立复现;闭环地板混杂;B 需三任务族 + 反事实孪生 +
  checkpoint 续跑 + 无历史 SFT 对照 + 交叉评测;J 在 +1.5pp 下 80% 功效需 ~1,100 状态。采纳表见该文档。
- 先例核实(WebSearch/WebFetch,2026-09-05 22:00 PT):
  **STAMP**(arXiv 2605.29324,2026-05-28):程序化向合成任务注入确定性记忆变量,控制何时编码/何时检索,产出可验证监督数据并做
  online RL;提出 Memory-World benchmark;与 GUI-Owl-1.5-8B-Think 对比。⇒ "构造记忆关键任务 + SFT/RL" 已被占。
  **MementoGUI**(arXiv 2605.18652,2026-05):学习型控制器在线选择/压缩/检索界面快照(工作记忆 = 文本摘要 + 视觉 ROI;情景记忆 =
  过往轨迹),**底座全部冻结**(UI-Venus-1.5-8B、MAI-UI-8B、GUI-Owl-1.5-8B/32B),插件式;GUI-Odyssey 上 Venus-1.5-8B:
  无历史 AMS 54.6 → 预测文本历史 66.3 → 文本摘要记忆 62.2 → 工作+情景记忆 68.3;消融称去掉 ROI 图会掉分。**无无关帧对照。**
  另有 Mem-W(2605.09317)、SE-GA(2605.16883)同期。
- STAMP 细读(WebFetch 全文,2026-09-06 01:20 PT):记忆格式是**显式文本 Memory 字段**(每步输出 think/Action/Memory/tool_call),
  近 5 轮全量 + 更早轮压缩成"Step i: action / Memory";记忆变量 = 日期/价格/验证码/菜谱,注入到**合成的虚拟应用**(如 StarWave);
  SFT 4,194 条合成轨迹(记忆步 3:1 上采样),RL 奖励 r_task + r_fmt + 0.3·记忆完整度;底座 GUI-Owl-1.5-8B-Think,Memory-World
  T-Acc 44.9→75.3,AndroidWorld-M 10.3→30.8。**没有截图记忆、没有截图 vs 文本比较、没有 recency 窗基线。** ⇒ 与我们的
  "视觉余量 + recency 默认"问题正交;它训的是文本记忆写入。B 若沿用"验证码/价格只出现一次"要引用并区分:我们要的是视觉证据
  与文本轨迹的对照,不是记忆写入训练。
- 判断 J17:我们与 MementoGUI 的数据**相容而非矛盾**——它也显示文本历史拿走了大头(+11.7),视觉 ROI 只再加 +2.0(离线 AMS);
  我们的增量在于 (i) **无关帧对照**(它没有)把"视觉增益"与"扰动"分开,(ii) **部署协议**(保留 agent 自己的文本轨迹)下增益归零,
  (iii) 训练后的 agent 上同样归零。可区分的贡献必须落在"**视觉记忆余量的协议依赖性 + 它不随训练转移**",不是任务集。
  B 的任务集只作为 D 的载体,不作为贡献。
- 实证(`find_memory_states.py`,基于 GLM/30B 两套 caption 做溯源):337 自一致状态里,type/answer 动作且事实不在当前屏的 21 个,
  可溯源到更早某屏的 12 个,**其中事实不在任务指令里的只有 1 个**(MastodonPostPollTask step 15,事实 'philippe aghion',
  来源第 6 屏,且不在文本轨迹里)。即:**MobileWorld 自然轨迹里几乎不存在"必须回看截图"的状态**——这是"任务不需要视觉记忆"
  最直接的证据,也意味着 D 在自然状态上没有靶子,只能在构造/抹除的状态上做(与 B 的 pilot 合并)。局限:只覆盖 type/answer
  的字符串事实,依赖记忆的点击目标(如选择此前见过的条目)未覆盖。
- 修订顺序:**C(Venus 跨模型,在跑)→ B-pilot(24–40 个反事实孪生场景,同时承载 D 的因子干预)→ 按 pilot 结果定 B 主研究。**
- 闭环分母审计:44 任务里 recency 4 / random 2 / older 3 个无结果,全部为模拟器 HTTP 500(环境无效),非 agent 失败;下次评测
  用固定规则区分并重跑无效任务。

### 2026-09-05 21:00 PT ★★★ 判决:三臂配对 SFT + 三裁判 + seed 复现 + 未见闭环(全部落地)
- 数据:337 自一致状态 × 6 模型(底座、三臂 s0、两臂 s1)× 23 协议上下文 + 9 个裁判帧对(3 裁判 × 3 口径)。
  完整输出 `rl/docs/results/history_layout_final_20260906.txt`;叙述见 `story_20260905.md` 第二章结果。
- 主终点:H(底座)=−8.4pp;三臂 −2.1 / −0.5 / −0.7;J=+1.5 [−0.4,+3.4];seed1 +1.3 [−0.2,+3.0];J_SO=−0.2 [−1.6,+1.3]。
  训练后 V(真实历史)≈V(无关历史)≈V(不给图)。预注册判据 ①无法评估 ②勉强 ③**不满足** → 不进第三章。
- 裁判:54 格净信息量全部在 ±6–9pp 的 CI 内压零;裁判两两一致率 20–28%。oracle 循环性实证:∃-over-15 26.2% vs 单帧对 9.0% vs 无关帧 9.5%。
- 未见闭环:1/40、0/42、0/41,底座 0。
- 判断更新:J14 主张改为"文本轨迹即记忆通道;截图无额外可用信息,冻结与训练后皆然;训练只让模型不再受害";
  J15 基于冻结模型的 oracle-SFT 路线作废(循环性);J16 联合 RL 在现有任务上无回报信号(两次零梯度的机制),
  须先构造记忆关键任务(路 B)。用户拍板:先 C(Venus 跨模型)后 B,不轻易放弃。
- 工程:思考型裁判解析事故(见错误节)导致 GLM / Qwen3.8 首轮作废重跑;容器误删事故(见错误节)。

### 2026-09-05 16:04 PT ★★ 事故:我的发射脚本误删同事刚起跑的训练容器(hyper01 sglang-omni-jaxan-1)
- 经过:judge 链脚本把容器名写死为 `sglang-omni-jaxan-1`,每个阶段以 `docker rm -f <名字>` 清场后再同名新建。
  23:04Z 重启该链时,同名容器已是另一条 session(critic-hack Stage 3 GRPO)刚起跑的训练容器,被连锅端掉。
  对方 session 通过跨会话消息告知;我已回复认错并说明修复。
- 根因:(1) 名字写死而非按"docker ps -a 与 map 两边都空缺的最小编号"现场分配;(2) 用名字而非归属凭据判断
  "这是我的容器"。全局规则(2026-08-11、2026-09-03)早已禁止删除非本人创建的容器,是我的脚本模式违反了它。
- 修复(已落地到本线全部发射脚本 hyper00 ×3、hyper01 ×1):创建前 `alloc_name` 同查 docker ps -a 与 map;
  `docker run --label cc.owner=<本次运行唯一 token>` 并当场登记 map;`rm_own` 只删标签等于本 token 的容器,
  其它一律 SKIP 并打印。旧链全部停掉、按新脚本重启。附带发现并修掉:两条链同秒选卡撞到同一张 GPU0——
  `pick_gpu` 改为同时排除 map 中已登记且容器仍存在的卡。
- 代价:对方一次训练启动被打断(其已改用 jaxan-2 重启);我方三条链各损失一次服务重启(约 3 分钟)。

### 2026-09-05 00:00 PT ★ 自查发现:第二章没有真正未见的评测模板(口径降级,不重训)
- 触发:外审把主终点改为 `J = H_S − H_C` 后,我去核 SFT 训练集与评测切分的关系。
- 现象:SFT 数据取自 442 条**成功**轨迹,覆盖 73 个任务,**其中 21 个属 heldout 切分**(39 中的 21)。
  因此分析里筛的 "heldout" 对三臂而言**不是未见模板**。补救空间几乎为零——标注状态只能取自成功轨迹,
  而有成功轨迹的任务恰好就是那 73 个;底座新 rollout 的 31 个成功任务**全部**落在 SFT 训练集内,
  自一致标注的 337 个状态里**没有一个**来自未训练任务。
- 判断:**主终点 J 仍然有效**——三臂在这些任务上的训练暴露完全相同,泄漏对三臂对称,J 测的是
  "训练方案是否改变历史的价值",不受影响。**但不能说成泛化结果**:预注册判据里"在未见模板上"
  这句话无法满足,只能表述为"在训练过的任务模板上"。这是实质性削弱。
- 处置(默认推进,可推翻):**不重训**(重建切分后重训三臂 7.5 小时,今日交付不了,且现有结果不作废);
  今早的表按"训练模板内"如实标注,J 照报,不主张泛化;泄漏写进汇报稿的"不会说的话"。
- 待裁决:是否补一个**真正未见**的评测。两条路——
  (a) 放宽状态来源到**失败轨迹**,让 44 个未训练任务立刻有状态。**我不推荐**:参考动作变成"模型在
      失败路径上做过的动作",匹配它测的是"与底座的一致性"而非正确性,偏离越多分越低,对被训练过的
      臂系统性不利,是比泄漏更坏的混杂;
  (b) 在 44 个未训练任务上跑**闭环任务成功率**。这是真正的终点,不需要参考动作,天然跨模板。
      代价:三个合并后的权重需从 Tilde 传到 hyper00(约 48GB,走 HF)+ 三臂各跑一次闭环,合计约 3 小时。
      **推荐 (b)**,若用户认可则在标注作业之后接上。
- 教训记入 §7:**切分要在建数据时就切,不能等到分析时再筛**。`build_random_s_sft.py` 当时用了
  `--roots` 全量目录、没有按 `mw_split_v1.json` 过滤,这一步本该在建集时就做。

### 2026-09-04 13:00 PT 三臂配对数据集建成,时间距离与非连续性已分离
- 外审要求加 OLDER-CONTIGUOUS 臂把『看更老的帧』与『两帧彼此散开』分开。实现:older 规则与 random 共用
  同一个 rng 抽样,取其两帧**中位帧龄**处的一对**相邻**帧。三臂状态键完全一致,各 15,473 条:

| 臂 | 平均帧龄(中位/均值) | 两帧间距(中位/均值) | 隔离了什么 |
|---|---|---|---|
| recency | 1.5 / 1.5 | 1.0 / 1.0 | 基线(官方默认布局) |
| older | 10.5 / 12.5 | 1.0 / 1.0 | 与 recency 比 → **帧龄**的作用 |
| random | 10.5 / 12.7 | 6.0 / 8.7 | 与 older 比 → **非连续性**的作用(帧龄已配平) |

  older 与 random 的平均帧龄中位数都是 10.5、均值 12.5 vs 12.7,**时间距离配平**;唯一差别是两帧相邻还是散开。
- 无关历史对照(测干扰易感性)属**评测期**控制,不是训练臂:喂来自其他轨迹的历史帧,看各臂退化多少;
  实现放在标注器里,发射评测前完成。
- 脚本:`build_random_s_sft.py --frame-rule {recency,older,random,oracle}`;产物
  `sglang-omni-rl/sft_{recency,older,random}_v2.jsonl`。

### 2026-09-04 12:05 PT GUI-Owl 地板的失败形态拆解(回答用户『是不是一直打转用完步数』)
- MobileWorld heldout 三臂(`traj_final_held_{recency,S0,St}`)的失败轨迹拆解:

| 臂 | 成功 | 失败 | 撞满 50 步 | 其中死循环(末 8 步 ≤2 种动作) | 极短失败(≤5 步) | 中位步数 |
|---|---|---|---|---|---|---|
| recency | 9/36 | 27 | 25(93%) | 12(44%) | 0 | 50 |
| S0 | 9/35 | 26 | 21(81%) | 4(15%) | 0 | 50 |
| St | 12/35 | 23 | 23(100%) | 5(22%) | 0 | 50 |

- 判读:**失败几乎全是把 50 步预算跑光**,没有早停;但严格死循环只占失败的 15–44%,**其余是"持续做看似合理却不推进的动作"**(末动作以 click 为主),属能力不足而非退化循环。非法动作在这三臂为 0(§4.18 的 157 次非法动作出在官方 runner 通路,不是这里)。
- 对『SFT 让它学会』的判断:**不做**。(1) 可训数据不存在——442 条成功轨迹都是它本就会做的任务,
  MobileWorld/MemGUI 不提供标准轨迹(MemGUI 只有 `golden_steps` 计数);(2) 要示范就得从更强模型蒸馏,
  那是 MemGUI-Agent 走过的『训一个更好的 GUI agent』路线,属用户明确要避开的拥挤方向;(3) 动 executor 权重
  会毁掉『冻结 executor + 学出的记忆控制器』这一主张的干净性(涨幅归因不清);(4) **不需要修**——地板只伤
  Stage II,而 Stage II 在 Venus 上跑;Stage I 是步级反事实回报,不看任务成败,GUI-Owl 在其中反而可动步更多
  (可控上下文 24.4% vs Venus 保留文本历史时 7.5%)。

### 2026-09-04 11:10 PT ★ 用户定案:先做 Stage I,控制对象改为动态 B(设计与发射)
- 用户裁定(原话要点):(1)『先做这个吧』——指 Stage I(executor 反馈的反事实密集回报),J7 撤销;
  (2)否决我提的『文本与帧的预算分配』:记忆方向文章已拥挤,且探针本身说明『保留文本历史就够,不需要图』,
  分配问题不成立;(3)**把现在的固定 B 变成动态 B:文本部分不变,平时 B=0,需要图片记忆时才存 B 张。**
- 我的评估:用户的框架优于我的。数据直接支持它——保留文本历史时 **72% 的状态在全部 23 个上下文下输出
  同一个动作**(那些步 B=0 是免费的),B=0 已有 90.4%,而 ∃正确对 97.7%:**门控的可得区间就是这 +7.3pp,
  且大部分步不该花图像预算**。与拥挤赛道的区分点也更清晰:主流做法是**模型内部**的上下文折叠/摘要或
  **常开**检索,我们是**外挂、学出来、带预算意识的门控**,默认零成本。

**Stage I 设计(动态 B,预注册)**
- 动作空间:M ∈ {∅} ∪ {单帧 i}(6)∪ {帧对 (i,j)}(15),即 B ∈ {0,1,2};**文本推理历史恒定保留**(部署条件)。
- 回报:`r(M) = Match(E(x,M), a*) − Match(E(x,∅), a*) − λ·|M|`。以 **B=0 为反事实基线**,直接对齐门控语义;
  λ = 图像预算价格,做 sweep 出 Pareto 曲线(超参 ablation 规则)。
- 优化:RLOO(组 = 同状态多次采样 M);对照臂:恒 B=0 / 恒 recency-2 / 同预算随机门控 / 同预算监督模仿。
- **关键性质:标签集穷举全部 22 个动作,任何策略都能在其上离线精确评估**——λ sweep、对照、Pareto 曲线
  全部零 GPU 成本,只有特征抽取与训练需要 GPU。
- 闸门(预注册):在**未见模板**上,学出的门控须在平均图像预算 ≤ recency-2 的一半时,步级正确率 ≥ recency-2,
  且显著高于同预算随机门控;否则不进 Stage II。
- 数据:Venus 自身成功轨迹 204 条 → 1,726 个状态 × 23 上下文,hyper00 GPU1(41041)+ GPU3(41042)
  双服务并行标注,约 4 小时;脚本 `stage1_label.sh`,产物 `rl_v2/venus/stage1_labels_{a,b}.jsonl`。
- 已知偏置(写入 limitation,Stage II 覆盖):参考动作取自轨迹自身(自一致性偏置);仅用成功轨迹(幸存者偏置)。
- 同时:no-text 诊断在 116 状态处停止(用户裁定后不再是决策输入),部分结果留档。

### 2026-09-04 10:40 PT ★★ 关键更正:Venus 的"无记忆余量"是协议假象——去掉文本推理历史后余量 +30pp
- 假设(用户三问触发的自审):G2 测得 Venus 帧选择余量只有 +5.8pp,是不是因为我们让它保留了**自己的文本
  推理历史**,而 §17 的 GUI-Owl 探针**没有**文本历史?两者根本不可比,"Venus 没有记忆余量"可能是协议造成的。
- 现象(同一批状态、同 23 上下文、只去掉历史 assistant 文本;`venus_oracle.py --no-text`;n=70,运行中):

| 口径 | null(仅当前帧) | recency-2 | 随机对 | **∃正确对** | oracle − recency | 每状态不同动作数中位 |
|---|---|---|---|---|---|---|
| Venus **保留**文本历史(396 态) | 90.4% | 91.9% | 89.0% | 97.7% | **+5.8pp** | 1(72% 状态完全不敏感) |
| Venus **去掉**文本历史(70 态) | 37.1% | 55.7% | 46.0% | **85.7%** | **+30.0pp** | 4(仅 16% 状态不敏感) |
| GUI-Owl(§17,300 态,本就无文本历史) | 31.4% | 35.5% | 29.6% | 53.2% | +17.7pp | — |

  去文本口径下 recency 错的 31 态里,**17 态只能靠含远帧(帧龄 ≥5)的对救回**(55%),而保留文本口径下
  这一比例是 15/32 中的"仅远帧"占全体状态 3.8%。
- 判断:**Venus 的帧敏感度不但存在,而且比 GUI-Owl 更强**(+30.0 vs +17.7pp,且它是更强的 executor)。
  §5 08:50 PT 的"视觉记忆余量约 4%"只在"executor 已保留自己完整文本推理历史"这一条件下成立——
  那不是"记忆无用",而是**记忆已由文本承载**。两条线的正确读法:记忆的载体可以是文本也可以是帧,
  我们的帧选择器此前一直在与"模型自写的文本记忆"竞争而非互补。
- 对 J10 与 §8 裁决项的影响:**§8 中"不投入帧选择器"的推荐依据被削弱**,但也没有直接翻转——
  部署态(文本 + 帧)的任务级余量仍然小;真正有价值的问题变成"**固定上下文预算下,文本与帧如何分配**"。
  裁决项改写见 §8。
- 结果:no-text 全量(≥400 态)运行中;完成后更新本条与 §8。

### 2026-09-04 10:15 PT 温度对照臂:温度不是 45.7% vs 论文 65.8% 的原因(方向相反)
- 假设:四臂用官方离线示例脚本默认 temperature=0.0,而模型卡对 agent 类任务建议 1.0;20pp 的绝对分差距
  可能出自此,若成立则 G0/G1/G2 全部需在正确配置上重测。
- 现象(同 117 题、同协议、只改温度;`venus_temp_arm.sh`):

| 臂 | 全集 | heldout-39 | 对 temp 0 配对 W/L/T | 中位步数 | 撞上限 |
|---|---|---|---|---|---|
| recent2 @ temp 0(原基线) | 53/116 = 45.7% | 15/38 = 39.5% | — | 16 | 14% |
| recent2 @ temp 1.0 | 23/116 = 19.8% | 7/38 = 18.4% | 1/31/84;1/9/28 | 4 | 0% |

  机制:温度 1.0 下动作串大面积不可解析——106 个 episode 中 **59 个以非法动作终止**(temp 0 为 17),
  样例含残缺坐标、中英混杂乱码(`Click(point=(50Moderating?, let's say (400))`、`Swipe(start=(500, 780),
  end=(50不完整…`)。官方示例脚本把 `repetition_penalty=1.05` + `frequency_penalty=0.3` 与温度 0 配套使用,
  提温即退化。模型卡的"agent 任务用 1.0"应对应其自有采样配置。
- 判断:**温度 0 是本 agent 的正确设置;温度不解释绝对分差距**。四臂与 G2 的测量在这一项上干净,
  §5 08:50 PT 与 07:05 PT 两条结论维持。剩余嫌疑 = 我方移植 agent 与其内部评测实现的细节差异;
  该差距只影响绝对分,不影响臂间配对与 G2 判读。
- 改动:`ui_venus2_agent.py` 加 `CC_VENUS_TEMP`(默认 0.0)。去留:温度项结案,不再追。

### 2026-09-04 10:05 PT 我的操作错误:本地副本覆盖主机 agent,退化版跑了一轮
- 现象:把本地 scratchpad 的 agent 副本 scp 到主机时,覆盖掉了此前**只在主机上**打的两个补丁
  (think 标签归一化、`recent:N`/`change2` 历史策略);第一次启动的温度臂因此跑在退化版上。
- 处置:立刻停臂、删产出(9 题)、重打两个补丁并单测(`change2 S=[2,5]`)后重跑。**同时发现 Git 里的
  agent 副本也缺这两个补丁**(commit6 提交的是本地未打补丁版),已用主机实机版本覆盖并核对 size 一致。
- 教训:主机侧热补丁必须当场 scp 回本地并提交,否则 Git 与实机分叉,且任何"本地→主机"同步都会静默回滚。

### 2026-09-04 08:50 PT 离线主闸门 G2 出数:形式上过线(+5.8pp),但效应量在阈值边缘且一半来自扰动
- 假设:Venus-2 在未见模板上存在 oracle-2 对 recency-2 的动作级余量(预注册:≥ +5pp 且 random-2 ≤ recency-2 + 2pp)。
- 现象(400 状态,取自四臂成功轨迹各约 90–124 个;23 上下文/状态;贪心解码;保留全部文本推理历史;等价类 TOL=60/999):

| 子集 | 状态数 | recency-2 | 无历史图 | 随机对均值 | ∃正确对 | ∃正确单帧 | recency 错且某对能救 |
|---|---|---|---|---|---|---|---|
| 全部 | 396 | 91.9% | 90.4% | 89.0% | **97.7%** | 96.0% | 28/32 = 88% |
| heldout-39 | 136 | 90.4% | 90.4% | 88.2% | **98.5%** | 97.8% | 11/13 = 85% |
| train-78 | 260 | 92.7% | 90.4% | 89.5% | 97.3% | 95.0% | 17/19 = 89% |

  赢家诅咒对照(`venus_oracle_breakdown.py`):72% 的状态在 23 种上下文下输出**同一个**动作(每状态不同动作数
  中位 1、均值 1.78);recency 错的 32 个状态里 **16 个不给任何历史图也对**(扰动而非记忆),**只靠含远帧
  (帧龄 ≥5)的对才能救回的 15 个**(3.8% 的状态;heldout 5/136)。救回对里较老帧的帧龄中位 5(≥5 占 54%,
  ≥10 占 21%);61% 的救回状态里"视觉变化最大帧"落在某个救回对中(change2 启发式已覆盖的部分)。
- 判断:**按预注册口径 G2 通过**(全部 +5.8pp、heldout +8.1pp;随机对低于 recency)。但这是"存在某对"
  的统计,在基础复现率 90% 的区间里天然膨胀;**扣除扰动后,视觉记忆特有的可救状态约 4%**,与闭环 G1 的
  图景一致(2→8 帧净胜 2 题、启发式单独救回 6 题但整体净负 6)。**该 executor 上"选对帧"的可得余量是正的
  但小**;记忆的主要载体是它自己的文本推理历史(无图 90.4% 与 recency 91.9% 只差 1.5pp)。
  与 §17(GUI-Owl:recency 35.5 / oracle 53.2)不可比:那里参考动作来自别的策略且无文本历史。
- 改动:无。结果:G0 ✓、G2 ✓(形式)、效应小。
- 去留:按预注册应进入 selector 重建;但效应量意味着任务级收益上限约 +2–4pp,117 题需 ≥4 净胜才能分辨,
  MemGUI(128 题、LLM 判分)更难。**方向性抉择交用户**(见 §8 新增项);辅助诊断(无文本历史口径)与补跑轮
  仍在自动执行,不改判决。

### 2026-09-04 07:05 PT Venus-2 闭环探针 v2 收官(G0 过线;G1 诊断:多帧小增益、盲选远帧平均有害但能救少数题)
- 假设:UI-Venus-2-9B 在 MobileWorld 上地板健康(G0),且历史截图给法对任务成败有杠杆(G1)。
- 现象(117 题各臂一题 runner 报错无结果,n=116;heldout-39 子集 n=38;50 步上限;程序判分;温度 0):

| 臂 | 全集成功率 | heldout-39 | 对 recency-2 配对 W/L/T(全集;heldout) | 死循环 | 撞上限 |
|---|---|---|---|---|---|
| recent0(仅当前帧 + 文本历史) | 49/116 = 42.2% | 15/38 = 39.5% | 5/9/102;3/3/32 | 9% | 18% |
| recent2(官方 N_IMG=2,基线) | 53/116 = 45.7% | 15/38 = 39.5% | — | 3% | 14% |
| recent8 | 55/116 = 47.4% | 17/38 = 44.7% | 10/8/98;4/2/32 | 3% | 12% |
| change2(最近帧 + 视觉变化最大帧) | 47/116 = 40.5% | 16/38 = 42.1% | 6/12/98;4/3/31 | 6% | 17% |

  任一臂成功(四臂并集)65/116 = 56.0%(heldout 21/38),四臂全成 34;只靠 change2 或 recent8 才成功的题
  12(heldout 6)。产物 `rl_v2/venus/v2/`,汇总脚本 `venus_tally2.py`。
- 判断:**G0 过线**(recency-2 45.7% ≥ 40%),但比论文 65.8% 低约 20pp——差异候选:解码温度(官方脚本默认 0,
  模型卡建议 agent 任务用 1.0 并保留推理历史)、harness/版本;已列待核,不影响 G1/G2 的配对判读。
  G1:帧数剂量响应 42.2 → 45.7 → 47.4,2 帧之后边际收益小且 8 帧对 2 帧净胜 2 题在噪声内;非连续启发式
  整体净负 6 题,与 J2(盲选远帧平均有害)一致,但它单独救回 6 题、heldout 上净 +1——"旧帧有用"的题存在
  但少;四臂并集比 recency-2 高约 10pp,是任务级"给对记忆"的粗上界,含跨臂赢家诅咒,不作定量引用。
- 改动:无(诊断)。结果:待 G2(离线 oracle-2 余量,13:57Z 起,约 400 状态 × 23 上下文,预计 1.5 小时)。
- 去留:G0/G1 不构成停止条件;去留由 G2 决定(预注册)。

### 2026-09-04 03:50 PT 外审改判闸门;Venus-2 探针 v1 停、v2 重发
- 假设:换 executor 后"满历史 > 最近两帧"能作为记忆杠杆的闸门。
- 现象:外审指出该闸门非命题的必要条件(token 稀释可让满历史输而 B=2 选择赢),39 题分辨不了 3pp,
  且未诊断 selector OOD 失败就换表征是乱撞。
- 判断:采纳。主闸门改为 Venus 上离线 oracle-2 余量(G2),闭环探针改为 117 题四臂剂量/选择诊断(G1),
  地板健康 G0;全部在数据落地前预注册于 reviews/executor_swap_review_20260904.md §3。
- 改动:v1 探针(heldout-39,N_IMG 0/2/all)于 08:5xZ 停止,部分结果归档 `rl_v2/venus/partial_v1_*`;
  agent 加非连续启发臂;tally 加列联表与循环/撞限诊断;v2 探针 117 题四臂发射(见 §9 更新)。
- 结果:待。去留:待 G0–G2。

### 2026-09-04 03:00 PT 判断整合与方向改判(本文件建立)
- 假设:历史台账互相冲突导致我反复改口(SFT 一会做一会不做)。
- 现象:J1(iter_59 能读非连续帧,§10/§17)与 §4.13"executor 用不动"叙事并存;后者是联合线在
  非 oracle 选帧下的任务级现象,我把它误当作能力缺失。
- 判断:两者不冲突——能力在(J1),收益取决于选帧精度(J2),精度不泛化(J3)。
- 改动:撤回 SFT(J6),Stage I 暂停(J7),方向改为换 executor + 余量探针(J10)。
- 结果:待外审与探针。去留:待定(探针出数后更新)。

### 2026-09-04 01:00 PT 判分费用事故与止损
- 现象:用户指出 Gemini 账单约 50 美元。核算:MemGUI-Eval mode=full 每轨迹 ≈50 次 flash +
  3 次 pro(终判/IRR/BadCase),≈$0.15–0.20/轨迹,论文自报 $0.213;我们多轮重判放大到
  300–350 轨迹次。我又在未获预算时起了官方口径重跑(9 题起跑、2 题判分后停)。
- 改动:止损脚本;IRR/BadCase 开关(J9);判分列为硬停止。出处 summary §27.9–27.10。

### 2026-09-04 00:40 PT MemGUI 两臂终表(50 步上限口径)
- 臂 A 官方默认窗 7/128;臂 B recency B=2 7/128;配对 同成 4 / 仅 A 3 / 仅 B 3 / 同败 118,
  McNemar p=1.0;82% episode 跑满 50 步;B 臂死循环尾 49% vs A 22%。
- 协议偏差:固定 50 步 vs 官方 golden×2.5+1,79/75 条被截断 → 只作内部配对(J8)。§27.9。

### 2026-09-03 AndroTMem 探针(已删)
- 原版 8B 在 gold 文本历史下,远程截图无增益;结论限离线设定。用户 09-04 00:05 PT 令删除。§27.5–27.8。

### 2026-09-03 冻结 iter_59 selector RL(v2 服务)null result
- 22 轮,heldout 贪心 RL=init=recency;70% 组零对比;停。§25–26.6;外审 reviews/rl_null_result。

### 2026-09-01/02 行为标签预训练
- 宽口径 +5.1pp(3 种子稳健),严口径 +0.2pp;数据减半不掉(J3)。§19–20。

### 2026-09-01 行为天花板与侵蚀假说反转
- oracle 对 53.2% vs recency 35.5% vs 随机对 29.6%(J1/J2);iter_59 帧敏感度高于底座(§10)。

### 2026-08-25→09-01 联合训练 P1–P4(cua_lite §4.x)
- 批 60 交叉:Et×recency 45% > Et×St 40% > Et×S0 30%(§4.13);批 100 终判 learned−recency
  +8.8pp(p=0.45)与 selector-only −6.2pp(p=0.73)符号相反 → 效应≈0(§4.15/4.18);selector
  三连病理修复(§4.16);P4 因结构缺陷停线(§4.20)。

### 2026-08-04→09 OSWorld RL v1/v2
- 20 迭代 RL 81/240 vs recent 88/240,零效应;selector/LoRA 均未训动。`audit_ledger_20260809.md`。

## 6. 数字总表(口径不同不并排比较)

| 口径 | 数字 | 出处 |
|---|---|---|
| MemGUI 榜单 GUI-Owl-1.5-8B / 32B | 11.7 / 10.9 Pass@1;15.6 / 20.3 Pass@3 | leaderboard.json |
| MemGUI 榜单 UI-Venus-2-9B / 27B;Qwen-UI-Agent(235B) | 62.6 / 70.3;77.3 | memgui README 新闻栏 |
| MemGUI 自测(50 步上限,3.1-pro 终判)A / B | 5.5 / 5.5(7/128 各) | §27.9 |
| MobileWorld heldout-20 贪心(RL pv22 / init / recency) | 15.8 / 15.8 / 15.8–26.3 | §26.6 |
| 行为天花板 300 状态(iter_59) | recency 35.5,当前帧 31.4,随机对 29.6,oracle 对 53.2 | §17 |
| 可控上下文 | 452 / 1,853 = 24.4% | §26.2 |
| 监督预训练 val B=2 宽/严口径 vs recency | +5.1 / +0.2 pp | §20 |
| 联合线批 60 交叉(20 题) | 40 / 45 / 30 | cua_lite §4.13 |
| 判分成本 | ≈$0.15–0.20/轨迹(论文 $0.213) | §27.9 |
| Venus-2 离线 G2 **去文本历史**口径(70 态,运行中) | null 37.1;recency 55.7;随机对 46.0;∃对 85.7(oracle−recency **+30.0pp**) | 本文件 §5 10:40 PT |
| Venus-2 温度对照(117 题,recency-2) | temp 0: 45.7 / 39.5;temp 1.0: 19.8 / 18.4(配对 1 胜 31 负) | 本文件 §5 10:15 PT |
| Venus-2 离线 G2(396 状态 / heldout 136;全部 / heldout) | recency 91.9/90.4;无图 90.4/90.4;随机对 89.0/88.2;∃对 97.7/98.5;仅远帧可救 3.8% | 本文件 §5 08:50 PT |
| Venus-2 闭环四臂(MobileWorld 116 题 / heldout 38,50 步,温度 0) | recent0 42.2/39.5;recent2 45.7/39.5;recent8 47.4/44.7;change2 40.5/42.1;四臂并集 56.0/55.3 | 本文件 §5 2026-09-04 07:05 PT |

## 7. 我犯过的错与纠正

| 错误 | 如何发现 | 纠正 | 影响过的结论 |
|---|---|---|---|
| 标注器布局(图连排在前 + 全部 conclusion)≠ 部署布局,同协议 T=0 仅复现 55.8%;步级'recency 有害''裁判≈无关'可能是布局伪影 | harm 线重建部署布局后自复现 93.5% | 部署布局下重测裁判帧与三臂 | J14/J19 及第一/二章的步级表 |
| 状态采样条件于'标注策略下成功',记忆关键状态被稀释;所有余量估计是条件下界 | 用户 09-06 指出 | 分层重算;补底座多臂闭环取 union;记忆关键任务上用非 recency 成功轨迹做参考重标;B-pilot 按构造取状态 | 'Venus 上 recency 最优'撤回;'截图无可用信息'加'条件采样'限定 |
| 发射脚本按写死的容器名 `rm -f`,误删同事刚起跑的训练容器 | 对方 session 跨会话通报 | 名字现场分配 + owner 标签 + 只删自己标签的容器;全部脚本已改 | 无(工程事故,不涉结论) |
| 用思考型模型(GLM-4.6V / Qwen3.8)做结构化选帧,max_tokens=32 把答案截在 `<think>` 里,正则捡到无关数字 | 检查原始输出:310/337 解析为空 | 关 thinking、预算 2048、要求 `ANSWER: i,j` 收尾并只解析 `</think>` 之后 | 两个更强 judge 的首轮结果作废重跑 |
| SFT 训练集未按 `mw_split_v1.json` 过滤,21/39 个 heldout 任务进了训练集,导致第二章没有真正未见的评测模板 | 外审改主终点后复核训练集与切分的关系 | 口径降级为『训练模板内』,不主张泛化;推荐补闭环未见集 | 第二章预注册判据中『未见模板』一条 |
| J12 初稿把 Venus 45.7%(heldout-38)与 GUI-Owl 15.8%(anchor-20 贪心子集)并列,考场与协议都不同 | 用户追问地板成因时复算三臂 heldout | 改为同口径 25–34% vs 39.5%,差距由『约 30pp』更正为『5–13pp』 | J12 的『换 executor 的配对证据』强度 |
| 本地 agent 副本覆盖主机热补丁,退化版跑了一轮温度臂;Git 副本同样缺补丁 | 重启后 CC_TRACE 打印退化格式 | 停臂删产出、重打补丁单测、用实机版本覆盖 Git 并核对 size | 无(该轮产出已删除) |
| MemGUI 步数上限写死 50,违反官方口径 | 09-04 读 runner 源码 | 脚本改默认官方口径;5.5% 降级为内部口径 | "可与 11.7% 比"的表述 |
| 判分 API 花费约 $50 未先要预算;又起了会再花 $80–100 的重跑 | 用户账单 | 止损、开关、硬停止 | — |
| "executor 用不动非连续帧"标签贴错,导致提出多余的 SFT | 用户追问 iter_59 证据 | J1/J6 | 09-04 02:30 PT 的建议 |
| 配对 oracle 53.2% 被当作固定策略可达值(赢家诅咒) | 09-03 用户质疑后审计 | §27.7 更正 | "路线的肉还在"的定量表述 |
| heldout 曲线"连续上行"报为信号 | 配对 recency 复测 | 判噪声 | §25 |
| 误删并行 session 容器 sglang-omni-jaxan-1 | 对方重建 | 判僵尸看 map 与创建 session | — |
| 本地 TCC 拒读把空 blob 推上 main;map 三次被 read-filter-overwrite 抹空 | 事后核对 size / 不变量 | 每次 push 验 size;map 只走 rebuild 脚本 | cua_lite §4.20 |

## 8. 待用户裁决

| 事项 | 我的推荐 | 理由 | 若不批 |
|---|---|---|---|
| **方向(10:45 PT 改写,取代此前"不投入"的推荐)**:记忆载体是文本还是帧?<br>去文本口径显示 Venus 帧敏感度 +30pp(强于 GUI-Owl 的 +17.7pp),但保留其自写文本历史后只剩 +5.8pp | **我的推荐:把研究问题定为"固定上下文预算下的记忆分配"**——控制器同时决定保留哪几张帧与保留/压缩哪段文本推理历史,reward 用反事实记忆优势;在 GUI-Owl(帧敏感、任务级地板)与 Venus(强 executor、文本记忆强)两个 executor 上各跑一遍,后者兼作"强模型如何自解记忆"的对照 | 帧选择单独看是在与模型自写文本记忆竞争,天花板小;预算分配问题两边都有余量,且是 RL 天然适用的组合决策 | 若只想最快出结果:仅在 GUI-Owl 上做 Stage I(它的 +17.7pp 余量与 24.4% 可控上下文都已量过) |
| executor 换为 UI-Venus-2-9B(放弃 GUI-Owl/iter_59 主线) | 批,但以探针为闸 | J5 地板三重实证;榜单 9B 级模型已达 62.6% | 只能走"缩主张"路线 |
| MemGUI 定稿运行的判分预算 | 每臂约 $10–15(开关已开) | 只跑定稿配置一次 | 无对外可比数字 |
| 第二档磁盘清理(hyper00 `osworld/` 33G、`aw/` 13G) | 删 | 均已上 HF 或可重建 | 占盘 |

## 9. 未来 6 小时的执行计划(2026-09-04 03:50 PT 修订,用户睡眠期间)

1. [done 03:40 PT] 外审落盘 `reviews/executor_swap_review_20260904.md`,闸门 G0–G2 预注册。
2. [done 01:50–03:00 PT] rle(GUI-Owl)容器删除;GPU1 起 `sglang-omni-jaxan-3` = UI-Venus-2-9b vLLM
   (131k 上下文,64 图/请求);map 登记;Venus-2 官方 mobile 协议移植为 `ui_venus2` agent,单任务冒烟通过
   (8 步、约 5 s/步)。
3. [done 14:04Z] 探针 v2:MobileWorld 117 题 × 四臂(N_IMG 0 / 2 / 8 / 启发式非连续 B=2),池 p00–p11 并发 12,程序判分,
   零 API 费用;每臂预计 40–60 分钟;tally 报成功率、对 recency-2 的 wins/losses/ties、死循环率、撞限率、
   成功中位步数;heldout-39 另报。
4. [done 15:36Z] 离线 G2:`venus_oracle.py`(Venus 官方多轮协议,23 上下文/状态,状态取自 v2 四臂成功轨迹,≤400
   状态,heldout-39 / train-78 分报)+ `venus_oracle_verdict.py`(0–999 网格 TOL=60 等价类);两状态管线
   测试通过;`venus_g2_after_probe.sh` 等 v2 收官自动执行,产物 `rl_v2/venus/oracle_v2_verdict.txt`。
5. 出数 → 更新 §2 J10 与 §5;G2 成立则起草 selector 重建方案(先诊断 OOD 失败原因,再谈表征),否则写
   负结果并停。
