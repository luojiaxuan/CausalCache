# CausalCache 记忆控制器 RL 线 实验台账(截至 2026-09-05 22:05 PT)

本文件是 RL 线的**唯一现行判断清单**。历史日志(`cua_lite_integration_20260825.md` §4.x、
`summary_retrieval_design_20260831.md` §1–28、`rl_recipe.md`、`audit_ledger_20260809.md`)只作
证据仓库,**与本文件冲突时以本文件为准**;本文件每次判断变更都要写进 §4 并注明推翻了谁。

## 0. 一句话现状

**当前主线:Stage I(动态 B)**——文本推理历史恒定保留,控制器学『何时该存图、存哪几张』,平时 B=0
(用户 2026-09-04 11:10 PT 定案,J11;J7 撤销)。executor = 冻结的 UI-Venus-2-9B(hyper00 GPU1/GPU3 两台
vLLM),标注中(1,726 状态 × 23 上下文,约 4 小时)。可得区间已量化:B=0 已有 90.4%、∃正确对 97.7%,
且 72% 的步对上下文完全不敏感。历史四条 RL 线的判死/停线结论见 §2 J1–J9。下一个决策点 = Stage I 的
未见模板闸门(§5 11:10 PT 条目)。

## 1. 三十秒背景

问题:冻结的 GUI executor 之上,能否**学**出一个记忆控制器(每步从历史截图里选 B 张喂给
executor),使第三方记忆 benchmark(MemGUI-Bench,128 题闭环,LLM 判分)显著提升、常规 benchmark
(MobileWorld / AndroidWorld)不退化。成功 = MemGUI Pass@1 显著高于同一 executor 的默认历史窗,
且方法是学出来的(RL 或密集反馈的 bandit),不是手写规则。

## 2. 当前生效的判断(冲突消解后的唯一版本)

| # | 判断 | 证据(可 grep) | 推翻了什么 |
|---|---|---|---|
| J1 | **iter_59 能利用非连续历史帧**。oracle 双帧−recency 的 gold 动作 logprob:底座 +0.187,iter_59 +0.255,配对差 +0.068 显著(80 状态);行为口径 300 状态:recency 35.5%、仅当前帧 31.4%、oracle 对 53.2% | summary §10 表、§17 表 | 09-04 02:30 PT 我说的"executor 用不动非连续帧" |
| J2 | **随机远帧有害**:随机对 29.6% < recency 35.5% < oracle 53.2%;executor 只在选得准时受益 | summary §17 | — |
| J3 | **selector 精度跨模板不泛化**:密集行为标签监督预训练,宽口径(域内)+5.1pp 胜 recency,严口径(跨模板)+0.2pp 持平;训练数据减半不掉 → 数据量不是瓶颈,表征是 | summary §19–20 | "再标注扩量"的路线 |
| J4 | **终局 0/1 回报的 selector RL 在冻结 executor 上无信号**:RLOO 组 70% 零对比;22 轮策略熵 4.05≈均匀 4.19;KL(final‖init)=0.026;argmax 一致 61.5%;heldout 贪心 RL=init=recency 15.8% | summary §25–26.6 | §25"heldout 连续上行"(配对复测后判噪声) |
| J5 | **任务级地板**:MobileWorld heldout-20 有 11 题四次评测全零;MemGUI(GUI-Owl-1.5-8B,50 步口径)两臂各 7/128,118 题两臂同败,82% episode 跑满上限 | summary §26.1、§27.9 | — |
| J6 | **random-S 格式 SFT 不做**:它针对"executor 不会读非连续帧",J1 证明该前提对 iter_59 不成立 | 由 J1 推出 | 09-04 02:30 PT 我提的"先 SFT"(用户已批,本条撤回并已告知) |
| J7 | ~~Stage I 暂停~~ **已由用户 2026-09-04 11:10 PT 推翻:先做 Stage I**。原搁置理由(同数据的监督预训练没泛化)不成立——监督模仿 oracle 标签学的是模板捷径,与 RL 在自身分布上优化反事实回报不是一回事;外审也把『oracle 余量大而学出的 selector 崩』列为『该重做 selector』的分支 | 用户裁定;reviews/two_stage §3;§19–20 | 我 09-04 02:30 PT 的 J7 |
| J8 | **MemGUI 对外基线一律引用论文 11.7% / 15.6%**;我们的 5.5% 是 50 步上限口径,只作内部配对 | §27.9–27.10;用户裁定 | — |
| J9 | **判分只保留 Pass@1 所需调用**(逐步描述 + 终判),IRR/BadCase 关闭;终判模型 gemini-3.1-pro-preview 替代 2.5-pro 须脚注 | §27.10;开关已入库 | — |
| J10 | **下一步 = 换 executor 为 UI-Venus-2-9B**(MemGUI 62.6、AndroidWorld 80.2、MobileWorld 65.8;权重公开 18.8GB,Qwen3.5-9B 底座,vLLM 镜像已支持;权重许可证"待确认")。**闸门按外审改判并预注册**(reviews/executor_swap_review_20260904.md §3):G0 闭环地板 recency-2 ≥ 40%(117 题);G1 剂量/选择臂 N_IMG 0/2/8 + 启发式非连续 B=2 只作诊断;**G2 主闸门 = heldout-39 轨迹上离线 oracle-2 − recency-2 ≥ +5pp 且 random-2 ≤ recency-2 + 2pp**;MemGUI 不参与任何决策 | 外审 2026-09-04 03:40 PT;HF 仓核验 03:00 PT | 03:30 PT 版 J10 的"满历史 − 最近两帧 ≥ 3pp"闸门(外审指出不是命题的必要条件) **[08:50 PT 结果]** G0 ✓(recency-2 45.7%);G1 剂量 42.2→45.7→47.4,启发式净负 6 但救 6;G2 形式 ✓(+5.8pp)但赢家诅咒对照显示视觉记忆特有可救状态仅约 4%。**去留待用户裁决(§8)** |
| J11 | **控制对象 = 动态 B(用户 2026-09-04 11:10 PT 定案)**:文本推理历史恒定保留,平时 B=0 不存图,只在需要视觉记忆时才取 B 张。取代我提的『文本与帧的预算分配』(用户判:记忆方向已拥挤,且探针本身表明保留文本即够,分配问题不成立) | 用户裁定;§5 10:40 PT 与 08:50 PT 的数据 | 我 09-04 10:45 PT 的 §8 推荐 |
| J12 | **UI-Venus-2 的 MemGUI 62.6 / MobileWorld 65.8 是他方自报,不进我们的任何基线表**(只可标注为『他方自报』)。理由:(a) MemGUI 仓库 `site/leaderboard.json` 只有 8 条、最高 M3A 32.8,Qwen3-VL-235B 仅 23.4,62.6/77.3 只出现在 README 新闻栏的第三方自报;(b) 我们按官方协议、官方 mobile 示例逐条移植,同 117 题 50 步测得 45.7%,比其自报低 20pp,复现不出;(c) 时间线上 MemGUI 2026-02-09 公开,UI-Venus-2 2026-08 才发布(Qwen3.5-9B 底座),GUI-Owl-1.5 2026-02-15 与 benchmark 同期——两者不在同样的『benchmark 是否已公开』条件下。**我们对外只用自测的配对数**(同为 MobileWorld heldout 口径:Venus recency-2 39.5%/38 题;GUI-Owl 三臂 9/36、9/35、12/35 = 25–34%,来自 §4.15 期的联合训练 checkpoint) | 本地 leaderboard.json;§5 07:05 PT;arXiv 2609.00028 / 2602.06075 | 此前把 62.6 当作『换 executor 的理由之一』的表述 |
| J13 | **主张按外审弱化(2026-09-04 12:50 PT)**:不是『训练造出了利用非连续帧的能力』(冻结模型本来就会用 oracle 对:Venus 去文本 +30.0pp、GUI-Owl +17.7pp),而是**『配对的历史采样训练会改变 agent 从检索到的截图中获得的收益;因此冻结 agent 的比较不必然预测联合适配系统的排名与表现』**。核心实验为三臂配对 SFT + 固定选择器交互效应 | reviews/history_layout_sft_review_20260904.md | 我 09-04 11:40 PT 的『训练造出能力』表述 |

## 3. 当前生效的假设与口径

| 假设/口径 | 取值 | 谁定的 | 依据 | 若错会怎样 |
|---|---|---|---|---|
| 记忆预算 B | 2 张历史帧 + 当前帧 | 外审+用户(09-01) | 可枚举 C(n,2),可变 B 排在固定 B 之后 | 结论只对 B=2 成立 |
| 研究范围 | 文本动作/推理历史之上的**视觉情景记忆选择**(Venus N_IMG=0 仍保留 assistant 文本历史) | 外审(09-04) | Venus 官方协议 | 不得表述为"记忆 vs 无记忆" |
| 探针考场 | MobileWorld GUI-only 全部 117 题(train 78 + heldout 39;executor 探针不涉及 selector 训练故可全用),heldout-39 另报;50 步上限 | 外审(09-04) | 39 题一题 = 2.56pp,分辨不了 3pp | — |
| 解码参数 | temperature 0.0 + repetition_penalty 1.05 + frequency_penalty 0.3(官方离线示例脚本原配);`CC_VENUS_TEMP` 可改 | 官方示例 + 我方实测 | temp 1.0 实测 19.8%(动作解析崩坏),已排除 | 绝对分与论文差 20pp 的原因仍未定位 |
| MobileWorld 切分 | 按主 app 分层 2:1,train 78 / heldout 39,seed 20260825 | 我选的 | `cc_recipe/fixtures/mw_split_v1.json` | heldout 含训练模板则泛化数字虚高 |
| 最小值得效应 MDE | +3pp 绝对 | 外审功效论证 | rl_recipe §6 | 更小效应视为未证 |
| "可控上下文"定义 | recency 对错、∃某帧对使贪心解码对(等价类匹配) | 我选的 | summary §26.2:1,853 中 452 个(24.4%) | ∃ 带赢家诅咒,真可控率偏低 |
| MemGUI 分数口径 | 成功/128,缺失计失败;判错另列 | 我选的 | `memgui_tally.py` | 与榜单 Pass@1 定义一致 |
| MemGUI 步数预算 | **官方 golden×2.5+1**(脚本默认);09-03 两臂用的固定 50 已判偏差 | 官方 | `core/runner.py:87` | 固定上限会截断 90/128 题 |
| 判分模型 | 逐步 gemini-2.5-flash;终判 gemini-3.1-pro-preview | 用户裁定(2.5-pro 账号门控) | §26.7 | 与榜单判分不完全同源,须脚注 |
| 判分费用 | **硬停止**:未获预算不发起任何 judge 调用 | 用户(09-04 01:00 PT) | §27.9 事故 | — |
| selector 特征 | 冻结视觉塔均值池化 + 文本 embedding 均值 | 我选的 | summary §12/§19 | 已判为泛化瓶颈(J3) |
| 榜单对照 | **官方榜单**(leaderboard.json):M3A 32.8、Qwen3-VL-235B 23.4、GUI-Owl-1.5-8B 11.7/15.6。**他方自报**(README 新闻栏,不入基线表):UI-Venus-2-9B 62.6、Qwen-UI-Agent 77.3 | 第三方 | `memgui/site/leaderboard.json` 与 README | 见 J12 |
| 主机/算力 | hyper00,账号 4 卡上限;executor 单卡 vLLM | 全局规则 | — | — |

## 4. 已剪枝与未做的事

| 被剪掉的 | 方式 | 为什么 | 风险(偏向哪边) |
|---|---|---|---|
| MemGUI 官方步数口径重跑 | 不做 | 用户裁定,费用与价值不匹配 | 我们的 5.5% 偏悲观,不对外用 |
| AndroTMem 离线探针线 | 全部删除 | 用户裁定:非闭环 | 失去"oracle 记忆上界"的第三方度量 |
| oracle 记忆的闭环任务级上界 | 未测 | 无 oracle 帧标注的闭环考场 | 项目余量未知,靠 J10 探针间接判 |
| IRR / BadCase | 关闭 | Pass@1 不需要 | 无榜单 IRR/FRR 列 |
| per-step 状态依赖 baseline | 未做 | 联合线停线 | 联合线方差偏大 |
| 非法动作(double_tap)训练侧惩罚 | 未做 | 联合线停线 | 联合线绝对分偏低 |
| 难度优先采样真跑验证 | 未做 | 联合线停线 | 该机制效果未知 |
| 可变 B | 推迟 | 固定 B 未过闸 | — |
| 池 p00–p31 | 已停(CPU 让 MemGUI 后端) | 探针需重启 8–16 台 | 重启约十分钟 |

## 5. 实验台账(时间倒序,只列改变判断的条目;细节指向源文档)

### 2026-09-05 22:20 PT 定位(用户拍板):记忆余量 + "视觉历史不该默认取最近帧",不做记忆控制器方法
- 用户:"我们跟 MementoGUI 不太一样,我们探究的还是记忆余量,然后指出视觉并不应该总是最近图。"
- J18:论文定位 = (i) 冻结与训练后 agent 的**视觉记忆余量**如何随协议、模型、训练变化;(ii) 官方默认的 recency-N 历史窗
  **不是安全默认**——GUI-Owl 上 recency-2 比不给历史图更差(63.2 → 55.8,自一致 337 态;heldout 63.0 → 54.6),Venus 上中性。
  与 MementoGUI(插件式记忆控制器、冻结底座、无无关帧对照)的关系:它是方法,我们是余量与协议的测量;我们的无关帧对照与
  训练后再测是它没有的。B 的任务集只作为测余量的载体。
- 配套分析:逐状态 rescue / harm 计数(`harm_rescue.py`),把"recency 有害"从均值差变成逐状态证据。

### 2026-09-05 22:00 PT ★ 路 B 外审 + 先例核实 + "自然状态里几乎没有记忆依赖"的实证
- 外审(GPT-6 Pro,`reviews/memory_critical_tasks_review_20260906.md`):不直接做 B;先做文本×视觉因子通道干预(D);
  "诊断强于证据"——V(真实)≈V(无关)≈V(不给) 不证明文本含记忆;54 格非独立复现;闭环地板混杂;B 需三任务族 + 反事实孪生 +
  checkpoint 续跑 + 无历史 SFT 对照 + 交叉评测;J 在 +1.5pp 下 80% 功效需 ~1,100 状态。采纳表见该文档。
- 先例核实(WebSearch/WebFetch,2026-09-05 22:00 PT):
  **STAMP**(arXiv 2605.29324,2026-05-28):程序化向合成任务注入确定性记忆变量,控制何时编码/何时检索,产出可验证监督数据并做
  online RL;提出 Memory-World benchmark;与 GUI-Owl-1.5-8B-Think 对比。⇒ "构造记忆关键任务 + SFT/RL" 已被占。
  **MementoGUI**(arXiv 2605.18652,2026-05):学习型控制器在线选择/压缩/检索界面快照(工作记忆 = 文本摘要 + 视觉 ROI;情景记忆 =
  过往轨迹),**底座全部冻结**(UI-Venus-1.5-8B、MAI-UI-8B、GUI-Owl-1.5-8B/32B),插件式;GUI-Odyssey 上 Venus-1.5-8B:
  无历史 AMS 54.6 → 预测文本历史 66.3 → 文本摘要记忆 62.2 → 工作+情景记忆 68.3;消融称去掉 ROI 图会掉分。**无无关帧对照。**
  另有 Mem-W(2605.09317)、SE-GA(2605.16883)同期。
- 判断 J17:我们与 MementoGUI 的数据**相容而非矛盾**——它也显示文本历史拿走了大头(+11.7),视觉 ROI 只再加 +2.0(离线 AMS);
  我们的增量在于 (i) **无关帧对照**(它没有)把"视觉增益"与"扰动"分开,(ii) **部署协议**(保留 agent 自己的文本轨迹)下增益归零,
  (iii) 训练后的 agent 上同样归零。可区分的贡献必须落在"**视觉记忆余量的协议依赖性 + 它不随训练转移**",不是任务集。
  B 的任务集只作为 D 的载体,不作为贡献。
- 实证(`find_memory_states.py`,基于 GLM/30B 两套 caption 做溯源):337 自一致状态里,type/answer 动作且事实不在当前屏的 21 个,
  可溯源到更早某屏的 12 个,**其中事实不在任务指令里的只有 1 个**(MastodonPostPollTask step 15,事实 'philippe aghion',
  来源第 6 屏,且不在文本轨迹里)。即:**MobileWorld 自然轨迹里几乎不存在"必须回看截图"的状态**——这是"任务不需要视觉记忆"
  最直接的证据,也意味着 D 在自然状态上没有靶子,只能在构造/抹除的状态上做(与 B 的 pilot 合并)。局限:只覆盖 type/answer
  的字符串事实,依赖记忆的点击目标(如选择此前见过的条目)未覆盖。
- 修订顺序:**C(Venus 跨模型,在跑)→ B-pilot(24–40 个反事实孪生场景,同时承载 D 的因子干预)→ 按 pilot 结果定 B 主研究。**
- 闭环分母审计:44 任务里 recency 4 / random 2 / older 3 个无结果,全部为模拟器 HTTP 500(环境无效),非 agent 失败;下次评测
  用固定规则区分并重跑无效任务。

### 2026-09-05 21:00 PT ★★★ 判决:三臂配对 SFT + 三裁判 + seed 复现 + 未见闭环(全部落地)
- 数据:337 自一致状态 × 6 模型(底座、三臂 s0、两臂 s1)× 23 协议上下文 + 9 个裁判帧对(3 裁判 × 3 口径)。
  完整输出 `rl/docs/results/history_layout_final_20260906.txt`;叙述见 `story_20260905.md` 第二章结果。
- 主终点:H(底座)=−8.4pp;三臂 −2.1 / −0.5 / −0.7;J=+1.5 [−0.4,+3.4];seed1 +1.3 [−0.2,+3.0];J_SO=−0.2 [−1.6,+1.3]。
  训练后 V(真实历史)≈V(无关历史)≈V(不给图)。预注册判据 ①无法评估 ②勉强 ③**不满足** → 不进第三章。
- 裁判:54 格净信息量全部在 ±6–9pp 的 CI 内压零;裁判两两一致率 20–28%。oracle 循环性实证:∃-over-15 26.2% vs 单帧对 9.0% vs 无关帧 9.5%。
- 未见闭环:1/40、0/42、0/41,底座 0。
- 判断更新:J14 主张改为"文本轨迹即记忆通道;截图无额外可用信息,冻结与训练后皆然;训练只让模型不再受害";
  J15 基于冻结模型的 oracle-SFT 路线作废(循环性);J16 联合 RL 在现有任务上无回报信号(两次零梯度的机制),
  须先构造记忆关键任务(路 B)。用户拍板:先 C(Venus 跨模型)后 B,不轻易放弃。
- 工程:思考型裁判解析事故(见错误节)导致 GLM / Qwen3.8 首轮作废重跑;容器误删事故(见错误节)。

### 2026-09-05 16:04 PT ★★ 事故:我的发射脚本误删同事刚起跑的训练容器(hyper01 sglang-omni-jaxan-1)
- 经过:judge 链脚本把容器名写死为 `sglang-omni-jaxan-1`,每个阶段以 `docker rm -f <名字>` 清场后再同名新建。
  23:04Z 重启该链时,同名容器已是另一条 session(critic-hack Stage 3 GRPO)刚起跑的训练容器,被连锅端掉。
  对方 session 通过跨会话消息告知;我已回复认错并说明修复。
- 根因:(1) 名字写死而非按"docker ps -a 与 map 两边都空缺的最小编号"现场分配;(2) 用名字而非归属凭据判断
  "这是我的容器"。全局规则(2026-08-11、2026-09-03)早已禁止删除非本人创建的容器,是我的脚本模式违反了它。
- 修复(已落地到本线全部发射脚本 hyper00 ×3、hyper01 ×1):创建前 `alloc_name` 同查 docker ps -a 与 map;
  `docker run --label cc.owner=<本次运行唯一 token>` 并当场登记 map;`rm_own` 只删标签等于本 token 的容器,
  其它一律 SKIP 并打印。旧链全部停掉、按新脚本重启。附带发现并修掉:两条链同秒选卡撞到同一张 GPU0——
  `pick_gpu` 改为同时排除 map 中已登记且容器仍存在的卡。
- 代价:对方一次训练启动被打断(其已改用 jaxan-2 重启);我方三条链各损失一次服务重启(约 3 分钟)。

### 2026-09-05 00:00 PT ★ 自查发现:第二章没有真正未见的评测模板(口径降级,不重训)
- 触发:外审把主终点改为 `J = H_S − H_C` 后,我去核 SFT 训练集与评测切分的关系。
- 现象:SFT 数据取自 442 条**成功**轨迹,覆盖 73 个任务,**其中 21 个属 heldout 切分**(39 中的 21)。
  因此分析里筛的 "heldout" 对三臂而言**不是未见模板**。补救空间几乎为零——标注状态只能取自成功轨迹,
  而有成功轨迹的任务恰好就是那 73 个;底座新 rollout 的 31 个成功任务**全部**落在 SFT 训练集内,
  自一致标注的 337 个状态里**没有一个**来自未训练任务。
- 判断:**主终点 J 仍然有效**——三臂在这些任务上的训练暴露完全相同,泄漏对三臂对称,J 测的是
  "训练方案是否改变历史的价值",不受影响。**但不能说成泛化结果**:预注册判据里"在未见模板上"
  这句话无法满足,只能表述为"在训练过的任务模板上"。这是实质性削弱。
- 处置(默认推进,可推翻):**不重训**(重建切分后重训三臂 7.5 小时,今日交付不了,且现有结果不作废);
  今早的表按"训练模板内"如实标注,J 照报,不主张泛化;泄漏写进汇报稿的"不会说的话"。
- 待裁决:是否补一个**真正未见**的评测。两条路——
  (a) 放宽状态来源到**失败轨迹**,让 44 个未训练任务立刻有状态。**我不推荐**:参考动作变成"模型在
      失败路径上做过的动作",匹配它测的是"与底座的一致性"而非正确性,偏离越多分越低,对被训练过的
      臂系统性不利,是比泄漏更坏的混杂;
  (b) 在 44 个未训练任务上跑**闭环任务成功率**。这是真正的终点,不需要参考动作,天然跨模板。
      代价:三个合并后的权重需从 Tilde 传到 hyper00(约 48GB,走 HF)+ 三臂各跑一次闭环,合计约 3 小时。
      **推荐 (b)**,若用户认可则在标注作业之后接上。
- 教训记入 §7:**切分要在建数据时就切,不能等到分析时再筛**。`build_random_s_sft.py` 当时用了
  `--roots` 全量目录、没有按 `mw_split_v1.json` 过滤,这一步本该在建集时就做。

### 2026-09-04 13:00 PT 三臂配对数据集建成,时间距离与非连续性已分离
- 外审要求加 OLDER-CONTIGUOUS 臂把『看更老的帧』与『两帧彼此散开』分开。实现:older 规则与 random 共用
  同一个 rng 抽样,取其两帧**中位帧龄**处的一对**相邻**帧。三臂状态键完全一致,各 15,473 条:

| 臂 | 平均帧龄(中位/均值) | 两帧间距(中位/均值) | 隔离了什么 |
|---|---|---|---|
| recency | 1.5 / 1.5 | 1.0 / 1.0 | 基线(官方默认布局) |
| older | 10.5 / 12.5 | 1.0 / 1.0 | 与 recency 比 → **帧龄**的作用 |
| random | 10.5 / 12.7 | 6.0 / 8.7 | 与 older 比 → **非连续性**的作用(帧龄已配平) |

  older 与 random 的平均帧龄中位数都是 10.5、均值 12.5 vs 12.7,**时间距离配平**;唯一差别是两帧相邻还是散开。
- 无关历史对照(测干扰易感性)属**评测期**控制,不是训练臂:喂来自其他轨迹的历史帧,看各臂退化多少;
  实现放在标注器里,发射评测前完成。
- 脚本:`build_random_s_sft.py --frame-rule {recency,older,random,oracle}`;产物
  `sglang-omni-rl/sft_{recency,older,random}_v2.jsonl`。

### 2026-09-04 12:05 PT GUI-Owl 地板的失败形态拆解(回答用户『是不是一直打转用完步数』)
- MobileWorld heldout 三臂(`traj_final_held_{recency,S0,St}`)的失败轨迹拆解:

| 臂 | 成功 | 失败 | 撞满 50 步 | 其中死循环(末 8 步 ≤2 种动作) | 极短失败(≤5 步) | 中位步数 |
|---|---|---|---|---|---|---|
| recency | 9/36 | 27 | 25(93%) | 12(44%) | 0 | 50 |
| S0 | 9/35 | 26 | 21(81%) | 4(15%) | 0 | 50 |
| St | 12/35 | 23 | 23(100%) | 5(22%) | 0 | 50 |

- 判读:**失败几乎全是把 50 步预算跑光**,没有早停;但严格死循环只占失败的 15–44%,**其余是"持续做看似合理却不推进的动作"**(末动作以 click 为主),属能力不足而非退化循环。非法动作在这三臂为 0(§4.18 的 157 次非法动作出在官方 runner 通路,不是这里)。
- 对『SFT 让它学会』的判断:**不做**。(1) 可训数据不存在——442 条成功轨迹都是它本就会做的任务,
  MobileWorld/MemGUI 不提供标准轨迹(MemGUI 只有 `golden_steps` 计数);(2) 要示范就得从更强模型蒸馏,
  那是 MemGUI-Agent 走过的『训一个更好的 GUI agent』路线,属用户明确要避开的拥挤方向;(3) 动 executor 权重
  会毁掉『冻结 executor + 学出的记忆控制器』这一主张的干净性(涨幅归因不清);(4) **不需要修**——地板只伤
  Stage II,而 Stage II 在 Venus 上跑;Stage I 是步级反事实回报,不看任务成败,GUI-Owl 在其中反而可动步更多
  (可控上下文 24.4% vs Venus 保留文本历史时 7.5%)。

### 2026-09-04 11:10 PT ★ 用户定案:先做 Stage I,控制对象改为动态 B(设计与发射)
- 用户裁定(原话要点):(1)『先做这个吧』——指 Stage I(executor 反馈的反事实密集回报),J7 撤销;
  (2)否决我提的『文本与帧的预算分配』:记忆方向文章已拥挤,且探针本身说明『保留文本历史就够,不需要图』,
  分配问题不成立;(3)**把现在的固定 B 变成动态 B:文本部分不变,平时 B=0,需要图片记忆时才存 B 张。**
- 我的评估:用户的框架优于我的。数据直接支持它——保留文本历史时 **72% 的状态在全部 23 个上下文下输出
  同一个动作**(那些步 B=0 是免费的),B=0 已有 90.4%,而 ∃正确对 97.7%:**门控的可得区间就是这 +7.3pp,
  且大部分步不该花图像预算**。与拥挤赛道的区分点也更清晰:主流做法是**模型内部**的上下文折叠/摘要或
  **常开**检索,我们是**外挂、学出来、带预算意识的门控**,默认零成本。

**Stage I 设计(动态 B,预注册)**
- 动作空间:M ∈ {∅} ∪ {单帧 i}(6)∪ {帧对 (i,j)}(15),即 B ∈ {0,1,2};**文本推理历史恒定保留**(部署条件)。
- 回报:`r(M) = Match(E(x,M), a*) − Match(E(x,∅), a*) − λ·|M|`。以 **B=0 为反事实基线**,直接对齐门控语义;
  λ = 图像预算价格,做 sweep 出 Pareto 曲线(超参 ablation 规则)。
- 优化:RLOO(组 = 同状态多次采样 M);对照臂:恒 B=0 / 恒 recency-2 / 同预算随机门控 / 同预算监督模仿。
- **关键性质:标签集穷举全部 22 个动作,任何策略都能在其上离线精确评估**——λ sweep、对照、Pareto 曲线
  全部零 GPU 成本,只有特征抽取与训练需要 GPU。
- 闸门(预注册):在**未见模板**上,学出的门控须在平均图像预算 ≤ recency-2 的一半时,步级正确率 ≥ recency-2,
  且显著高于同预算随机门控;否则不进 Stage II。
- 数据:Venus 自身成功轨迹 204 条 → 1,726 个状态 × 23 上下文,hyper00 GPU1(41041)+ GPU3(41042)
  双服务并行标注,约 4 小时;脚本 `stage1_label.sh`,产物 `rl_v2/venus/stage1_labels_{a,b}.jsonl`。
- 已知偏置(写入 limitation,Stage II 覆盖):参考动作取自轨迹自身(自一致性偏置);仅用成功轨迹(幸存者偏置)。
- 同时:no-text 诊断在 116 状态处停止(用户裁定后不再是决策输入),部分结果留档。

### 2026-09-04 10:40 PT ★★ 关键更正:Venus 的"无记忆余量"是协议假象——去掉文本推理历史后余量 +30pp
- 假设(用户三问触发的自审):G2 测得 Venus 帧选择余量只有 +5.8pp,是不是因为我们让它保留了**自己的文本
  推理历史**,而 §17 的 GUI-Owl 探针**没有**文本历史?两者根本不可比,"Venus 没有记忆余量"可能是协议造成的。
- 现象(同一批状态、同 23 上下文、只去掉历史 assistant 文本;`venus_oracle.py --no-text`;n=70,运行中):

| 口径 | null(仅当前帧) | recency-2 | 随机对 | **∃正确对** | oracle − recency | 每状态不同动作数中位 |
|---|---|---|---|---|---|---|
| Venus **保留**文本历史(396 态) | 90.4% | 91.9% | 89.0% | 97.7% | **+5.8pp** | 1(72% 状态完全不敏感) |
| Venus **去掉**文本历史(70 态) | 37.1% | 55.7% | 46.0% | **85.7%** | **+30.0pp** | 4(仅 16% 状态不敏感) |
| GUI-Owl(§17,300 态,本就无文本历史) | 31.4% | 35.5% | 29.6% | 53.2% | +17.7pp | — |

  去文本口径下 recency 错的 31 态里,**17 态只能靠含远帧(帧龄 ≥5)的对救回**(55%),而保留文本口径下
  这一比例是 15/32 中的"仅远帧"占全体状态 3.8%。
- 判断:**Venus 的帧敏感度不但存在,而且比 GUI-Owl 更强**(+30.0 vs +17.7pp,且它是更强的 executor)。
  §5 08:50 PT 的"视觉记忆余量约 4%"只在"executor 已保留自己完整文本推理历史"这一条件下成立——
  那不是"记忆无用",而是**记忆已由文本承载**。两条线的正确读法:记忆的载体可以是文本也可以是帧,
  我们的帧选择器此前一直在与"模型自写的文本记忆"竞争而非互补。
- 对 J10 与 §8 裁决项的影响:**§8 中"不投入帧选择器"的推荐依据被削弱**,但也没有直接翻转——
  部署态(文本 + 帧)的任务级余量仍然小;真正有价值的问题变成"**固定上下文预算下,文本与帧如何分配**"。
  裁决项改写见 §8。
- 结果:no-text 全量(≥400 态)运行中;完成后更新本条与 §8。

### 2026-09-04 10:15 PT 温度对照臂:温度不是 45.7% vs 论文 65.8% 的原因(方向相反)
- 假设:四臂用官方离线示例脚本默认 temperature=0.0,而模型卡对 agent 类任务建议 1.0;20pp 的绝对分差距
  可能出自此,若成立则 G0/G1/G2 全部需在正确配置上重测。
- 现象(同 117 题、同协议、只改温度;`venus_temp_arm.sh`):

| 臂 | 全集 | heldout-39 | 对 temp 0 配对 W/L/T | 中位步数 | 撞上限 |
|---|---|---|---|---|---|
| recent2 @ temp 0(原基线) | 53/116 = 45.7% | 15/38 = 39.5% | — | 16 | 14% |
| recent2 @ temp 1.0 | 23/116 = 19.8% | 7/38 = 18.4% | 1/31/84;1/9/28 | 4 | 0% |

  机制:温度 1.0 下动作串大面积不可解析——106 个 episode 中 **59 个以非法动作终止**(temp 0 为 17),
  样例含残缺坐标、中英混杂乱码(`Click(point=(50Moderating?, let's say (400))`、`Swipe(start=(500, 780),
  end=(50不完整…`)。官方示例脚本把 `repetition_penalty=1.05` + `frequency_penalty=0.3` 与温度 0 配套使用,
  提温即退化。模型卡的"agent 任务用 1.0"应对应其自有采样配置。
- 判断:**温度 0 是本 agent 的正确设置;温度不解释绝对分差距**。四臂与 G2 的测量在这一项上干净,
  §5 08:50 PT 与 07:05 PT 两条结论维持。剩余嫌疑 = 我方移植 agent 与其内部评测实现的细节差异;
  该差距只影响绝对分,不影响臂间配对与 G2 判读。
- 改动:`ui_venus2_agent.py` 加 `CC_VENUS_TEMP`(默认 0.0)。去留:温度项结案,不再追。

### 2026-09-04 10:05 PT 我的操作错误:本地副本覆盖主机 agent,退化版跑了一轮
- 现象:把本地 scratchpad 的 agent 副本 scp 到主机时,覆盖掉了此前**只在主机上**打的两个补丁
  (think 标签归一化、`recent:N`/`change2` 历史策略);第一次启动的温度臂因此跑在退化版上。
- 处置:立刻停臂、删产出(9 题)、重打两个补丁并单测(`change2 S=[2,5]`)后重跑。**同时发现 Git 里的
  agent 副本也缺这两个补丁**(commit6 提交的是本地未打补丁版),已用主机实机版本覆盖并核对 size 一致。
- 教训:主机侧热补丁必须当场 scp 回本地并提交,否则 Git 与实机分叉,且任何"本地→主机"同步都会静默回滚。

### 2026-09-04 08:50 PT 离线主闸门 G2 出数:形式上过线(+5.8pp),但效应量在阈值边缘且一半来自扰动
- 假设:Venus-2 在未见模板上存在 oracle-2 对 recency-2 的动作级余量(预注册:≥ +5pp 且 random-2 ≤ recency-2 + 2pp)。
- 现象(400 状态,取自四臂成功轨迹各约 90–124 个;23 上下文/状态;贪心解码;保留全部文本推理历史;等价类 TOL=60/999):

| 子集 | 状态数 | recency-2 | 无历史图 | 随机对均值 | ∃正确对 | ∃正确单帧 | recency 错且某对能救 |
|---|---|---|---|---|---|---|---|
| 全部 | 396 | 91.9% | 90.4% | 89.0% | **97.7%** | 96.0% | 28/32 = 88% |
| heldout-39 | 136 | 90.4% | 90.4% | 88.2% | **98.5%** | 97.8% | 11/13 = 85% |
| train-78 | 260 | 92.7% | 90.4% | 89.5% | 97.3% | 95.0% | 17/19 = 89% |

  赢家诅咒对照(`venus_oracle_breakdown.py`):72% 的状态在 23 种上下文下输出**同一个**动作(每状态不同动作数
  中位 1、均值 1.78);recency 错的 32 个状态里 **16 个不给任何历史图也对**(扰动而非记忆),**只靠含远帧
  (帧龄 ≥5)的对才能救回的 15 个**(3.8% 的状态;heldout 5/136)。救回对里较老帧的帧龄中位 5(≥5 占 54%,
  ≥10 占 21%);61% 的救回状态里"视觉变化最大帧"落在某个救回对中(change2 启发式已覆盖的部分)。
- 判断:**按预注册口径 G2 通过**(全部 +5.8pp、heldout +8.1pp;随机对低于 recency)。但这是"存在某对"
  的统计,在基础复现率 90% 的区间里天然膨胀;**扣除扰动后,视觉记忆特有的可救状态约 4%**,与闭环 G1 的
  图景一致(2→8 帧净胜 2 题、启发式单独救回 6 题但整体净负 6)。**该 executor 上"选对帧"的可得余量是正的
  但小**;记忆的主要载体是它自己的文本推理历史(无图 90.4% 与 recency 91.9% 只差 1.5pp)。
  与 §17(GUI-Owl:recency 35.5 / oracle 53.2)不可比:那里参考动作来自别的策略且无文本历史。
- 改动:无。结果:G0 ✓、G2 ✓(形式)、效应小。
- 去留:按预注册应进入 selector 重建;但效应量意味着任务级收益上限约 +2–4pp,117 题需 ≥4 净胜才能分辨,
  MemGUI(128 题、LLM 判分)更难。**方向性抉择交用户**(见 §8 新增项);辅助诊断(无文本历史口径)与补跑轮
  仍在自动执行,不改判决。

### 2026-09-04 07:05 PT Venus-2 闭环探针 v2 收官(G0 过线;G1 诊断:多帧小增益、盲选远帧平均有害但能救少数题)
- 假设:UI-Venus-2-9B 在 MobileWorld 上地板健康(G0),且历史截图给法对任务成败有杠杆(G1)。
- 现象(117 题各臂一题 runner 报错无结果,n=116;heldout-39 子集 n=38;50 步上限;程序判分;温度 0):

| 臂 | 全集成功率 | heldout-39 | 对 recency-2 配对 W/L/T(全集;heldout) | 死循环 | 撞上限 |
|---|---|---|---|---|---|
| recent0(仅当前帧 + 文本历史) | 49/116 = 42.2% | 15/38 = 39.5% | 5/9/102;3/3/32 | 9% | 18% |
| recent2(官方 N_IMG=2,基线) | 53/116 = 45.7% | 15/38 = 39.5% | — | 3% | 14% |
| recent8 | 55/116 = 47.4% | 17/38 = 44.7% | 10/8/98;4/2/32 | 3% | 12% |
| change2(最近帧 + 视觉变化最大帧) | 47/116 = 40.5% | 16/38 = 42.1% | 6/12/98;4/3/31 | 6% | 17% |

  任一臂成功(四臂并集)65/116 = 56.0%(heldout 21/38),四臂全成 34;只靠 change2 或 recent8 才成功的题
  12(heldout 6)。产物 `rl_v2/venus/v2/`,汇总脚本 `venus_tally2.py`。
- 判断:**G0 过线**(recency-2 45.7% ≥ 40%),但比论文 65.8% 低约 20pp——差异候选:解码温度(官方脚本默认 0,
  模型卡建议 agent 任务用 1.0 并保留推理历史)、harness/版本;已列待核,不影响 G1/G2 的配对判读。
  G1:帧数剂量响应 42.2 → 45.7 → 47.4,2 帧之后边际收益小且 8 帧对 2 帧净胜 2 题在噪声内;非连续启发式
  整体净负 6 题,与 J2(盲选远帧平均有害)一致,但它单独救回 6 题、heldout 上净 +1——"旧帧有用"的题存在
  但少;四臂并集比 recency-2 高约 10pp,是任务级"给对记忆"的粗上界,含跨臂赢家诅咒,不作定量引用。
- 改动:无(诊断)。结果:待 G2(离线 oracle-2 余量,13:57Z 起,约 400 状态 × 23 上下文,预计 1.5 小时)。
- 去留:G0/G1 不构成停止条件;去留由 G2 决定(预注册)。

### 2026-09-04 03:50 PT 外审改判闸门;Venus-2 探针 v1 停、v2 重发
- 假设:换 executor 后"满历史 > 最近两帧"能作为记忆杠杆的闸门。
- 现象:外审指出该闸门非命题的必要条件(token 稀释可让满历史输而 B=2 选择赢),39 题分辨不了 3pp,
  且未诊断 selector OOD 失败就换表征是乱撞。
- 判断:采纳。主闸门改为 Venus 上离线 oracle-2 余量(G2),闭环探针改为 117 题四臂剂量/选择诊断(G1),
  地板健康 G0;全部在数据落地前预注册于 reviews/executor_swap_review_20260904.md §3。
- 改动:v1 探针(heldout-39,N_IMG 0/2/all)于 08:5xZ 停止,部分结果归档 `rl_v2/venus/partial_v1_*`;
  agent 加非连续启发臂;tally 加列联表与循环/撞限诊断;v2 探针 117 题四臂发射(见 §9 更新)。
- 结果:待。去留:待 G0–G2。

### 2026-09-04 03:00 PT 判断整合与方向改判(本文件建立)
- 假设:历史台账互相冲突导致我反复改口(SFT 一会做一会不做)。
- 现象:J1(iter_59 能读非连续帧,§10/§17)与 §4.13"executor 用不动"叙事并存;后者是联合线在
  非 oracle 选帧下的任务级现象,我把它误当作能力缺失。
- 判断:两者不冲突——能力在(J1),收益取决于选帧精度(J2),精度不泛化(J3)。
- 改动:撤回 SFT(J6),Stage I 暂停(J7),方向改为换 executor + 余量探针(J10)。
- 结果:待外审与探针。去留:待定(探针出数后更新)。

### 2026-09-04 01:00 PT 判分费用事故与止损
- 现象:用户指出 Gemini 账单约 50 美元。核算:MemGUI-Eval mode=full 每轨迹 ≈50 次 flash +
  3 次 pro(终判/IRR/BadCase),≈$0.15–0.20/轨迹,论文自报 $0.213;我们多轮重判放大到
  300–350 轨迹次。我又在未获预算时起了官方口径重跑(9 题起跑、2 题判分后停)。
- 改动:止损脚本;IRR/BadCase 开关(J9);判分列为硬停止。出处 summary §27.9–27.10。

### 2026-09-04 00:40 PT MemGUI 两臂终表(50 步上限口径)
- 臂 A 官方默认窗 7/128;臂 B recency B=2 7/128;配对 同成 4 / 仅 A 3 / 仅 B 3 / 同败 118,
  McNemar p=1.0;82% episode 跑满 50 步;B 臂死循环尾 49% vs A 22%。
- 协议偏差:固定 50 步 vs 官方 golden×2.5+1,79/75 条被截断 → 只作内部配对(J8)。§27.9。

### 2026-09-03 AndroTMem 探针(已删)
- 原版 8B 在 gold 文本历史下,远程截图无增益;结论限离线设定。用户 09-04 00:05 PT 令删除。§27.5–27.8。

### 2026-09-03 冻结 iter_59 selector RL(v2 服务)null result
- 22 轮,heldout 贪心 RL=init=recency;70% 组零对比;停。§25–26.6;外审 reviews/rl_null_result。

### 2026-09-01/02 行为标签预训练
- 宽口径 +5.1pp(3 种子稳健),严口径 +0.2pp;数据减半不掉(J3)。§19–20。

### 2026-09-01 行为天花板与侵蚀假说反转
- oracle 对 53.2% vs recency 35.5% vs 随机对 29.6%(J1/J2);iter_59 帧敏感度高于底座(§10)。

### 2026-08-25→09-01 联合训练 P1–P4(cua_lite §4.x)
- 批 60 交叉:Et×recency 45% > Et×St 40% > Et×S0 30%(§4.13);批 100 终判 learned−recency
  +8.8pp(p=0.45)与 selector-only −6.2pp(p=0.73)符号相反 → 效应≈0(§4.15/4.18);selector
  三连病理修复(§4.16);P4 因结构缺陷停线(§4.20)。

### 2026-08-04→09 OSWorld RL v1/v2
- 20 迭代 RL 81/240 vs recent 88/240,零效应;selector/LoRA 均未训动。`audit_ledger_20260809.md`。

## 6. 数字总表(口径不同不并排比较)

| 口径 | 数字 | 出处 |
|---|---|---|
| MemGUI 榜单 GUI-Owl-1.5-8B / 32B | 11.7 / 10.9 Pass@1;15.6 / 20.3 Pass@3 | leaderboard.json |
| MemGUI 榜单 UI-Venus-2-9B / 27B;Qwen-UI-Agent(235B) | 62.6 / 70.3;77.3 | memgui README 新闻栏 |
| MemGUI 自测(50 步上限,3.1-pro 终判)A / B | 5.5 / 5.5(7/128 各) | §27.9 |
| MobileWorld heldout-20 贪心(RL pv22 / init / recency) | 15.8 / 15.8 / 15.8–26.3 | §26.6 |
| 行为天花板 300 状态(iter_59) | recency 35.5,当前帧 31.4,随机对 29.6,oracle 对 53.2 | §17 |
| 可控上下文 | 452 / 1,853 = 24.4% | §26.2 |
| 监督预训练 val B=2 宽/严口径 vs recency | +5.1 / +0.2 pp | §20 |
| 联合线批 60 交叉(20 题) | 40 / 45 / 30 | cua_lite §4.13 |
| 判分成本 | ≈$0.15–0.20/轨迹(论文 $0.213) | §27.9 |
| Venus-2 离线 G2 **去文本历史**口径(70 态,运行中) | null 37.1;recency 55.7;随机对 46.0;∃对 85.7(oracle−recency **+30.0pp**) | 本文件 §5 10:40 PT |
| Venus-2 温度对照(117 题,recency-2) | temp 0: 45.7 / 39.5;temp 1.0: 19.8 / 18.4(配对 1 胜 31 负) | 本文件 §5 10:15 PT |
| Venus-2 离线 G2(396 状态 / heldout 136;全部 / heldout) | recency 91.9/90.4;无图 90.4/90.4;随机对 89.0/88.2;∃对 97.7/98.5;仅远帧可救 3.8% | 本文件 §5 08:50 PT |
| Venus-2 闭环四臂(MobileWorld 116 题 / heldout 38,50 步,温度 0) | recent0 42.2/39.5;recent2 45.7/39.5;recent8 47.4/44.7;change2 40.5/42.1;四臂并集 56.0/55.3 | 本文件 §5 2026-09-04 07:05 PT |

## 7. 我犯过的错与纠正

| 错误 | 如何发现 | 纠正 | 影响过的结论 |
|---|---|---|---|
| 发射脚本按写死的容器名 `rm -f`,误删同事刚起跑的训练容器 | 对方 session 跨会话通报 | 名字现场分配 + owner 标签 + 只删自己标签的容器;全部脚本已改 | 无(工程事故,不涉结论) |
| 用思考型模型(GLM-4.6V / Qwen3.8)做结构化选帧,max_tokens=32 把答案截在 `<think>` 里,正则捡到无关数字 | 检查原始输出:310/337 解析为空 | 关 thinking、预算 2048、要求 `ANSWER: i,j` 收尾并只解析 `</think>` 之后 | 两个更强 judge 的首轮结果作废重跑 |
| SFT 训练集未按 `mw_split_v1.json` 过滤,21/39 个 heldout 任务进了训练集,导致第二章没有真正未见的评测模板 | 外审改主终点后复核训练集与切分的关系 | 口径降级为『训练模板内』,不主张泛化;推荐补闭环未见集 | 第二章预注册判据中『未见模板』一条 |
| J12 初稿把 Venus 45.7%(heldout-38)与 GUI-Owl 15.8%(anchor-20 贪心子集)并列,考场与协议都不同 | 用户追问地板成因时复算三臂 heldout | 改为同口径 25–34% vs 39.5%,差距由『约 30pp』更正为『5–13pp』 | J12 的『换 executor 的配对证据』强度 |
| 本地 agent 副本覆盖主机热补丁,退化版跑了一轮温度臂;Git 副本同样缺补丁 | 重启后 CC_TRACE 打印退化格式 | 停臂删产出、重打补丁单测、用实机版本覆盖 Git 并核对 size | 无(该轮产出已删除) |
| MemGUI 步数上限写死 50,违反官方口径 | 09-04 读 runner 源码 | 脚本改默认官方口径;5.5% 降级为内部口径 | "可与 11.7% 比"的表述 |
| 判分 API 花费约 $50 未先要预算;又起了会再花 $80–100 的重跑 | 用户账单 | 止损、开关、硬停止 | — |
| "executor 用不动非连续帧"标签贴错,导致提出多余的 SFT | 用户追问 iter_59 证据 | J1/J6 | 09-04 02:30 PT 的建议 |
| 配对 oracle 53.2% 被当作固定策略可达值(赢家诅咒) | 09-03 用户质疑后审计 | §27.7 更正 | "路线的肉还在"的定量表述 |
| heldout 曲线"连续上行"报为信号 | 配对 recency 复测 | 判噪声 | §25 |
| 误删并行 session 容器 sglang-omni-jaxan-1 | 对方重建 | 判僵尸看 map 与创建 session | — |
| 本地 TCC 拒读把空 blob 推上 main;map 三次被 read-filter-overwrite 抹空 | 事后核对 size / 不变量 | 每次 push 验 size;map 只走 rebuild 脚本 | cua_lite §4.20 |

## 8. 待用户裁决

| 事项 | 我的推荐 | 理由 | 若不批 |
|---|---|---|---|
| **方向(10:45 PT 改写,取代此前"不投入"的推荐)**:记忆载体是文本还是帧?<br>去文本口径显示 Venus 帧敏感度 +30pp(强于 GUI-Owl 的 +17.7pp),但保留其自写文本历史后只剩 +5.8pp | **我的推荐:把研究问题定为"固定上下文预算下的记忆分配"**——控制器同时决定保留哪几张帧与保留/压缩哪段文本推理历史,reward 用反事实记忆优势;在 GUI-Owl(帧敏感、任务级地板)与 Venus(强 executor、文本记忆强)两个 executor 上各跑一遍,后者兼作"强模型如何自解记忆"的对照 | 帧选择单独看是在与模型自写文本记忆竞争,天花板小;预算分配问题两边都有余量,且是 RL 天然适用的组合决策 | 若只想最快出结果:仅在 GUI-Owl 上做 Stage I(它的 +17.7pp 余量与 24.4% 可控上下文都已量过) |
| executor 换为 UI-Venus-2-9B(放弃 GUI-Owl/iter_59 主线) | 批,但以探针为闸 | J5 地板三重实证;榜单 9B 级模型已达 62.6% | 只能走"缩主张"路线 |
| MemGUI 定稿运行的判分预算 | 每臂约 $10–15(开关已开) | 只跑定稿配置一次 | 无对外可比数字 |
| 第二档磁盘清理(hyper00 `osworld/` 33G、`aw/` 13G) | 删 | 均已上 HF 或可重建 | 占盘 |

## 9. 未来 6 小时的执行计划(2026-09-04 03:50 PT 修订,用户睡眠期间)

1. [done 03:40 PT] 外审落盘 `reviews/executor_swap_review_20260904.md`,闸门 G0–G2 预注册。
2. [done 01:50–03:00 PT] rle(GUI-Owl)容器删除;GPU1 起 `sglang-omni-jaxan-3` = UI-Venus-2-9b vLLM
   (131k 上下文,64 图/请求);map 登记;Venus-2 官方 mobile 协议移植为 `ui_venus2` agent,单任务冒烟通过
   (8 步、约 5 s/步)。
3. [done 14:04Z] 探针 v2:MobileWorld 117 题 × 四臂(N_IMG 0 / 2 / 8 / 启发式非连续 B=2),池 p00–p11 并发 12,程序判分,
   零 API 费用;每臂预计 40–60 分钟;tally 报成功率、对 recency-2 的 wins/losses/ties、死循环率、撞限率、
   成功中位步数;heldout-39 另报。
4. [done 15:36Z] 离线 G2:`venus_oracle.py`(Venus 官方多轮协议,23 上下文/状态,状态取自 v2 四臂成功轨迹,≤400
   状态,heldout-39 / train-78 分报)+ `venus_oracle_verdict.py`(0–999 网格 TOL=60 等价类);两状态管线
   测试通过;`venus_g2_after_probe.sh` 等 v2 收官自动执行,产物 `rl_v2/venus/oracle_v2_verdict.txt`。
5. 出数 → 更新 §2 J10 与 §5;G2 成立则起草 selector 重建方案(先诊断 OOD 失败原因,再谈表征),否则写
   负结果并停。
