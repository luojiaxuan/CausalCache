# 枚举 oracle 选帧:强基线 + 上界 + 标签(实验契约 v1)

> ## 首批结果(2026-08-08,30 态全枚举,AgentNet/OpenCUA ubuntu)
>
> | 臂 | 步级正确率 |
> |---|---|
> | **oracle-最优2** | **23/30 = 76.7%** |
> | recent-2 | 18/30 = 60.0% |
> | 随机-2 | 17/30 = 56.7% |
> | B=0 | 17/30 = 56.7% |
>
> - **上界高度 oracle − recent = +16.7pp**。⚠️ **结构性非负、不可做显著性
>   检验**:recent-2 与随机-2 都是被枚举的子集之一,oracle 按定义 ≥ 两者。
>   这个数只能读作"上界有多高",**不能**当作"策略对内容敏感"的证据;
> - **公平对照 recent − 随机 = +3.3pp**(1 个 state)——**"选最近两张"并不
>   比"随便两张"更强**,部署默认选法本身没有优势;
> - 三类拆分:easy 56.7% / **winnable 20.0%(6 态)** / hopeless 23.3%。
>
> **内容敏感性的有效证据(不靠上界差):**
> 1. winnable 态内**子集间成败分化**:正确子集占比中位 **39.3%**——同一
>    state、同一策略,换两张历史图,约 40% 组合对、60% 错;
> 2. **承载帧集中**:最强承载帧的携带率平均比该 state 基准高 **+33.4pp**,
>    说明成败绑定在特定帧上,不是均匀噪声。
>
> **→ AndroidWorld 的 "correct − shuffled ≈ 0" 负结果在桌面数据上未复现;
> 帧内容确实影响动作,且信号集中(可学)。**
>
> **剪枝校验:22/23 = 95.7% 命中**(top-8),达标,后续可用便宜版
> (53 次/态)铺开。
>
> **不确定性(必须同报)**:30 态、6 个 winnable,样本极小;+16.7pp 仅由
> 5 个 state 的差异撑起,置信区间很宽。**本轮只够定性(头寸存在、可学),
> 不够定量。** 下一轮需扩样本(建议 `--skip-easy` 铺 300-500 态,把算力集中
> 到 B=0 出错的态,目标 50-100 个 winnable)。
>
> 运行记录:h00 GPU0,~40 分钟/30 态;`processed=30`,
> `skipped={eval_error:ValueError:1, too_many_candidates:1}`。

预注册于 2026-08-08,用户裁定"提前做"。本实验**不训练 HGKV**,
只回答一个干净的问题:

> **冻结 policy 不动,只把历史帧换成更好的,步级动作正确率能提多少?**

## 0. 为什么值得优先做(三个产出,一次拿到)

1. **强基线**:冻结 policy + 无 HGKV + 完美选帧。上限低于联合训练(缺 HGKV),
   但它是"选择本身值多少"的干净度量,是后续所有方法的对照;
2. **上界(标尺)**:任何 selector 学到的成绩都可以除以它,得到捕获率。
   v1 时代我们只捕获了 **6.5%** 却不自知,就是因为没有这把尺子在手边;
3. **监督标签**:每个 state 的"最优 B 帧",可直接喂给 selector 做排序学习。

## 1. 纠正一个此前反复出现的错误框架

历史文档多处写"离线增益不迁移闭环",**这个说法不成立**。实测:

| 部署口径 | 模型−锚 | oracle−锚 | 捕获率 |
|---|---|---|---|
| B=2 | **+0.0066** | +0.1019 | **6.5%** |

模型的离线增益只有可得头寸的十五分之一,**这个量级不足以改变动作**,
闭环没反应是应然。加之离线语料(AgentNet/OpenCUA 桌面示范)与闭环
(OSWorld)不同源。**正确表述:我们从未做出足够大的离线增益,
"离线→闭环"这个命题至今未被检验。**本实验的目的之一就是先把离线增益做大,
让这个命题第一次变得可检验。

## 1.5 v3 步级 RL 的判据结果(2026-08-08):训动了,但没学到

v3(离线步级 GRPO,奖励 = teacher-forced `log p(gold action)`)跑完 3000 态:
**750 次优化步、位移 1.557、熵 3.94→2.32**,机制完全奏效
(对比 v1 OSWorld 闭环 20 迭代仅 67 步、位移 0.044)。

同口径判据(**200 个** B=0 出错的困难态,训练未见的行,τ=0 取 top-2):

| 臂 | 步级正确率 |
|---|---|
| **recent-2** | **65/200 = 32.5%** |
| 随机-2 | 53/200 = 26.5% |
| init(随机初始化头) | 46/200 = 23.0% |
| **learned(训练后)** | **43/200 = 21.5%** |

- **learned vs init:−3,p=0.701** → 训练一无所获;
- **learned vs recent:−22,13 胜 35 负,p=0.002** → **显著劣于 recent-2**;
- recent vs 随机:+12,p=0.104。

(92 态时四臂看似打平,是样本不足;200 态下 recent 显著领先。**中途读数
不可当结论**,这条本身也是教训。)

**两个决定性观察:**

1. **learned 与 init 的选帧仅 12% 重合** —— 训练把行为改得面目全非却毫无收益,
   **排除了 v1 那种"没训动"**,病因是"改的方向错";
2. **init(随机初始化)23.0% 也显著低于 recent 32.5%** —— 在 hidden state 上
   做线性打分的**几何偏置本身有害**(实测该头强烈偏好老帧,归一年龄 0.84,
   而老帧平均不如近期帧)。**这意味着换损失函数可能不够,架构也在嫌疑名单上。**

与 oracle 并置(同一困难态分层):oracle **44.6%** / recent **32.5%** /
随机 26.5% / init 23.0% / learned 21.5%。
**头寸真实(+12pp),但 v4 要超越的标杆是 recent 的 32.5%,不是 init 的 23%。**

### 归因假设(可检验):训练目标与评测指标脱钩

训的是 `log p(gold action)`(连续),评的是 **argmax 是否正确**(离散)。
log p 可以显著上升而 argmax 一次都不翻 —— 恰好解释"reward 小幅改善 +
位移巨大 + 正确率纹丝不动"。

**→ 解法即本契约的产出:用枚举标签(哪些子集能做对)做监督排序学习,
目标与评测指标同源。** 这也正是用户最初的提案。

## 2. 数据

| 项 | 值 |
|---|---|
| 语料 | **AgentNet/OpenCUA 桌面示范**(人类标注,每步有 ground-truth 动作) |
| 起步规模 | `desktop_did_corpus_v1` 的 969 组打通流程 |
| 扩展 | hyper00 `agentnet-frames-v3`(规模更大),流程通过后再扩 |
| **不用 GUI-Odyssey** | 已判 NO-GO,理由与本假设相悖:"Odyssey 上 recent 天然更强且目标 case 稀少"——用它等于自己发烂牌 |
| 候选池 | **该 state 的全部历史帧(含 recent)**,不设 `age ≥ B+2` 之类阈值——去启发式是本实验的要点 |

## 3. 打分指标(容差在这里解决)

**步级正确率** = 动作类型匹配 **且** 坐标误差 ≤ **25px**
(沿用语料里已实现的 `coordinate_tolerance = 25`)。

关键设计:**这个指标不需要可导**。selector 的监督来自"哪个子集得分最高",
标签在离线生成阶段算完;训练时用排序/分类损失。**容差问题被完全吸收进
标签生成,不进损失函数。**(只有后续 SFT HGKV 时容差才会真正咬人,
届时用"容差内则以模型自身输出为目标"的 relabel 方案。)

## 4. 枚举与剪枝

- 全枚举 C(n,2),n≈25 → **300 组/state**,每组一次前向(约 2s)→ 10 min/state,太贵;
- **剪枝**:先固定一个搭档逐个评估单帧(n≈25 次)→ 取 top-8 → 其间全配对
  C(8,2)=28 → 合计 **53 次/state**,省 5.7 倍;
- **剪枝校验(必做,不可省)**:在 **30 个 state** 上同时跑全枚举与剪枝,
  比对命中率。
  - 命中率 ≥90% → 采用剪枝;
  - 否则放大到 top-12(C(12,2)=66,合计 91 次)重校验;仍不达标则全枚举 + 缩小样本。
  - **理由**:旧数据显示单帧边际为正 68.4%、集合边际为正仅 36.8%,
    **两帧间存在交互**,可能存在"单独平庸、合起来很强"的组合被剪枝漏掉。
    漏掉会低估头寸 → 可能误判成"没头寸"而错误终结项目。

## 5. 四臂(缺一不可)

| 臂 | 含义 |
|---|---|
| **oracle-最优2** | 枚举出的最佳组合(上界) |
| **recent-2** | 部署默认基线 |
| **随机-2** | **防自欺的关键对照** |
| **B=0** | 无历史图 |

**随机-2 这一臂是硬要求**:归档里有一条警示——AndroidWorld success-anchor 数据上
"喂正确历史图 vs 喂打乱/无关历史图,差异 ≈ 0","说明它没有稳定使用图像内容"。
不设这一臂,我们会把"模型对图像内容不敏感"误读成"选择有效"。

**但"oracle ≈ 随机"不是无条件的终结信号**(2026-08-08 用户指出):它有两种
互斥解释,本实验**无法分辨**——

1. 策略确实不使用历史图内容 → 选择无价值;
2. **策略没见过"动作行 + 插入历史图"这种 prompt 格式,读不懂**
   → 不是内容无用,而是接口没对齐。**这正是 HGKV 要解决的问题**,
   必须 selector 与 HGKV 联合训练才能检验。

该情形下的正确动作是**记录并转入联合训练分支**,不是终结项目。

## 6. 预注册判据

| 结果 | 结论与动作 |
|---|---|
| oracle ≈ 随机-2 | **两种解释无法分辨**(不看内容 / 读不懂插图格式)→ **不终结**,记录后转入 selector+HGKV 联合训练分支再判 |
| oracle ≈ recent-2 且 > 随机-2 | 策略确实用内容,但 recent 已接近最优 → 选择头寸小;**冻结 policy 这条路到顶**,价值转移到 HGKV |
| **oracle >> recent-2** | 选择有真实头寸 → 产出标签,进入 selector 学习阶段,并**以此为分母报捕获率** |

**本实验不产生"项目终结"结论**——它只测"冻结 policy + 只换帧"这一条路的
上限。终结与否要等联合训练(HGKV 参与)那一轮。

## 7. 之后:闭环检验点(用户裁定,硬要求)

oracle 臂本身**不能部署**(需要 ground truth)。闭环检验的对象是
**用这批标签训出的 selector**:

- **早做、小步做**:selector 训到约 2000 步就先跑一次 **40 任务闭环快测**,
  不等训完整套;
- 若步级正确率涨了但闭环纹丝不动,**先停下追问口径与 benchmark 的决定因素**,
  不要继续加训;
- 闭环判读沿用已标定的噪声口径(同系统重测翻转 11.7%、总数 ±4 任务/120,
  单轮检出下限 ≈9pp),**报点估计必须同时报噪声与检出力**。

## 8. 与联合训练的关系

本实验是**基线与标尺**,不是终点。后续的联合训练(步级 GRPO:
selector 用 score-function 绕过离散、HGKV 走可导 SFT,奖励 = 每步
`log p(ground-truth action)`)上限更高,但必须以本实验的 oracle 为分母
报捕获率,否则会重演 v1"涨了 6.5% 却以为方法有效"的误判。

## 1.6 v4 监督排序:**项目第一个正向结果**(2026-08-09)

用枚举标签做子集级成对排序(正=能做对的子集,负=做不对的;子集分=成员帧分
之和,与部署 top-B argmax 同口径)。训练 1011 态 / 留出 252 态,252 优化步,
成对损失 0.791→0.716,**留出集排序准确率 0.5828**(0.5=随机)。

同口径判据(205 个困难态,与 v3 完全同一评测:同 skip-first、同筛选、同指标):

| 臂 | v3(log p 目标) | **v4(监督排序)** |
|---|---|---|
| **learned** | 21.5% | **36.1%** |
| init(随机初始化) | 23.0% | 21.5% |
| recent-2 | 32.5% | 32.2% |
| 随机-2 | 26.5% | 25.9% |

- **learned vs init:+30,40 胜 10 负,p=0.000** —— 监督确实学到了可迁移能力;
- **learned vs recent-2:+8,21 胜 13 负,p=0.229** —— 首次超过部署基线,
  **但尚不显著,不可宣称胜利**。

**捕获率**(困难态分层,oracle 49.1% 为分母):
(36.1−32.2)/(49.1−32.2) = **约 23%**。对比 v1 时代的 6.5%(且那是与闭环
脱节的离线指标)。

**三条判断:**

1. **v3 的失败归因被证实**:同架构、同初始头、同数据,只换目标函数,
   21.5% → 36.1%。**病因是目标与指标脱钩,不是架构、不是数据量**;
2. **架构嫌疑洗清一半**:init 头 21.5% 确实劣于 recent,但同架构训完能到
   36.1% —— **几何偏置可被学习纠正,不必换架构**;
3. **p=0.229 是当前瓶颈**,已启动扩样本评测(4 分片 × 250 态)。

**下一步次序(先证伪再投入)**:① 扩评测把显著性打下来;② 若显著再加训
(损失仍在降,1011 态远未吃饱);③ 然后才做闭环快测 —— 按用户裁定,
离线涨了必须早验闭环。
