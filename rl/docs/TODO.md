# CausalCache 记忆选择线 TODO(维护于 2026-08-09)

> 配套文档:[实验台账](audit_ledger_20260809.md)(为什么这么做、踩过什么坑)、
> [枚举契约](oracle_selection_contract.md)、[方法转向契约](rl_pivot_contract.md)。
> **本文件只管"还要做什么"**;结论与证据一律以台账为准。
>
> 每条固定五段:**为什么**(不做会怎样)、**完成判据**(什么算做完,可验收)、
> **阻塞**、**成本**、**状态**。没有完成判据的条目不许进这张表 —— 那种条目
> 永远做不完。

## 状态图例

`🔴 在跑` · `🟡 就绪(可立即开工)` · `⚪ 阻塞` · `🟣 待你裁决` · `✅ 完成` · `❌ 作废`

---

## A. 在跑

### A1 🔴 B 曲线重测(你指定优先)

- **为什么**:B=2 这个工作点的选型依据已经不可靠。旧依据是
  teacher-forced log-prob DiD 指标(oracle 空间 B=2 +0.102 / B=4 +0.037),
  正是被 v3 证伪的那套口径;而 `frozen_main_row_v1` 记录 B2→B4 在真实任务
  成功率上还有 **+4.6pt**。不重测,审稿人一句"为什么不在 B=4 上比"就能
  把主结果的操作点打成 cherry-pick。
- **完成判据**:两张表 —— ①**可部署臂**(recent-B / 随机-B):全部 1000 态,
  按真实占比(2809:2494)重加权,给出 **recent-B 跨预算的配对检验**,
  这是选工作点的依据;②**上界**(oracle):只在困难层 600 态,标明头寸是
  结构性非负、不可检验。另需:三个 B 打在同一批 dp_id 上;easy / 非easy
  分层拆开看大 B 修好了什么又弄坏了什么;披露预算饱和(候选数 < B)条数;
  **报整条容差曲线而非单点**(见 D1)。
- **阻塞**:无。
- **成本**:5 卡,剩余约 **8.6 小时**(实测 2.91 前向/秒、每次 1.72s)。
- **🔧 08-09 第二次改计划:easy 态改为只打三臂(`--arms-only`)。**
  精确量过速率后,全量三档要 **13.2 小时**,太长。唯一不牺牲结论的砍法:
  easy 层要回答的是"加历史图会不会把本来答对的题弄坏",而那由 **recent-B
  这条可部署臂**回答;easy 的 oracle 接近饱和(只要存在保住正确的子集,
  oracle 必然找到),不值 48-83 次前向。改后每个 easy 态 3 次前向,省 20 倍。
  **跨预算的可部署对照仍是全量 1000 态,一点没缩。** 13.2h → 8.6h。
  代价:**oracle 只在困难层定义**(600 态),报告时必须写明,不能让读者
  以为总体 oracle 也测了。归约脚本已把"可部署臂"与"上界"拆成两个分母两张表。
  B=1 例外:它本来就便宜,整批 1000 态全枚举,白拿一列 easy 层 B=1 oracle。
- **状态**:h00 GPU6/7(分片 0,1)+ h01 GPU1/2/3(分片 2,3,4),顺序 1→2→4。
  **B=1、B=2 各 1000 态已完成**;B=4 在跑,**剩余约 12.2 小时**。
  ETA 我算错过两次,两次错因不同,都记在这里:
  1. "约 83 次前向/态" —— 错在把枚举池当成 C(8,4)=70。实际 `top` 是
     top_k **并上 B−1 个搭档帧**,最多 11 个元素,池上限 C(11,4)=330。
     按真实候选数分布精算后是每态中位 135 / 均值 171 / 最大 360。
  2. "9.9 小时" —— 错在**只数前向次数、没算每次前向的单价**。B=4 的 prompt
     含 4 张历史图 + 当前屏 ≈ 12800 视觉 token,B=1 只有 5120;实测 B=4
     单次前向 2.36s,比 B=1/B=2 混合时的 1.72s **慢 1.37×**
     (2.12 vs 2.91 前向/秒,5 卡)。
  **教训:成本模型必须同时建模"做多少次"和"每次多贵",后者随 B 变化。**
  中期结果见台账 §4 首条:预算翻倍对部署基线无用(+1.3pp,p=0.275),
  但上界从 +17.4 抬到 +28.0pp。
  归约:`rl/code/scripts/rl_bcurve_analyze.py`。监控已挂。
- **🔧 08-09 修正:B=2 由"复用"改为"重跑"。** 空跑归约时发现两个问题:
  ① `labels_all.jsonl` 里的 easy 态当初走了 `--skip-easy`,只有一行
  `mode:screen`、**没有 oracle** —— 于是计划里那 400 个 easy 态在交集里
  只剩 **5 个**,权重被放大到 **561.8×**。而 easy 层正是这条曲线区分
  "头寸"与"净收益"的唯一手段,丢了它整条曲线就只剩"大 B 头寸更大"这句
  同义反复。② 今天把单帧探针的 `top` 集合改成含搭档帧后,B=2 的子集池由
  C(8,2)=28 变成 C(9,2)=36,**与旧产物已不是同一口径**。
  现在整条曲线出自同一份代码。代价是多约 4 GPU-小时。
- **🔧 归约脚本加了分层下限守卫**(`--min-stratum`,默认 50):任一层态数
  不足就**拒绝输出重加权的总体数字**。用 5 个态乘以 561.8 倍去代表 2809 个,
  是数字幻觉不是估计 —— 宁可报错也不能打印出来。
- **不再扩卡**:h00 GPU0/5 虽只被邻居占 5.3/1.6 GB,但你明确要求腾出 hyper
  的卡给别的项目,不主动回收。要提速就等 tilde(C1)。

### A2 🔴 存活监控

- **为什么**:共享机上长跑必须能自愈 + 被发现。邻居 OOM、驱动抖动、
  别的会话重启容器都属常态。
- **完成判据**:随 A1 结束。
- **状态**:进度事件每 15 分钟一次;supervisor 重启环最多 5 次/阶段,
  逐 state 断点续跑(最大重放 1 个 state)。

---

## B. 就绪(可立即开工,不与 A1 抢卡)

### B1 🟡 selector 容量升级 —— **当前主线**

- **为什么**:欠拟合已被直接证据锁定 —— 在**训练过的** 421 个 winnable 态上
  (按构造上界 100%)learned 只到 **51.1%**。一张查表都能拿满分的地方模型
  连一半都没记住。加性打分 `s(子集)=Σ head(帧特征)` **结构上**表达不了帧间
  交互;"单帧边际 68.4% vs 集合边际 36.8%" 就是交互项存在的直接证据。
  不改架构而继续加数据/加训练量,是在治一个不存在的病。
- **具体改什么**:① `Linear(4096,1)` → 两层 MLP;② 加**成对交互项**
  (子集分不再是成员分之和);③ 特征由 mean 池化 → **mean+max 双池化**。
- **完成判据**:**先过前置门槛** —— 在训练过的 winnable 态上步级正确率
  ≥ 80%(现 51.1%)。**过不了就不评留出集**,直接判该架构仍欠拟合、
  换下一个。过了之后再报干净留出集上 learned vs recent-2 的配对检验。
- **阻塞**:无(训练只要 1 张卡;可等 A1 空出,或用 h00 GPU0/5 —— 那两张
  邻居只占 1.8/1.5 GB)。
- **成本**:特征缓存 1 卡 × 约 30 分钟(一次性),之后每档分钟级。
- **一条命令就绪**:`GPU=<id> bash /data/selector_capacity_ablation.sh`
  ([runscripts/selector_capacity_ablation.sh](../runscripts/selector_capacity_ablation.sh))。
  自动:缓存特征 → 六档消融 + 一档过拟合探针(B2)→ 汇总表。
  已有结果的档自动跳过,可断点续跑。lr 按参数量分档下调
  (linear 1e-3 / mlp 3e-4 / mlp_pair 1e-4)。
- **阻塞的真正原因**:5 张卡全在 B 曲线上(还需约 12 小时)。h00 的 0 号和
  5 号卡邻居只占 5.3/1.6 GB,跑这个绰绰有余,但我说过不擅自动 hyper 的卡 ——
  **要我用就说一声**,缓存 30 分钟、六档消融再半小时,今天就能有结果。
- **代码已就绪(08-09)**:`causalcache_rl/subset_scorer.py`(三档 + 部署侧
  `score_subsets` 枚举选择规则)、`index_features` 支持 `mean_max` 与
  `return_context`、训练器加 `--head-arch/--pooling/--use-context` 与
  **训练集排序准确率静态复测**。四档配置各跑过 24 态冒烟,端到端通。
  **冒烟不构成容量结论** —— 只有 3 个优化步,四档的 train_rank_acc
  (0.75 / 0.79 / 0.75 / 0.83)全在初始化噪声里,不可解读为架构差异。
- **⚠️ 开跑前先调 lr**:mlp_pair 有 1310 万参数(v4 是 4097),冒烟里
  `mean_pair_loss` 在 1e-3 下从 3.2 涨到 12.7。沿用 v4 的 1e-3 大概率发散,
  按参数量往下调(先试 1e-4)并把前几步的损失曲线当门槛看。
- **⚠️ 部署侧要跟着改**:有成对项后 `top-B argmax` 不再等价于最优子集,
  serve 的选择规则必须换成 `score_subsets`(在缓存特征上枚举 C(n,B),
  B=2、n≤30 时最多 435 次点积,可忽略)。bundle 里已落 `selection_rule`
  字段供部署侧读取。**这一步漏了就是 v3"训 A 评 B"错误的翻版。**

### B2 🟡 特征信息量探针

- **为什么**:B1 若也拟合不上,病因就不在头而在**特征** —— 144 token 缩略图
  + 均值池化可能已经把判别信息丢了,那要动的是索引遍(提分辨率 / 换池化 /
  取多层),而不是继续换头。**这个探针能把"头太弱"和"特征没信息"分开**,
  不做就会在换头上反复空转。
- **完成判据**:缓存 winnable 态的帧特征,在**训练集**上拟合一个强模型,
  报训练集子集排序准确率。≥0.9 → 特征够用,是头的问题;
  ≈0.6 → 特征本身没信息,转去改索引遍(分辨率 / 池化 / 取多层)。
- **🔧 不需要写新脚本**:这个探针就是 B1 训练器的一组配置 —— 探针只问
  "**有没有任何模型能拟合训练集**",不关心泛化。所以:
  `--feature-cache <cache.pt> --head-arch mlp_pair --use-context
   --pooling mean_max --hidden 2048 --epochs 20 --holdout-frac 0.05
   --learning-rate 1e-4`,然后只读 `report.json` 的 `train_rank_acc`
  (训练后静态复测,不是滑动平均)。**故意让它过拟合** —— 过拟合得起来
  才说明特征里有信息。
- **阻塞**:需要先把特征缓存落盘(`cache_index_features.py`,1 卡约 30 分钟,
  与 B1 共用同一份产物)。
- **成本**:缓存 1 卡 × 约 30 分钟;之后每次拟合纯 CPU / 单卡分钟级。

### B3 🟡 v4 评测集按训练器划分重采样

- **为什么**:现在的"干净口径"是**事后剔除**训练态得来的,样本从 1053 掉到
  632,而且剩下的 632 里 531 个是 hopeless,信噪比很差(留出 winnable 只剩
  **101 个**,任何结论都缺功效)。要下定论必须**按训练器的真实划分**
  (`sorted(lab)` + `random.Random(20260809)` 洗牌,前 20% 为 holdout)
  重新采一批留出态来评。
- **完成判据**:留出 winnable ≥ 400 态上的 learned vs recent-2 配对检验。
- **阻塞**:留出池总共只有 252 个 winnable 态 —— **不够 400**。
  所以必须先扩枚举标签(见 C3),或改用交叉验证(k 折重训)。
  **这条要先定方案再开工。**
- **成本**:视方案定。

---

## C. 阻塞中

### C1 ⚪ tilde 补投(把 B 曲线样本从 1000 扩回 3294)

- **为什么**:1000 态是被算力逼出来的妥协(见台账 §8.6:批处理零收益,
  单次前向 1.75s 不动)。样本越大,B 曲线各点的置信区间越窄。
- **完成判据**:tilde 8 卡跑完 `bcurve_ids_full.txt` 与 `bcurve_ids.txt`
  的**差集**,与 hyper 的产出合并归约。
- **阻塞**:tilde 账号 `zhen` 的 GRES 硬顶 8 卡,已被同账号
  `s2s_omni_v4/lrfix_train`(1 天时限,08-09 06:39 起)占满。
  作业 134033 已撤,避免与 hyper 分片重叠。
- **成本**:8 卡 × 约 4 小时。

### C2 ⚪ v4/后续架构的闭环快测

- **为什么**:你定过规矩"离线涨了必须早验闭环"。
- **为什么现在不做**:干净口径下**离线增益本身不存在**(+0.5pp,p=0.736)。
  拿一个不存在的增益去测迁移,只会烧掉一整轮 GPU 换回必然的"无差异"。
- **完成判据**:OSWorld 双臂配对(selector vs recent-B),同主机同轮次。
- **阻塞**:等 B1 拿到**统计上确凿**的离线增益。
- **成本**:约 4–6 卡 × 数小时。

### C3 ⚪ 扩枚举标签(winnable 池从 1263 扩大)

- **为什么**:winnable 只有 1263 态、留出仅 252,B3 的功效不够。
  扩标签同时利好训练量与评测功效。
- **完成判据**:winnable ≥ 3000 态。
- **阻塞**:ubuntu 分片已枚举 99.4%(5303/5335),**要扩只能上 winmac 分片**
  (存在但未处理,需要先拉帧集)。
- **成本**:大。上 tilde 或等 hyper 空。

---

## D. 待你裁决

### D1 🟣 容差 25 → 15?

- **数据**:预测-gold 距离分布**确实双峰** —— 近端 0–10 每单位密度 2–3
  (点在同一 UI 元素上),≥25 回升到 0.44–0.49(点错元素),
  **谷底在 [15,25),密度 0.35**。
- **实测(412 态,台账 8.8 二次订正)**:oracle 头寸在容差 5–40 之间
  **大体持平**(+19.5 / +18.0 / +17.0 / +16.2 / +16.3pp),15 与 25 只差
  **0.8pp**。我先前基于 40 态说的"头寸随容差单调收缩、15 对我们有利"
  是噪声,已撤回。
- **推荐**:**默认沿用 25**(保守),**主表报整条容差曲线**。
  换容差不花 GPU,两个都报即可 —— **这条不需要你拍板**,除非你不同意
  "默认取更保守的那个"。
- **附带结论(回答你此前问的"能不能固定 gold 不准偏移")**:不能。
  容差 0 时四臂全塌到 12.5%,**oracle 与 recent 完全相等、头寸为 0**,
  严格匹配下这个实验测不出任何东西。容差是让指标具备分辨力的必要条件。
- **🔧 08-09:这个决定已经不再有时间压力。** 枚举现在也落盘原始预测
  (`all[].p`、`recent_s`/`random_s`、`all[].e` 标记 oracle 池、`gold`),
  `rl_bcurve_analyze.py --tolerance X` 可直接重算四臂,不必碰 GPU。
  代价是重跑了已完成的 B=1(约 1.3 GPU-小时)。
  换来的不只是"改容差免费",还有 **"B 的选择随容差怎么变"** 这条稳健性
  曲线 —— 审稿人问"你的预算结论是不是被那个魔法数字撑起来的",这是唯一
  能一次性堵住的答法。

### D2 🟣 "全历史低清"基线要不要做?

- **为什么**:审稿人必问"既然上下文放得下缩略图,为什么不把**所有**历史帧
  低清全喂给 policy,还要 selector 干什么?"。没有这一栏,方法的存在理由
  站不住。
- **完成判据**:同一批态上多一条臂 —— 所有候选帧按索引遍分辨率(144 token)
  全部进 prompt,报步级正确率。
- **成本**:1 卡 × 约 2 小时(每态 1 次前向)。
- **我的推荐**:做,而且**放在 B1 之前** —— 如果全历史低清就已经打平甚至
  超过 oracle-B2,那 selector 这条线的动机需要重写,越早知道越好。

### D3 ✅ RL 闭环训练 —— **已判作废**(2026-08-09,用户裁定"RL 只是手段不是目的")

见 §G「路线顺序与 RL 准入门槛」。基础设施保留,不再作为活跃路线。

---

## G. 路线顺序与 RL 准入门槛(2026-08-09 定)

**顺序**:B 曲线 → selector 结构 → HGKV 对照 → 2×2 交互检验 → 才轮到 RL。
每一步都是**后一步的前提**,不是并列的待办。

### RL 为什么被判作废

它当初的两条立论,现在各自的状态:

| 立论 | 状态 |
|---|---|
| "选帧离散不可导,需要 score-function 估计" | **失效**。枚举给出精确标签,监督排序条件数好得多,而且**远未用尽** —— v4 连训练集都拟合不上(见过的 421 态上界 100%、只做对 51.1%) |
| "selector 与 HGKV 相互影响,需联合优化" | **预设了 HGKV 成立**,而这正是无证据的部分 |

再加 v3 的判据:参数动得很凶(位移 1.557 / 750 优化步)成绩纹丝不动 ——
**瓶颈在表达力不在优化算法**,而 RL 换的正是优化算法。在一个还没学会拟合
标签的头上加 RL,是优化一个没东西可优化的模型。

### RL 重新有资格进来的门槛(满足其一)

1. **监督选帧已饱和**(拟合得上训练集且泛化),但闭环成绩仍显著低于离线
   预测 —— 说明存在只有闭环奖励能修的训练/部署错配;
2. **selector 与策略适配器被证明存在交互**(各自单独有效,组合起来非加性)
   —— 这个用 2×2 离线消融就能测,**不需要 RL**。

两条都不满足时,监督 + 离线标签在成本与条件数上都完胜。

### 「策略读不懂非 recent 帧」这个担心,现有数据的回答

**基本可以排除,但有一个明确上界。** 困难态上 B=2 的 oracle 52.8% vs
recent 24.8% —— 若策略读不懂非最近帧,oracle(选出来的多半不是最近两帧)
应当 ≈ recent,而不是两倍多。**冻结策略确实能用上非最近帧。**

上界:困难态里 **47.2% 是 hopeless**(枚举证明无任何子集可解)。其中多少是
"策略消化不了"、多少是"信息真的不在历史图里",**现有数据分不开**。
而这恰恰是策略适配(HGKV / SFT)唯一可能有价值的地方 —— 选帧在 hopeless
态上按定义无能为力。下条实验就打在这里。

### H1 🟡 HGKV vs 全层 SFT 对照(排队中,等 B1 让出卡)

- **为什么**:HGKV(末 8 层 k/v LoRA,rank 8,65.5 万参数)当初是按
  "便宜 + 像记忆"选的,**从未与全层 SFT 比过**。用户的质疑成立:
  没有这个对照,"为什么是 HGKV 而不是全参微调"在论文里答不出来。
- **为什么打在 hopeless 分层**:那里选帧的贡献**按定义恒为零**,
  信号干净 —— 任何提升都只能来自策略适配,不会与 selector 混淆。
- **🔧 08-09 复核:基础设施已存在,基本是配置工作。**
  `code/scripts/train_success_sft_lora.py` 已支持三种 adapter(`adapter_settings`),
  并有现成配置 `causalcache_desktop_did_{hgkv_v4,full_lora_v3}.json`。
  **而且它带了一个我原设计里缺的对照臂**:

  | adapter_type | 是什么 | 隔离什么 |
  |---|---|---|
  | 冻结 | 0 参数 | 基线 |
  | `history_gated_kv` | 末 N 层 k/v + **restored-history 门控** | 现有主张 |
  | **`ungated_kv_lora`** | **同层位、同 rank,无门控** | **门控机制本身值不值** |
  | `full_policy_lora` | 全层 q/k/v/o | 层位与容量够不够 |
  | (可选)全参 SFT | 80 亿 | 容量上限 |

  **`ungated_kv_lora` 是关键**:它与 HGKV 同层位同 rank、只差门控。
  没有它,"HGKV 有效"里"位置选对了"和"门控起作用了"两件事混在一起,
  答不出"为什么非要门控" —— 而那正是审稿人会问的。

- **⚠️ 必须并排 B=0 对照臂(今天的发现加的硬约束)**:在 hopeless 态上做
  gold 监督,提升可能全部来自**任务 SFT** 而与记忆无关。因此评测要做
  **{冻结, HGKV, ungated, full} × {B=0, recent-2}** 的 2×N:
  若某臂在 B=0 与 recent-2 上提升相同 → 那是任务 SFT;
  只有 recent-2 上的提升**显著大于** B=0 上的,才是"适配让历史变得可读"。
- **⚠️ 目标函数要改**:现有配置跑的是 DiD group loss,#26 要的是
  hopeless 态上的 gold action 监督。adapter 机制可复用,**目标与数据管线要新写**。
- **完成判据**:留出 hopeless 态上,各臂 (recent-2 提升 − B=0 提升) 的配对检验。
  HGKV ≈ 全参 SFT 而参数少两个数量级 → 卖点成立;
  HGKV ≈ ungated → 门控无用,该简化;全参远超 → HGKV 前提死掉。
- **数据量**:hopeless 共 1290 态(约 1030 训 / 260 留出)—— **偏小**,
  LoRA 容易记住而非学会,留出集数字是唯一可信的。
- **成本**:前三臂各 1 卡约 3 小时(含 2×N 评测);全参 SFT 要多卡,等 B 曲线让位。

## E. 已知缺陷(暂不修,但报告时必须披露)

| 缺陷 | 偏差方向 | 为什么暂不修 |
|---|---|---|
| 枚举用剪枝(单帧 top-8 → 组合),非全枚举 | **低估 oracle**(命中率 95.7%,漏约 4.3%) | 全枚举 10 分钟/态,不可承受 |
| 候选 >30 的 state 直接跳过 | **低估长历史场景** —— 而长历史正是"旧帧有用"最可能出现的地方,**对我们不利** | 成本 |
| 缺图的 state 整态跳过(约 11%) | 无方向性(缺图与内容无关),但样本少 11% | 只丢部分候选会让候选池不完整,反而低估 oracle |
| 捕获率跨样本计算(评测无 oracle 臂) | 口径缺陷,不是偏差 | oracle 需枚举,太贵。B 曲线跑完后可在同一批态上闭合 |
| 结论只覆盖 ubuntu 桌面 | 外部效度 | winmac 帧集未处理 |

---

## F. 已完成

- ✅ **枚举 oracle 全量**(5303 唯一态,覆盖 99.4%):困难态分层
  oracle 49.1% / recent-2 28.4% / 随机-2 22.9%。上界标尺与监督标签都来自这里。
- ✅ **v4 复检**:定位训练集污染(评测 1053 态里 421 个训练过)+ 欠拟合。
- ✅ **容差敏感性扫描**:双峰、谷底 [15,25)。
- ✅ **批处理评估**:实测零收益(瓶颈在视觉编码+prefill),`--batch-size`
  保留但默认 1,改用缩样本控成本。
- ❌ **加训练量 / 加数据**:被欠拟合证据划掉 —— 数据不够不会让**训练集本身**
  拟合失败。
