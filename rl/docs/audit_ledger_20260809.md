# CausalCache 记忆选择线 实验台账(截至 2026-08-09)

> 本文档按 `experiment-audit` skill 维护。目的不是汇报进度,而是让**不在场
> 的人重建上下文**,并能挑出"我替你做了哪些没说的决定"。

## 0. 一句话现状

**v4 那个"第一个正向结果"是训练集污染造成的,已撤回。** 清掉训练过的
dp_id 后,learned 相对 recent-2 只剩 **+0.5pp(p=0.736)**;在真正留出的
winnable 态上甚至是 **−6.9pp(41.6% vs 48.5%,n=101,p=0.167)**。
唯一稳住的结论是 learned 显著优于随机初始化头(**+4.0pp,p=0.003**)——
训练确实在学东西,但学到的量不足以超过 recent-2。

**病因已定位到"欠拟合",不是"过拟合"也不是"数据不够"**:在**训练过的**
421 个 winnable 态上(按构造上界 100%),learned 只到 **51.1%**。一张查表
都能拿 100% 的地方,模型连一半都没记住 → 加性线性头 + 均值池化特征
**表达不了标签函数**。这条把"再喂数据/再训久点"从候选里划掉了,
下一步应当是**换架构**(MLP / 成对交互项 / 双池化)。

**正在跑**:B 曲线重测(用户指定优先),1000 态 × B∈{1,4},h00 两卡 +
h01 三卡,ETA 约 6–7 小时;B=2 复用已有全量枚举结果不重跑。

**⚠️ 口头汇报变更史(以本文档为准)**:205 态时我说 learned 36.1% /
recent 32.2%(+3.9pp);扩到 838 态收窄到 +1.8pp;**现在剔除污染后是
+0.5pp 且不显著**。捕获率 23% → 9% → **约 1%,不可区分于 0**。

## 1. 三十秒背景

**问题**:GUI agent 每步只能带 B=2 张历史截图进 prompt(上下文有限)。
部署默认取"最近 2 张"。问:**换一种选法能不能更好?**

**为什么值得答**:如果不能,CausalCache 的核心主张(固定预算内重分配)
就不成立;如果能但学不出来,那是学习问题;两者的应对完全不同。

**成功长什么样**:学出来的 selector 在**部署口径**下显著优于 recent-2。

## 2. 当前生效的假设与口径 ← 请重点审这一节

| 假设/口径 | 取值 | 谁定的 | 依据 | 若错会怎样 |
|---|---|---|---|---|
| 记忆预算 B | 2 | **用户裁定** | oracle 空间是 B=4 的 2.8 倍 | — |
| 训练搬到离线视频 | OSWorld 只做评测 | **用户裁定** | 闭环 RL 太慢、奖励太稀疏 | — |
| 数据源 | AgentNet/OpenCUA **ubuntu**(6003 态) | **我选的** | GUI-Odyssey 已被判 NO-GO("recent 天然更强、目标 case 稀少"),用它等于自发烂牌;winmac 分片存在但未用 | 若 ubuntu 子集有特殊性,结论不能推广到全桌面 |
| **步级正确率定义** | 动作类型匹配 **且** 坐标 L2 ≤ **25**(归一 [0,999] 空间) | **我选的**(沿用语料里已有的 `coordinate_tolerance=25`) | 语料构造时就是这个容差,保持一致 | 容差过松会高估所有臂;过紧会把"点在按钮内但不居中"判错 |
| 评测只在困难态上做 | 先跑 B=0,已对则跳过 | **我选的** | 选帧在 easy 态无杠杆,混进去会稀释信号 | 若 easy 态上"加图反而变坏",这个筛选会掩盖该风险 |
| 评测/训练切分 | 评测只用 manifest 第 4000 行之后 | **我选的** | 训练用了前 3000 行,防污染 | 若 manifest 有顺序性(如按任务聚集),切分即分布偏移 |
| v3 训练奖励 | teacher-forced `log p(gold action)` | **我选的** | 稠密、免生成、组内比较可抵消容差 | **已被证伪**,见台账 v3 |
| v4 训练目标 | 子集级成对排序(正=能做对的子集) | **我选的** | 与评测指标同源 | — |
| selector lr / LoRA lr | 1e-3 / 1e-4 | **我选的** | 离线反事实扫描:1e-4 位移仅 0.049,1e-3 达 0.589,≥1e-2 失控 | 过大则失控,过小则不动(v1 的病因) |
| 显著性口径 | 双侧精确符号检验 | **我选的** | 问"有没有差异",不预设方向 | 单侧会让 p 值减半,更容易误判为有效 |
| 架构 | 索引遍(144 token 缩略图)+ Linear(4096,1) 打分头 | 沿用历史(v2 起) | 选择是场景级的,缩略图够用 | 若关键信息在高分辨率细节里,低清索引遍会看不见 |
| HGKV LoRA | **v4 中未训练**,只训 selector | **我选的** | 先单独验证选帧是否有效,避免两个变量同时变 | 联合训练的上限未知,可能被低估 |

## 3. 已剪枝与未做的事 ← 第二重点

| 被剪掉的 | 方式 | 为什么 | 偏差方向 |
|---|---|---|---|
| 枚举不做全组合 | 单帧 top-8 → 配对(53 次/态 vs 300 次) | 全枚举 10 分钟/态,不可承受 | **低估 oracle**(校验命中率 95.7%,即漏掉约 4.3% 的最优组合) |
| easy 态不枚举 | `--skip-easy`,B=0 已对则只记一行 | 头寸只存在于 B=0 出错的态 | 无偏(那些态本就无杠杆),但**它们的 oracle 状态未知**,不能并入四臂统计 |
| 候选 >30 的 state | 直接跳过并计数 | 成本控制 | **低估长历史场景**——而长历史恰是"旧帧有用"最可能出现的地方,这是**对我们不利**的剪枝 |
| 缺图的 state | 整态跳过(约 11%,manifest 6003 → 可用 5335) | 只丢部分候选会让候选池不完整,从而低估 oracle | 无方向性偏差(缺图与内容无关),但样本量少了 11% |
| 评测无 oracle 臂 | 评测四臂不含 oracle | oracle 需枚举,太贵 | **捕获率必须跨样本计算**(评测样本的 learned/recent + 枚举样本的 oracle),这是一个真实的口径缺陷 |
| **闭环检验尚未做** | v4 只有离线步级结果 | 时间 | **最大的未验项**。用户已明确要求"离线涨了必须早验闭环",这条**尚未兑现** |
| winmac 数据 | 未使用 | 时间 | 结论目前只覆盖 ubuntu 桌面 |
| v2 未跑满 20 迭代 | iter-6 主动终止 | 用户裁定转向离线 | 非失败终止,权重保留可恢复 |
| trainer DDP | 未做,单卡 | 改坏过一次,且有 all-reduce 死锁风险,无人看管时不冒险 | 只影响速度不影响结论 |

## 4. 实验台账(时间倒序)

### B 曲线重测 —— 在跑(2026-08-09)

- **假设**:B=2 这个工作点的选型依据已经不可靠,在新指标(人标 gold +
  步级正确率)下重测,B=2 未必仍是头寸最大的预算。
- **为什么现在做**(用户指定优先):旧依据有两处硬伤。① 那组数字
  (oracle 相对 recent 的空间 B=2 是 +0.102、B=4 只剩 +0.037,出自
  `indomain_gap_v1/ROOT_CAUSE.md`)来自 teacher-forced log-prob DiD 指标
  + target-recurrence 启发式筛候选 —— **正是用户此前质疑、后来被 v3 证伪的
  那套口径**。② `frozen_main_row_v1/README.md` 记录 B2→B4 在真实任务成功率
  上还有 **+4.6pt**,意味着 B=2 是一个比 B=4 低 4.6pt 的工作点,审稿人可以
  直接问"为什么不在 B=4 上比?那里你的头寸小 2.8 倍"。
- **设计**(三处是为了不自欺):
  1. **配对**:三个 B 打在同一批 dp_id 上(`--only-dp-ids`)。不配对的话
     曲线的升降会混进样本差异。B=2 复用 `labels_all.jsonl` 已有全量枚举。
  2. **easy 态必须进样本**:easy 是按 B=0 定义的,B 变大后多出来的历史图
     完全可能把本来答对的题干扰错。净收益 = 大 B 修好的 winnable − 大 B
     弄坏的 easy。只测非 easy 会**系统性偏向大 B**。抽 400 easy + 600 困难,
     归约时按真实占比(2809 : 2494)重加权还原。
  3. **有效预算 = min(B, 候选数)**:旧写法"候选 < B 就整态丢弃"会让大 B
     悄悄剔除短轨迹、换掉样本集合。实测只有 0.2% 的态受影响。
- **可检验的只有一个对照**:oracle 与 recent/随机 的差是**结构性非负**
  (recent-B 与 随机-B 都是被枚举的子集之一),只能读作"上界有多高"。
  真正能做检验的是 **recent-B 跨预算的配对差** —— 那是两个真实可部署配置
  在同一批态上的对照。
- **改动**:`rl_oracle_enumerate.py` 加 `--only-dp-ids`/有效预算/
  `--batch-size`;单帧探针改用 **B−1 张最近帧**当搭档(旧版硬编码两图
  prompt,B=4 时探针 2 图、目标 4 图,形状不一致会让排序失真);
  新增 `rl_bcurve_analyze.py`、`tilde_bcurve.sbatch`、`bcurve_hyper.sh`。
- **结果**:待出。
- **去留**:待定。

### v4 复检 —— 训练集污染 + 欠拟合(2026-08-09,**推翻前一条结论**)

- **假设**:v4 的 +1.8pp 若是真的,应当在剔除训练过的 state 后依然存在;
  且若模型确实学会了选帧,它在**训练过的** state 上应当接近上界。
- **现象**:
  - 复现训练器的真实划分(`sorted(lab)` 后 `random.Random(20260809)` 洗牌,
    前 20% 为 holdout;复现得 winnable 1263 / train 1011 / holdout 252,
    与训练日志逐字一致)后发现:**评测的 1053 态里有 421 个是训练过的**。
  - 污染口径(n=1053):learned 29.5% / recent 28.4% = **+1.1pp,p=0.338**。
  - 干净口径(n=632,剔除训练过的):learned 15.2% / recent 14.7% =
    **+0.5pp,p=0.736**;learned − init = **+4.0pp,p=0.003**。
  - **训练过的 winnable(n=421,上界 100%)**:learned **51.1%** /
    recent 48.9%(+2.1pp,p=0.417)。
  - **留出的 winnable(n=101,上界 100%)**:learned **41.6%** /
    recent 48.5%(**−6.9pp**,翻正 6、翻负 13,p=0.167)。
- **判断**:
  - **已证实(污染)**:此前的 838 态头条数字分母里混了训练态。我原以为
    `--skip-first` 按 manifest 行号切就能防污染 —— 但训练器是对 winnable 态
    **按 seed 随机 80/20 切**的,两种切法毫无对应关系。这是我的口径错误。
  - **已证实(欠拟合)**:在见过的 421 态上、上界 100% 的条件下只到 51.1%。
    **不是过拟合**(过拟合的特征是训练集接近满分而留出集塌陷),
    **也不是数据不够**(数据不够不会让训练集本身拟合失败)。
    加性子集打分 `s(子集)=Σ head(帧特征)` 结构上无法表达帧间交互;
    此前观察到的"单帧边际 68.4% vs 集合边际 36.8%"正是交互项存在的直接证据。
  - **未证实**:留出 winnable 上的 −6.9pp 是不是真的负向(n=101,p=0.167)。
    只能说**没有证据说明 v4 优于 recent-2**。
- **改动**:诊断脚本落 `rl/code/scripts/`(见提交);台账 §0、§5、§6 改写。
- **结果**:v4 从"第一个正向结果"降级为"**训练有效(vs init 显著)但不足以
  超过部署基线,且受容量瓶颈限制**"。
- **去留**:**架构保留、结论撤回**。下一步不再加数据/加训练量,改做容量升级
  (MLP 头、成对交互项、mean+max 双池化),并把"训练集拟合度"作为**前置门槛**:
  拟合不上训练集就不必再评留出集。

### v4 —— 监督排序(2026-08-09)**⚠️ 本条结论已被上一条推翻,保留作记录**

- **假设**:v3 失败是因为"训 `log p` 评 `argmax`"两者脱钩;若换成与评测
  同源的目标(把能做对的子集排前面),应能学到有用的选帧。
- **现象**:训练 2022 态 / 252 优化步 / 16176 成对样本,成对损失
  0.791→0.716,留出集排序准确率 **0.5828**(0.5=随机)。
  同口径评测 838 个困难态:learned **32.6%** / init **20.3%** /
  recent-2 **30.8%** / 随机-2 **24.8%**。
  learned vs init **+103,155 胜 52 负,p=0.000**;
  learned vs recent **+15,64 胜 49 负,p=0.188**。
  选帧重合:learned==init 仅 5%,learned==recent 26%。
- **判断**:**已证实** —— 目标函数换掉后确实学到了可迁移能力(vs init 的
  p=0.000 和留出集 0.583 是两个独立证据)。**同时证实** v3 的病因诊断:
  同架构、同初始头、同数据,只换目标,20.3%→32.6%。
  **未证实** —— 是否真的优于 recent-2(p=0.188)。
- **改动**:新增 `rl/code/scripts/train_selector_supervised.py`(a31577a);
  评测脚本加分片以扩样本(7476323)。
- **结果**:见上。捕获率 ≈ (32.6−30.8)/(49.1−30.8) ≈ **9%**
  ⚠️ 跨样本计算,见 §3 缺陷。
- **去留**:**保留**。这是目前唯一学到东西的配置。

### v3 —— 离线步级 RL(2026-08-08)

- **假设**:把奖励从"整条 episode 成败"换成"每步的 `log p(gold action)`",
  信用分配从 670:1 降到 2:1,应能学出有用的 selector。
- **现象**:3000 态、**750 优化步**、位移 **1.557**、熵 3.94→2.32 ——
  优化机制完全奏效。但评测 200 困难态:learned **21.5%** vs init 23.0%
  (p=0.701,**无收益**)vs recent 32.5%(p=0.002,**显著更差**)。
  **learned 与 init 选帧仅 13% 重合** —— 行为大改,成绩不变。
- **判断**:**已证实"训动了但没学到"**(与 v1 的"没训动"是不同病因)。
  **推测(后被 v4 证实)**:`log p` 可以大涨而 argmax 一次不翻,
  连续目标与离散指标脱钩。
- **改动**:`train_step_level.py`(score-function 绕过选帧离散 + HGKV 可导
  SFT);新增同口径判据脚本 `rl_selector_eval.py`。
- **结果**:见上。
- **去留**:**回退**目标函数,保留训练框架(v4 复用了它的索引遍与架构)。

### 枚举 oracle —— 冻结 policy + 只换帧(2026-08-08~09)

- **假设**:冻结 policy 不动,只把历史帧换成最优的,步级正确率能提多少?
  这是"选择本身值多少"的干净度量,也是所有方法的上界标尺。
- **现象**:5303 个唯一态(可用 5335,覆盖 99.4%),三方并行(h00 3 卡 /
  h01 2 卡 / tilde 8 卡)。**困难态分层(2547 态)**:
  oracle **49.1%** / recent-2 **28.4%** / 随机-2 **22.9%**。
  全体分层:easy 53.0% / **winnable 22.7%(1204 态)** / hopeless 24.3%。
  winnable 内正确子集占比中位 37.9%;承载帧分析显示正确子集**集中**在
  少数关键帧(最强帧携带率比基准高 +33.4pp)。
- **判断**:**已证实头寸真实存在**(+20.7pp,n=2547)。
  **已证伪**归档里那条 AndroidWorld 警示("喂正确图 vs 打乱图差异≈0")
  在桌面数据上不复现 —— 帧内容确实影响动作。
  公平对照 recent−随机 = **+5.6pp**(p 由 n=2547 支撑):
  "选最近两张"只比随便选好一点点。
- **改动**:`rl_oracle_enumerate.py` + `rl_oracle_analyze.py`;
  契约 `oracle_selection_contract.md`(74e8af8 起)。
- **结果**:产出 1204 个 winnable 态作为 v4 的监督训练集。
- **去留**:**保留**,作为所有后续方法的分母。

### v2 —— OSWorld 闭环 RL,修 lr(2026-08-08)

- **假设**:v1 失败是因为学习率过小导致参数根本没更新。
- **现象**:selector lr 1e-4→1e-3、LoRA lr 1e-5→1e-4、grad_accum 8→4 后,
  **iter-1 单迭代位移 0.104**(v1 二十迭代累计才 0.044),
  iter-2 累计位移 0.247、与初始头选择重合率 0.141。
- **判断**:**已证实** lr 是 v1 的病因。
- **改动**:超参 env 化 + 前 3 迭代硬门槛(`selector_drift ≥0.02`)。
- **结果**:门槛通过(0.104/0.242/0.141),iter-6 时用户裁定转向离线,
  **主动终止(非失败)**,权重保留在 h00 `/data04/jaxan/rl2/`。
- **去留**:**归档**。它已完成"证明 lr 修复有效"的使命。

### v1 —— OSWorld 闭环 RL 首版(2026-08-05~08)

- **假设**:用任务成败做奖励、GRPO 联训 selector + HGKV 能提升闭环成功率。
- **现象**:20 迭代、936 episode、双轮 held-out 评测
  RL 81/240 vs recent-B 88/240(Δ=−2.9pp,p=0.345,CI 跨零)。
  **复盘发现两条通道都没训动**:selector 位移 4.4%、与随机初始化头选择
  重合 86.7%;LoRA 的 `lora_b` 范数 0.0399(vs `lora_a` 6.5),
  ΔW 约 0.1% 量级。**总优化步数仅 67 次。**
- **判断**:**已证实为配置失败而非假设失败**。
- **改动**:trainer 加 `selector_drift` / `optimizer_steps` 仪表 —— v1 的
  教训就是这两个数不可见,导致 20 迭代后才靠离线挖掘发现问题。
- **结果**:见上。
- **去留**:**作废**,预注册判据一并作废(它检验的配置不可能产生更新)。

## 5. 数字总表

**困难态(B=0 出错)步级正确率。⚠️ 各列样本不同,不可逐格直接相减:**

| 臂 | 枚举(n=2547) | v3 评测(n=200) | v4 污染口径(n=1053) | **v4 干净口径(n=632)** |
|---|---|---|---|---|
| oracle-最优2 | **49.1%** | — | — | — |
| learned | — | 21.5% | 29.5% | **15.2%** |
| init | — | 23.0% | 19.8% | 11.2% |
| recent-2 | 28.4% | 32.5% | 28.4% | **14.7%** |
| 随机-2 | 22.9% | 26.5% | 23.3% | 13.8% |

干净口径的绝对值大幅下降(29.5%→15.2%),**不是变差了**:剔除的 421 态
全是 winnable(有解),剩下的 632 态里 531 个是 hopeless(枚举证明无解),
所以四臂一起被拉低。要看能力必须切到 winnable 层:

| 层 | n | learned | recent-2 | init | 上界 |
|---|---|---|---|---|---|
| **训练过的 winnable** | 421 | **51.1%** | 48.9% | — | 100%(按构造) |
| **留出的 winnable** | 101 | **41.6%** | 48.5% | — | 100%(按构造) |

**这张表是欠拟合的直接证据**:见过的题上界 100%、只做对 51.1%。

recent-2 在各段样本上是 28.4 / 32.5 / 28.4 —— **这个 ±2pp 的浮动就是
样本间差异的量级**,任何小于它的"提升"都不可信。

**容差敏感性(300 态,四臂,归一 [0,999] 空间)**:

| 容差 | 0 | 2 | 5 | 10 | 15 | 25 | 40 | 60 |
|---|---|---|---|---|---|---|---|---|
| learned | 7.7 | 10.0 | 15.7 | 21.0 | 22.3 | 24.3 | 27.3 | 32.3 |
| recent | 9.0 | 11.3 | 16.0 | 20.3 | 21.7 | 23.7 | 28.7 | 34.0 |
| learned−recent | −1.3 | −1.3 | −0.3 | +0.7 | +0.7 | +0.7 | −1.3 | −1.7 |

**结论不随容差翻转**(任何容差下 learned 都不显著优于 recent),
所以容差不是当前结论的软肋。但**距离分布确实是双峰的**,谷底给出了
一个有原则的阈值,见 §8.5。

**测量噪声(独立标定)**:同一冻结基线跨评测重测,逐任务翻转 11.7%、
总数 ±4 任务/120;枚举里 GPU 数值非确定性造成的翻转约 2.1%。

## 6. 我犯过的错与纠正

| # | 错误 | 如何发现 | 纠正后 |
|---|---|---|---|
| 1 | 把"离线增益不迁移闭环"当既有结论反复引用 | **用户指出**离线增益本身极小 | 实测模型离线只涨 0.0066(头寸 0.1019 的 6.5%),不足以改变动作;正确表述是"从未做出足够大的离线增益,迁移命题未被检验" |
| 2 | 把 `mean_kl ≈ 0.2` 解读为"信任域按设计工作" | 复盘查 adapter 时发现 `lora_b` 范数仅 0.04 | 那是**每 episode 全 token 之和**,折合每 token ~1e-3 nats,真实含义是"策略几乎没被改动" |
| 3 | 报告方向读 p≈0.12 | 写分析脚本时算出双侧值 | 那是**单侧**;双侧 p=0.238,更不显著 |
| 4 | 用 oracle−随机 当"内容敏感性"证据 | 自查时发现随机臂本身在枚举空间内 | **结构性非负,不可检验**;有效证据改用 winnable 内子集分化率与承载帧 lift |
| 5 | 分析脚本一度让被筛掉的 easy 态默认"四臂都对" | 写完立即自查 | **这是编造数据**(B=0 对不蕴含 recent-2 对),当场撤回改为"oracle 未知、不进统计" |
| 6 | 92 态时报"四臂打平" | 扩到 200 态后结论变化 | recent-2 **显著优于** learned(p=0.002),不是打平 |
| 7 | 报 v4 捕获率 23% | 扩样本到 838 态 | 差距从 3.9pp 收窄到 1.8pp,捕获率约 **9%**,且不显著 |
| 8 | 枚举候选过滤 off-by-one(`steps[j-1]` 应为 `steps[j]`) | 跑批崩溃 | 丢弃已跑的 4 态重来(候选池可能不完整会低估头寸) |
| 9 | iter-7 成功数记成 24(实为 13) | 写战报时重算 | 24 是训练 episode 数不是成功数 |
| 10 | 分片编号冲突(h00/h01 都拿了分片 3)、`--shard-index 4 --shard-count 4` 空转 | 核对产出时发现 | 统一 9 分片;tilde 用 18 分片整除对齐 |
| 11 | 改完脚本只从远端拷回、忘了推上去 | 作业报 unrecognized arguments | 跨机改代码必须确认远端也变了 |
| 12 | **v4 评测集混入 421 个训练过的 state** | 想做"训练集拟合度"诊断时,复现训练器划分才发现两套切分不对应 | 我以为 `--skip-first`(按 manifest 行号切)能防污染,但训练器是对 winnable 态**按 seed 随机 80/20 切**的。干净口径下 v4 相对 recent 只剩 +0.5pp(p=0.736),**"第一个正向结果"这个说法撤回** |
| 13 | 连续三次把 v4 的优势报高(3.9pp → 1.8pp → 0.5pp) | 每次扩样本/换口径都缩水 | 根因是我在样本量不足和口径未固化时就下了定性结论。**改为:先固化口径与分母,再报数** |
| 14 | 判断"批处理能提速 8-10 倍",据此改了代码 | 实测批 16 与批 1 吞吐持平(1.2 态/分) | 我误判瓶颈在自回归解码;实际在视觉编码 + prefill(每样本约 7700 视觉 token)。批处理保留但**默认 batch-size=1**(与既有结果同口径),改用缩样本控成本 |
| 16 | **自己写的归档被自己的通配符删掉** | 重跑前核对目录时发现 | 归档名是 `bcurve_b1_sh0.nopreds.jsonl`,清理命令写的是 `rm -f bcurve_b1_sh*.jsonl` —— `*` 把 `.nopreds` 一并匹配。丢了 h00 两个分片的 B=1 产物(约 0.4 GPU-小时);h01 三片因早前拷去做空跑分析时带了 `h01_` 前缀而幸存。实质损失小(B=1 本就要带 preds 重跑),丢的是逐次非确定性的交叉校验样本。**教训:删除前先 `ls` 同一个通配符看命中什么,或用 `-not -name '*.nopreds.*'` 排除** |
| 15 | 连续几次 `git push origin jaxan/desktop-memory-training-handoff-e6c21f` 是**空推** | 要给 tilde 做增量 bundle 时发现该分支 ref 停在 8fb6a44 | 本项目约定直提 `main`,而我推的是一个旧分支名,**20 个提交一直没上远端**。已 rebase 到 origin/main 并推送(5509e2d) |

## 7. 待用户裁决

**⚠️ 本节已按 8-09 的欠拟合发现重排。此前推荐 A(先做闭环快测),现在
撤回该推荐**:闭环快测是用来检验"离线增益能否迁移"的,而干净口径下
**离线增益本身就不存在**(+0.5pp,p=0.736)。拿一个不存在的增益去测迁移,
只会烧掉一整轮 GPU 换回一个必然的"无差异"。闭环快测应当推迟到手上有
**统计上确凿的**离线增益之后。

| 选项 | 我的推荐 | 理由 |
|---|---|---|
| **C. 改架构(容量升级)** | ⭐ **推荐** | 欠拟合已被直接证据锁定:训练过的题上界 100%、只做对 51.1%。加性 `s(子集)=Σ head(帧)` 结构上表达不了帧间交互(单帧边际 68.4% vs 集合边际 36.8% 就是交互项存在的证据)。具体:MLP 头 + **成对交互项** + mean/max 双池化。**并把"训练集拟合度"设为前置门槛 —— 拟合不上训练集就不评留出集**,省掉一整轮评测 |
| B. 加训练量 | **划掉** | 欠拟合排除了数据量假设。数据不够不会让**训练集本身**拟合失败 |
| A. 闭环快测 | 推迟 | 见上。等有确凿离线增益再做 |
| E. 特征信息量探针 | 与 C 并行 | 在缓存特征上拟合一个强模型(GBDT/深 MLP)。若强模型也拟合不上训练集,病因就不在头而在**特征**(144 token 缩略图 + 均值池化丢了信息),那要动的是索引遍而不是头。这个探针不占 GPU,可与 B 曲线同跑 |
| D. 联合训 HGKV | 暂不 | 会同时引入两个变量,归因会变脏 |

**另需你确认的口径**:① 容差 —— 已有数据支持把 25 收紧到 **15**(见 §8.5),
结论不受影响但更好辩护,是否采纳?② easy 态对照臂 —— **已在 B 曲线里落实**
(抽了 400 个 easy 态进样本),不再是待办。

## 8. 追问补录(用户问、已答、写回文档)

### 8.1 25px 容差站不站得住?—— **站不住,需要改**

核实结果(`coordprobe`,6000 样本):目标坐标最大值 **989** → 确认是
**[0,999] 归一化空间**;屏幕 98% 为 1920×1080;**4174/6000(70%)带坐标**,
其余是 `type`/`key`/`terminate`(精确匹配,与容差无关)。

25 在归一空间换算到像素是**各向异性**的:

| 方向 | 换算 |
|---|---|
| 横向 | 25/999 × 1920 = **±48 px** |
| 纵向 | 25/999 × 1080 = **±27 px** |

`action_correct` 算的是归一空间 L2 → 像素空间里是横 48 / 纵 27 的**椭圆**。

**为什么会被审稿人攻击**:菜单行高、列表项高、工具栏按钮高度典型就是
25-35px,**纵向 ±27px 意味着点错一整行仍判对**。在文件浏览器、下拉菜单
这类纵向密集 UI 上,正确率可能被 off-by-one-row 灌水。

**这个值的来历(我的疏忽)**:它是语料构造时用来筛 target-recurrence
正例帧的 `coordinate_tolerance=25`,**不是为评测正确率设计的**;
我直接挪用,没有独立论证。

**为什么不能直接改用"点在元素框内"**:AgentNet 这份 screening manifest
**没有 bbox 字段**(已查 schema),要用元素框得回源数据集取。

**已做的修复**:评测脚本改为**落盘原始预测与 gold**,容差从"生成阶段
烘焙"改成"分析时旋钮"。此前只存布尔值,导致无法事后做敏感性曲线、
只能重跑整轮。后续重跑即可一条命令出 5/10/25/50 的敏感性表 ——
"结论在全区间成立"是应对审稿人的标准答法。

### 8.2 v2 是不是"fallback 到 recent"?—— **不是,但相关观察为真**

iter-6 停机是**用户裁定转向离线**,非自行退化。但 iter-2 时确实观察到
selector 选中帧的归一年龄从初始 0.733 → 0.329(**在往 recent 靠**),
当时标注了两种读法(真最优 / 塌缩),**转向后未再验证,至今无定论**。
参考数据点:v4 学出的头与 recent 的选择重合率仅 **26%**(未塌缩),
成绩却接近 —— 说明"不同的选法可以给出相近成绩"。

### 8.3 v3 与 v4 差别 —— **只有训练目标**

架构、初始头、数据、索引遍、超参完全相同。只换目标:
`log p(gold)` → 子集成对排序。结果 21.5% → 32.6%。

### 8.4 新观察:训练损失几乎没降 → 指向特征/容量而非数据量

v4 成对损失 0.791 → **0.716**,而随机水平是 **0.693**(=−log 0.5)。
252 步后仍在随机水平附近 —— **连训练集都拟合不好**。

若成立,含义是**瓶颈在特征或容量,不在数据量**,加标签加轮次不会有质变。
两个嫌疑:① 索引遍 144-token 缩略图信息不足(oracle 是"试出来"的,
selector 要"看出来",难得多);② mean-pool 后单层 Linear(4096,1) 容量太小。

**已发决定性诊断**:用同一套评测跑在**训练用过的 state** 上
(`--skip-first 0`)。训练集上也打不过 recent → 特征/容量问题;
训练集赢、留出不赢 → 泛化/数据问题。**先证伪再投入。**

### 8.5 25px 容差的**数据判据**(此前只有换算,现在有分布了)

拿到带原始预测的评测产物后跑了敏感性扫描(`rl_tolerance_sweep.py`,
300 态,四臂,仅动作类型匹配者共 593 个预测-gold 距离):

| 距离区间 | 占比 | **每单位密度** |
|---|---|---|
| [0,1) | 1.0% | 1.00 |
| [1,2) | 3.4% | 3.40 |
| [2,5) | 9.1% | 3.03 |
| [5,10) | 11.1% | **2.22** |
| [10,15) | 3.0% | 0.60 |
| **[15,25)** | 3.5% | **0.35 ← 谷底** |
| [25,40) | 6.6% | 0.44 |
| [40,60) | 9.8% | 0.49 |
| [60,100) | 9.6% | 0.24 |
| [100,200) | 10.1% | 0.20 |
| [200,∞) | 32.7% | — |

**是双峰的**:近端一簇(0–10,密度 2–3)= 点在同一个 UI 元素上;
远端一片(≥25,密度回升到 0.44–0.49)= 点错了元素;
中间 **[15,25) 密度 0.35 是谷底**。

**结论**:阈值不该拍脑袋,也不该照抄语料的 `coordinate_tolerance=25`。
数据给出的分界在 **15 附近**(谷底左沿),而 25 落在谷底右沿——仍在谷里,
所以不算错,但**15 更靠近真正的分界,且换算成像素是 ±29(横)/ ±16(纵)**,
远小于一行菜单高度,审稿人难以攻击。

**换成 15 不改变任何结论**:learned−recent 在容差 10 / 15 / 25 上都是
+0.7pp。**建议改用 15,并在论文里报整条敏感性曲线**——把"为什么是这个
数字"从设计选择变成可复核的观测。

### 8.7 容差已改为**归约时的旋钮**(D1 决定因此不再有时间压力)

原先枚举产物只存布尔 `correct`,等于把容差 25 烘焙进生成阶段 —— 想换容差
只能重跑 GPU。这条教训在 `rl_selector_eval.py` 上已经吃过一次并写进注释,
**却没同步到枚举脚本**,是我的疏漏。现在补上,枚举每条记录额外落盘:

| 字段 | 用途 |
|---|---|
| `all[].p` | 每个子集的原始预测(约 55 字节/条,千态约 2MB,可忽略) |
| `all[].e` | 该子集是否属于 **oracle 池**(recent/随机 是额外补跑的臂,原口径不进 oracle 的 max;不标记就无法忠实还原) |
| `recent_s` / `random_s` | 两条臂各自选了哪个子集 |
| `gold` / `b0_pred` | 事后重算所需 |

`rl_bcurve_analyze.py --tolerance X` 据此**重算四臂**,不必碰 GPU。
两个后果:① 容差 25→15 的决定不再有时间压力;② 可以直接报
**"B 的选择随容差怎么变"** 这条稳健性曲线 —— 审稿人问"你的预算结论是不是
被那个魔法数字撑起来的"时,这是唯一能一次性堵住的答法。

### 8.6 为什么最终没用批处理

改代码时我判断瓶颈在自回归解码,预期批 16 能提速 8–10 倍。**实测持平**:
单卡批 16 是 1.2 态/分,与批 1 的 1.15 态/分无区别,显存却从 21GB 涨到
76GB。原因是每个样本要过 **约 7700 个视觉 token**(B=2 时两张历史图 +
当前屏,各 2560),视觉编码与 prefill 才是主导项,解码不是。

`--batch-size` 参数保留(默认 1,与既有全量枚举结果逐位同口径),
成本改用**缩样本**控制:B 曲线从 3294 态缩到 1000 态(600 困难 + 400 easy),
22 小时 → 约 6–7 小时。缩样本是无偏的,批处理若采纳反而会引入一个
"批内数值是否等价"的新口径问题。
