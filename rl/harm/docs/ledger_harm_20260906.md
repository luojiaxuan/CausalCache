# history-harm 台账(2026-09-06 起;结构化、可 grep;第一读者是未来的 agent session)

## 0. 问题
给 GUI agent 历史截图为何反而有害?(现象见 `rl/harm/README.md` §0)

## 1. 当前生效的判断
- H0(现象):冻结 GUI-Owl-1.5-8B 在 MobileWorld 上,任何额外历史图都降低步级动作复现率;伤害随图与任务的相关性降低而增大,但相关图也伤害。
- 假说池:H1 视觉稀释(**排除**为主因:当前屏注意力 10.0→7.9%,与骤降不成比例);**H2 时序歧义(支持,机制 B)**;H3 token 数(**排除**);H4 过期动作复读(描述性);**H5 长交错历史诱发过早终止(支持,机制 A;诱因在文本轮次)**;H5′ 目标稀释(注意力上成立,但把指令重复到决策点无效 → 只是相关);现役解释:≥4 轮带原文回复的交错窗口对 GUI-Owl 是训练分布外输入(Venus 无塌陷)。

## 2. 预注册判据(数据落地前写定)
- **Step 1 伤害 vs N**(`harm_vs_n_chain.sh`,GUI-Owl 底座,337 态,两协议,recN 与 irrN,N∈{0,1,2,3,4,6}):
  P1.1 V(recN) 随 N 单调下降(Spearman ρ<0,配对 bootstrap CI 不含 0);
  P1.2 每个 N 上 V(irrN) ≤ V(recN)(无关图伤害 ≥ 相关图);
  P1.3 去文本轨迹协议下伤害减弱或反号(与 Venus 现象一致)。
- **Step 2 错误分类**(`harm_error_classes.py`,零 GPU):
  P2.1 某一类占伤害状态的多数(>50%);P2.2 该类在 irrelevant 与 recency 两条件下占比一致(同一机制)。
- **Step 3 干预**(同一链里的 rec2_mark / rec4_mark、rec2_blank / rec4_blank):
  P3.1 若 recN_blank 的伤害 ≈ recN,支持 H3(token 数效应);若 blank 伤害 ≈ 0 而 recN 伤害显著,排除 H3;
  P3.2 若 recN_mark 显著降低伤害,支持 H2;
  P3.3 注意力测量(待做,需要 HF transformers 前向)。

## 3. 台账(时间倒序)
### 2026-09-06 14:50 PT 终止的触发条件:不是"轮数",也不是"图数",而是**指令消息之后、最近两轮之前堆了多少内容**(`turns_vs_images.py`,零 GPU,汇总已有解码)
同一 337 态(hybridlast / hybridturn 为 307 态),部署布局,底座;"总 term%" = 终止动作占全部解码的比例:

| 规格 | 带原文回复的轮数 | 图数 | 复现率 | 总 term% | 指令消息与最近两轮之间的内容 |
|---|---|---|---|---|---|
| rec2 | 2 | 3 | 93.5 | 0.0 | 无 |
| rec3 | 3 | 4 | 90.2 | 0.0 | 1 轮(图+回复) |
| **rec4** | 4 | 5 | 70.0 | **22.0** | 2 轮 |
| **irr4**(无关图) | 4 | 5 | 68.2 | **16.6** | 2 轮 |
| rec6 | 6 | 7 | 72.4 | 20.5 | 4 轮 |
| **hybrid-first**(2 张标记老帧塞进指令消息) | 2 | 5 | 73.0–74.2 | **19–20** | 2 张图(在指令消息内) |
| hybridlast(2 张标记老帧放最后一条消息) | 2 | 5 | 83.4–83.7 | 0.3–0.7 | 无(额外图在末条) |
| hybridturn(2 个参考轮插在**指令消息之前**,回复 "Noted") | 2 | 5 | 87.0–88.3 | 0.7 | 无(参考轮在指令之前) |
| rec4_short(4 轮但回复置空) | 0 | 5 | 51.7 | 4.8 | 2 轮(空回复) |
| rec4_noresp(回复只留 conclusion 一行) | 4(缩) | 5 | 55.8 | 21.4 | 2 轮 |

- 图数相同(5 张)的四种放法,终止率从 0.3% 到 22%;轮数相同(2 轮)的三种放法,从 0.7% 到 20%。**单看轮数或图数都预测不了终止。**
- 能把所有行排齐的变量是"指令消息与最近两轮之间的内容量":rec3 夹 1 轮不塌,rec4 夹 2 轮塌;hybrid-first 把 2 张图塞进指令消息本身也塌;
  hybridlast 放末条、hybridturn 放在指令**之前**都不塌。rec4_short 是例外:夹 2 轮但回复为空时终止只有 4.8%(复现率却更低,是别的错)。
- 读法:GUI-Owl 在"指令 → 一串已完成的(图, 动作)对"之后倾向补全"收尾";这串东西越长(或指令消息越臃肿)越像"做完了"。
  这与"训练窗口短、长窗口分布外"相容,但更具体:分布外的不是轮数,是指令之后的历史块长度。
- 方法含义(重要):**检索到的老帧放在指令消息之前(hybridturn)或当前帧旁边(hybridlast)都不会触发终止**;hybridturn 的复现率
  (87–88%)已接近 rec2(93.5%),比同一裁判帧走"多保留轮"(pick,约 70%)高近 20 个点。这是"视觉历史不该默认取最近图"在 GUI-Owl 上
  可行的呈现方式。
- 预注册的分辨实验(`reply_pattern_chain.sh`,337 态 × 3 规格,1 张卡约 25 分钟):
  (i) `rec4_deploy_noted` / `rec6_deploy_noted`:保留 4/6 轮图,但最近两轮之前的 assistant 回复换成 "Noted the screenshot."——
  若终止消失,触发物是"已完成动作的序列";若仍塌,触发物是历史块的长度本身(图 token 为主);
  (ii) `hybridturnin`:同样的 2 个参考轮改插到指令消息**之后**、最近两轮之前——若从 0.7% 升到 rec4 量级,说明"位置在指令之后"是必要条件,
  hybridturn 的安全来自放在指令之前。
  判据:(i) 终止率 <5% 且复现率 ≥85% → "动作序列触发";(i) 仍 >15% → "历史块长度触发";(ii) >15% → 位置条件成立。
### 2026-09-06 14:43 PT H5′(目标稀释)的干预:指令重复到决策点 **无效**(`goal_interv_base.jsonl`,337 态,部署布局,底座)
| 规格 | 复现率 | 错误中 terminate | vs 原样(配对 ±95%CI) |
|---|---|---|---|
| N=2 原样 / +指令重复 | 93.5 / 86.4 | 0 / 0% | **−7.1 ±3.5** |
| N=4 原样 / +指令重复 | 70.0 / 71.8 | 73 / 56% | +1.8 ±3.9 |
| N=6 原样 / +指令重复 | 72.4 / 72.7 | 74 / 57% | +0.3 ±3.3 |

- 干预:把任务指令原文再放一遍到最后一条 user 消息里(紧挨当前屏,`messages_deploy(..., variant="goal")`),其余不动。这是 13:45 PT 注意力
  拆分给出的最针对性的推论(a):若 N≥4 的终止来自指令注意力被稀释,把指令搬到决策点旁边应当压下终止。
- 结果:N=4/6 的复现率没有变化(+1.8 / +0.3,CI 含 0);终止在错误中的占比从 73% 降到 56%,但省下的终止变成了别的错误,总体不变。
  N=2 反而掉 7 个点——多出来的指令文本打乱了模型熟悉的最后一轮格式。
- 判定:**H5′ 降为相关性观察**。指令注意力随 N 下降是真的,但它不是终止的原因(或至少不是能靠"补回指令"逆转的原因)。至此对 N≥4 终止的
  三种直接干预——未完成提示(hint)、指令重复(goal)、删/缩回复(noresp/short)——全部无效或更差;唯一改变终止率的操作是拿掉 assistant 回复原文,
  而那同时拿掉了复现线索。剩下的因果解释只有一个还没被推翻:**GUI-Owl-1.5 的训练分布里没有 ≥4 轮带原文回复的交错历史**(官方协议 history_n=1,
  连 2 轮都不给),长交错窗口对它是分布外输入;Venus(原生多轮协议、训练时见过长窗口)的 N 扫描没有任何塌陷,与此一致。
- 这条解释的可检验推论(下一步,不需要新数据):在 Venus 的 N 扫描里错误中的 terminate 占比应当与 N 无关(已有:3–7%,平);在 GUI-Owl 上
  终止率应随"带原文的 assistant 轮数"而非"图数"上升——用 `pickimg`(只加图不加轮)与 `recN`(加轮)在同一 N 下对比,数据已在 `format2_base.jsonl`。
### 2026-09-06 14:38 PT 对账第 1 轮出数 + 与 8-24 对账运行逐题比较(`parity_tally.py` / `parity_env_audit.py` / `parity_retry_effect.py` / `parity_compare.py`)
- 口径:官方默认协议(history_n=1,无历史截图,`CC_*` 不设),同一 harness(官方 repo @83e7b8f + CC 钩子;agent 文件与官方的差异只在 CC 选帧钩子,
  采样参数官方与现役均为温度 0 / top_p 1 / 2048 tokens),32 台全新模拟器、并发 32,vLLM 镜像与 8-24 相同(`vllm/vllm-omni:dev` = vllm-openai v0.24.0,ID 251eba5c)。
- run1:**31/117 = 26.5%**(114 题有结果);8-24 对账运行(`mw/traj_full`)39/117 = 33.3%;官方榜单 38.2%。
- 失败归因(每题取最后一次尝试):50 步耗尽 **73**(62%)、环境错误 5、主动终止判错 1、其它 7。失败几乎全是"转圈到 50 步",不是终止判错。
- 重试机制:runner 遇 "Device is not healthy" 把该题目录改名 `_backup_*` 重跑(`retry_on_device_unhealthy=2`,最多 3 次)。run1 有 28 题被重试(8-24 运行
  36 题,更多);一次跑完的 89 题成功 27(30.3%),重试过的 28 题成功 4(14.3%),重试 ≥3 次的 7 题全败。重试本身**不解释** 8-24→run1 的差距
  (8-24 重试更多但成功更高),但"被重试的题成功率减半"说明设备不健康与任务失败同源(后端应用 Mastodon / Mattermost 卡死时 agent 也在转圈)。
- 逐题:113 题两次都有结果,都成功 26、仅 8-24 成功 12、仅 run1 成功 5;翻转集中在 Mastodon(13/38 → 9/38),Mattermost 两次都是 0/15。
  同一系统两次运行翻转 17 题;单次运行标准差约 ±4pp(117 题、p≈0.3),8 题差距约 1.6σ,尚不能判为系统性退化。
- run2 进行中:72 题 25 成功、0 次重试、暂 34.7%(run1 同期为 46 次不健康事件)——支持"run1 是池重建后的冷启动轮"的解释。
- 14:53 PT 补:run2 到 111 题时 33 成功(28.2%)、36 次不健康事件——"冷启动"解释不成立,两轮都在 27–28%。逐项核对与 8-24 运行的差异:
  eval 命令相同(`--max_round 50`,任务集同为 117);模拟器镜像同为 `sglang-omni:env0`(= 官方镜像,ID b680380e);容器起法与官方
  `build_run_command` 等价(`--privileged` + 端口 + `.env` 挂载,官方额外只传 VNC/代理环境变量);兄弟 session 的 `install_pilot_tasks.sh`
  只改了它自己的 p12/p13(p00/p20 的任务服务未被重启)。**没有找到 harness 层面的差异**;剩余候选是主机负载(224 核负载 122,8-24 时未记录)
  与池内后端应用(Mastodon/Mattermost)的健康漂移。等 run3 出数后一并判定,并把三轮的按应用成功率与 8-24 并列。
- 判定推迟到 3 轮完成:三轮均值落在 30–34% 且与 8-24 同分布 → 26.5% 记为运行方差 + 冷启动;均值仍 <30% → 查主机负载(现 load 122 / 224 核)对模拟器响应的影响。
### 2026-09-06 14:25 PT 跨模型:Venus 的 N 扫描(`harm_vs_n_venus_text.jsonl`,400 态,保留文本轨迹,其标注布局 = 部署布局)
| N | 真实最近 N 帧 | 无关 N 帧 | 错误中 Finished |
|---|---|---|---|
| 0 | 88.7 [85.6, 91.7] | — | 7% |
| 1 / 2 / 3 | 91.6 / 91.9 / 90.9 | — / 85.8 / — | 6% |
| 4 / 6 / 8 | 91.1 / 91.9 / 91.6 | 87.5 / — / 84.5 | 3% / 0% / 3% |

- Venus 上历史图是**小而饱和的增益**(+3pp,N=1 即到顶),N=4/6/8 **没有骤降、没有早停激增**;真实帧比无关帧高 4–7pp。
- ⇒ **机制 A(N≥4 过早终止)是 GUI-Owl 特有的**,不是"多给图就崩"的普遍规律;P1.1(单调伤害)在 Venus 上不成立。
  与 Venus 四臂闭环(recent8 55 > recent2 53 > recent0 49 任务)一致。
- 对论文的含义:现象的普遍性主张要收窄为"呈现方式与数量的效应**因模型而异**,且方向可以相反(GUI-Owl 长历史致早停,Venus 长历史无害)";
  机制章节的核心是解释**为什么 GUI-Owl 会**——H5′(目标稀释)与训练窗口假说都指向"GUI-Owl 的训练/推理协议把历史当短窗口处理"。
### 2026-09-06 13:45 PT 注意力按轮次拆分(同 40 态):随 N 增大,**任务指令所在的首条 user 文本**失去注意力
全层平均、heads 平均(%):

| N | system | 首条 user 文本(指令 + conclusion) | assistant 回复(旧→新) | 其后 user 文本 | 当前屏 |
|---|---|---|---|---|---|
| 0 | 50.6 | **26.5** | 10.8 | 0 | 10.0 |
| 2 | 48.3 | 12.6 | 4.4, 6.7, 8.4 | 6.1 | 8.5 |
| 4 | 48.6 | 10.1 | 2.6, 1.9, 2.5, 5.8, 8.3 | 6.1 | 8.0 |
| 6 | 48.1 | **8.1** | 1.4, 1.2, 1.6, 1.6, 2.5, 6.0, 8.2 | 6.5 | 7.9 |

- 被稀释的是**指令文本**(26.5 → 8.1),不是当前屏图像(10.0 → 7.9)。assistant 回复的注意力集中在最近两轮(6–8%),更早的轮次
  各只有 1–3%——注意力并没有堆到最早的回复上。
- 与 H5 的关系(暂列为**假说 H5′:目标稀释**):长交错历史把注意力从任务指令上抽走,模型对"任务还没完成"的把握下降,在
  一串 "Action: … <tool_call>" 之后模式补全出终止。N=2→4 指令注意力只降 20%,复现率却掉 23 个点,**相关但非比例**,
  不能单凭注意力定案;可检验的推论:(a) 把指令重复放到最后一条 user 消息里(靠近决策点)能否压制 N≥4 的终止——这是比"未完成提示"
  更有针对性的干预(提示无效可能因为它也在首条消息里、同样被稀释);(b) 终止的状态 vs 未终止的状态,指令注意力是否更低。
- 这两条是下一批实验;(a) 只需在 messages_deploy 加一个变体。
### 2026-09-06 13:35 PT 注意力探针落地(`attn_probe.py`,HF eager 前向,40 态 × N∈{0,2,4,6},部署布局)——H1 基本排除
首个生成位置的注意力质量分配(heads 平均,全 36 层平均;%):

| N | 当前屏图像 | 历史帧合计 | 每帧均摊 | 文本 |
|---|---|---|---|---|
| 0 | **10.0** | 0 | — | 90.0 |
| 2 | 8.5 | 3.0 | 1.5 | 88.5 |
| 4 | 8.0 | 4.0 | 1.0 | 88.0 |
| 6 | 7.9 | 4.6 | 0.8 | 87.5 |

- 当前屏的注意力只从 10.0% 降到 7.9%(相对 −21%),N=2→4 几乎不动(8.5→8.0),**与 N=4 处 23 个点的复现率骤降不成比例**
  ⇒ 骤降不是"当前屏被稀释"造成的,H1 排除为主因。历史帧各自只拿 0.5–1.6%,最近的一张最多。
- 文本拿走 80–95%(逐层 82–98%);N 增大时增加的注意力去向是历史帧与 assistant 回复文本,不是当前屏减少的那点。
  与干预结果一致:H5 的诱因在**文本轮次**(去掉回复原文终止即消失),不在图像。
- 工程:首版把视觉塔 27 个 block 的注意力行混进了层序列(同一个 eager_attention_forward),层号错位、当前屏读数为 0——
  已改为只收语言模型层(键长 == 序列长),首版文件另存 `attn_probe_base_v1_mixedlayers.jsonl` 不作数。
- 假说状态:H1 排除(主因)、H2 支持(标注器布局)、H3 排除、H4 描述性(复读 30–41%)、H5 支持(部署布局 N≥4)。
- 后续可做:把"文本"再拆成"首条 user 文本 / 各 assistant 回复 / 当前 turn",看 N≥4 时注意力是否堆到最早的 assistant 回复上。
### 2026-09-06 11:05 PT 检索帧的放法(`format2_base.jsonl`,307 态)+ 闭环两臂无效 + 池重建
| 检索帧(裁判 direct|six)放法 | 全集 V | 救回率 | vs 原生 rec2 |
|---|---|---|---|
| 作为额外 user/assistant **参考轮次**插在最近两轮之前(保持一轮一图、角色交替) | 87.0–88.3 | 69–73% | **−4.9 ~ −6.2** |
| 放进**最后一条** user 消息、当前帧之前 | 83.4–83.7 | 66–68% | −9.4 ~ −9.8 |
| 放进**第一条** user 消息(前一节) | 73.0–74.2 | 61–63% | −19 ~ −21 |
| (参照)结构不变、把最近两帧换成裁判帧 pickimg | 87.5–88.7 | 77–81% | −4.7 ~ −5.9 |

- 放法决定代价:参考轮次(−5)≈ 换图(−5)< 最后一条(−10)< 第一条(−20)。**尊重"一轮一图"的放法代价最小**,但任何额外视觉历史
  在这批(多数不需要记忆的)状态上都要付 ≥5 个点——这是 GUI-Owl 视觉历史的"入场费"。方法含义:检索帧只有在收益 >5pp 的
  记忆关键状态上才值得加;B-pilot 的 checkpoint 场景是该收益的测量地。
- **闭环 recent8 / random2 两臂无效**:17:54Z 起模拟器 "Device is not healthy",recent8 只出 28 题、random2 0 题——与用户质疑的
  绝对成功率偏低同源:12 台旧池已退化(HTTP 500 61 行 / 6 题无结果)。处置:重建 32 台新池(p14–p33 已起,p00–p11 待四臂链
  释放后重建),官方默认协议 ×3 对账先跑,再重跑 recent8 / random2(`arms_rerun_after_parity.sh`)。当前有效的闭环数只有
  recent0 30/117 与 recency-2 31/117,且都带旧池噪声。
### 2026-09-06 11:20 PT ★ 口径更正:GUI-Owl-1.5 的官方榜单协议是**不带历史截图**(history_n=1),不是 recency-2
- 用户质疑绝对成功率(26%)偏低。核查:harness = 官方仓库 83e7b8f + 本项目的选帧钩子(消息构造 88/204 用例字节级 parity),
  与 8-24 对表时相同;官方榜 `site/leaderboard.json`:GUI-Owl-1.5-8B-Instruct GUI-only **38.2%**(3 runs,50 步,
  `num_images_in_history` 未填 → agent 类默认 `history_n=1`,即**不带历史图**);2B 32.2、32B 43.9。榜上其它模型多用 3 张历史图。
- 我方:8-24 对表(同 harness,默认协议)39/115 = 33.9%;9-05 recency-2(B=2,本项目约定)31/117 = 26.5%;9-06 recent0(= 官方默认)
  30/117 = 25.6%。**同一 harness 下比 8-24 低约 8pp**,原因未定:单次运行方差(官方是 3 次平均)、模拟器池 HTTP 500(61/38 行,
  6/2 题无结果)、服务配置漂移。已排队 `parity_rerun.sh`:官方默认协议 ×3(四臂闭环之后),量方差并对账。
- 对 harm 线措辞的影响:此前把 recency-2 称作"官方默认"是错的——它是本项目 P0.5 起的 B=2 约定;**官方榜单默认 N=0**。
  部署布局 N 扫描的读法不变(N=1–3 有益于步级复现、N≥4 终止病理),但"官方默认恰好是最优点"改为"官方榜单用 N=0,
  N=2/3 在步级更好而任务级持平(30 vs 31)"。README §0 与主线台账同步改。
### 2026-09-06 10:45 PT 格式解耦落地(`format_base.jsonl`,337 态,部署布局,底座)+ 闭环 recent0
| 格式 | 全集 V | 救回率(95 态) | vs 原生 rec2(配对 ±95%CI) |
|---|---|---|---|
| 原生:最近两 turn(图+回复原文) | 93.5 | 83.2% | — |
| pickimg:结构不变、两张图换成裁判老帧(3 裁判) | 87.5–88.7 | 76.8–81.1% | **−4.7 ~ −5.9(CI 不含 0)** |
| (参照)结构不变、图换成无关图 irr2 | 83.7 | 65.6% | −10 |
| hybrid-first:原生 + 带 PAST 标记的裁判帧放进第一条 user 消息(4 格) | 73.0–74.2 | 61–63% | **−19 ~ −21** |

- 结构固定时,**图的内容有价值但"最近"优于"更老"**:老帧 88 > 无关 84,但 < 最近 93.5。老帧不是无用,只是不如最近帧;之前标注器布局下
  "裁判≈无关"是伪影,部署布局下的真相是 最近 > 老 > 无关,差距各 5 个点。
- **额外图放进第一条消息是灾难**(−20):GUI-Owl 原生格式"一 user turn 一张图",第一条消息里多两张图破坏了图与轮次的对应;PAST 标记
  在此无效(它在标注器布局里有效,是因为那种布局本来就没有轮次对应可破坏)。检索格式必须尊重"一 turn 一图"。
- 正在测另两种放法(`format2_chain.sh`):放在最后一条 user 消息(当前帧之前);作为额外的 user/assistant 参考轮次插在最近两轮前。
- 闭环:底座 recent0(不给历史图)**30/117**,与 recency-2 的 31/117 持平 ⇒ 任务级上历史截图对 GUI-Owl 无影响(与"任务不需要记忆"一致)。
  recent8 / random2 在跑。
- 工程:format2 首发 18 态崩(见日志),已降并发重发。
### 2026-09-06 10:15 PT 机制 A 干预落地(`term_interv_base.jsonl`,337 态,部署布局)
| 规格 | 复现率 | 错误中 terminate | vs 原样(配对 ±95%CI) |
|---|---|---|---|
| N=2 原样 | 93.5 | 0% | — |
| N=2 回复只留 conclusion 一行(noresp) | 74.5 | 0% | **−19.0 ±4.5** |
| N=2 + "任务未完成勿终止"提示(hint) | 93.5 | 0% | +0.0 ±2.2 |
| N=4 原样 | 70.0 | 73% | — |
| N=4 noresp | 55.8 | 48% | −14.2 ±4.7 |
| N=4 hint | 68.8 | 73% | −1.2 ±2.3 |
| N=4 回复置空(short) | 51.7 | 10% | −15.2 ±6.8 |
| N=6 原样 / noresp / hint | 72.4 / 58.5 / 71.5 | 74 / 50 / 72% | — / −13.9 / −0.9 |

- **提示完全无效**:terminate 占比原样 73% → hint 73%,复现率不变。过早终止对指令不敏感,不是"读了任务描述后的判断"。
- **assistant 回复原文是承重的**:N=2 时只留 conclusion 一行就掉 19 个点——最近 turn 的原文(含 `<tool_call>` JSON)是模型复现自己动作的
  主要线索;去掉它,终止确实减少(73 → 48 / 10%),但其它错误暴增,总体更差。
- 结论:H5 的诱因是 **≥4 个带原文的 assistant turn 的堆叠**——模型在长的"Action: … <tool_call>"序列后模式补全出"收尾/终止";
  它不能靠提示压制,也不能靠删文本消除(删了就失去复现线索)。**GUI-Owl 原生协议的默认 N=2/3 恰好是这条病理曲线的最优点**,
  "给更多历史 turn 更差"是终止病理,不是记忆容量问题。训练侧假说(待验):GUI-Owl 的训练窗口只有短交错历史,长窗口 OOD。
- 方法含义:要检索老帧,不能通过"多保留 turn"实现(会触发 H5),必须走**不增加 assistant turn 的格式**——即 hybrid(额外带标记的图)。
  hybrid 正在跑(`format_chain.sh`)。
### 2026-09-06 10:05 PT 裁判帧 @ 部署布局(底座,337 态):把老帧塞进交错协议是有害的,因为它同时拆掉了最近 turn 的结构
`harm/judge_deploy_base.jsonl`,不给图会错 93 态(27.6%):

| 上下文 | 全集 V | 救回率 | 净(vs 无关帧 65.6%) |
|---|---|---|---|
| rec2(真实最近两 turn) | **93.8** | **82.8%** | **+17.2 ±8.8** |
| irr2(最近两 turn 结构不变,只换成无关图) | 83.7 | 65.6% | 0 |
| 裁判老帧作为保留 turn(9 格) | 62.9–76.3 | 25.8–62.4% | **−3 ~ −40**,9 格里 7 格 CI 不含 0 |

- 部署布局下"选哪几帧"与"哪几个 turn 的 assistant 回复保留原文"绑定:裁判 pick 把最近两步压成 conclusion、把老 turn 的回复原文塞进来
  ——结构与内容一起变了,所以裁判帧比无关帧还差得多。**在 GUI-Owl 的原生交错格式里,检索老帧的代价是破坏最近 turn 的结构。**
- 真实最近图比无关图多 17 个点(CI 不含 0)⇒ 最近帧的**内容**确实被用上了(标注器布局下的"裁判≈无关"是伪影;但"老帧有用"在两种布局下都不成立)。
- 追加实验(`format_chain.sh`,已发射):pickimg(结构固定为最近两 turn,只把图换成裁判老帧)分离纯内容效应;hybrid(最近两 turn 原样 +
  裁判帧作为带 PAST 标记的额外图)= 机制驱动的检索友好格式候选。判据:hybrid ≥ rec2(不伤害)是必要条件;pickimg vs irr2 判老帧内容是否有用。
### 2026-09-06 10:00 PT 机制 A 补证:过早终止不是"快做完了"的合理判断
`term_vs_position.py`:按步在轨迹中的相对位置 k/len 分箱,部署布局 N=4 输出 terminate 的比例——前 25% 的步 **25.5%**,25–50% 27.4%,
50–75% 21.2%,75–90% 19.6%,最后 10% **14.0%**(N=6 同型:27.3 → 15.8)。终止率在任务**开头最高、结尾最低**,与"任务已完成"的
真实概率反向;按历史长度也非单调(10–19 帧处峰值 31.8%)。⇒ 长交错历史诱发的终止是**病理**,不是校准过的完成判断。
rec2 的复现率随位置从 89 升到 98(越靠后越稳),rec4 从 64 到 77——两条曲线同向但 N=4 全程低 15–25 个点。
### 2026-09-06 09:40 PT ★★★ Step 1 + Step 3 落地:两个不同的机制,原"伤害"一半是布局伪影
数据:`harm/harm_vs_n_base_{deploy,text,notext}.jsonl`(337 态 × 各规格),图 `harm/fig1_harm_vs_n.png`,数字(动作复现率 %,bootstrap 95% CI 见图):

| N | 部署布局 真实 | 部署布局 无关 | 标注器布局 真实 | 标注器布局 无关 | 去文本 真实 | 去文本 无关 |
|---|---|---|---|---|---|---|
| 0 | 71.8 | — | 62.9 | — | 31.8 | — |
| 1 | 87.8 | 81.3 | 54.6 | 48.4 | 34.4 | 25.8 |
| 2 | **93.5** | 84.6 | 55.8 | 49.0 | 31.8 | 24.3 |
| 3 | 90.2 | — | 54.9 | 46.3 | 31.1 | 25.8 |
| 4 | **70.0** | 68.2 | 59.3 | 48.1 | 33.1 | 24.3 |
| 6 | 72.4 | — | 58.2 | 45.4 | 32.9 | 24.9 |

干预(标注器布局,保留文本):N=2 真实 55.8 → +PAST 标记 **63.5** / 灰图 **64.4**;N=4 真实 59.3 → +PAST **64.4** / 灰图 **63.8**;N=0 = 62.9。

- **部署忠实布局的自复现 = 93.5%**(rec2,T=0)⇒ 布局重建成功(标注器布局只有 55.8%,差异来自布局而非模型)。
- **机制 A(部署布局,非单调)**:历史图 N=1–3 **有益**(71.8 → 93.5),N≥4 **骤降**(70.0 / 72.4,低于 N=0)。骤降的失败形态是
  **过早终止**:rec4 / rec6 的错误里 73–74% 输出 "Terminate, task finished";rec2 对→rec4 错的 82 个状态里 85% 是 terminate;
  irr4 也 52%。目标动作没有一个是 terminate。⇒ **长的交错历史让模型误以为任务已完成**(新失败模式,与 H1–H4 都不同,记为 H5)。
  按历史长度分层骤降一致(5–9 帧 70.0、10–19 帧 61.2、20–29 帧 69.8、30–50 帧 75.7),不是构造问题。
- **机制 B(标注器布局,单调伤害)**:任何额外图都降低复现(62.9 → 54.6–59.3),无关图更甚(45–49)。两个干预都把它**恢复到 N=0 水平**:
  PAST/CURRENT 文本标记(63.5 / 64.4)与同 token 数灰图(64.4 / 63.8)。灰图有效 ⇒ **不是 token 数**(排除 H3);标记有效 ⇒
  **时序歧义**(H2):图连排在文本前、无轮次结构时模型分不清哪张是当前屏。部署布局的交错轮次天然消歧,所以 N≤3 反而有益。
- 无关图在部署布局下也有益(irr2 84.6 > rec0 71.8):部署布局里 N 同时控制"多少张图"与"多少个 turn 的 assistant 原始回复
  被保留",结构贡献约 +13,内容再 +9(rec2 − irr2)。**"真实帧 > 无关帧 9 个点"在部署布局下成立** ⇒ 主线"截图无额外信息"
  是标注器布局的伪影,须在部署布局下重测裁判帧(已发射)。
- 去文本轨迹(标注器布局):N 对复现率几乎无影响(31–34),真实 > 无关约 +7~+10;GUI-Owl 没有文本轨迹时整体崩到 32%。
- 判据:P1.1(单调下降)在部署布局**不成立**(非单调),在标注器布局成立;P1.2(无关 ≤ 真实)两布局都成立;P1.3 部分成立;
  P3.1 灰图≈N=0 ⇒ 排除 H3;P3.2 标记显著降低伤害 ⇒ 支持 H2。
- 论文骨架据此改为:(i) 现象:视觉历史的价值随**呈现方式**与**数量**非单调变化;(ii) 机制 B 时序歧义 + 干预;(iii) 机制 A 长历史
  诱发过早终止 + 干预(待做:N≥4 时加"任务尚未完成"提示 / 截断 assistant 回复只留 conclusion / 只保留图不保留回复);
  (iv) 方法:按机制设计的历史呈现或训练修复。

### 2026-09-06 09:20 PT 底座四臂闭环首发无效
- `docker run` 因端口冲突失败,`curl` 却打到了别的进程,agent 全程收到 None 响应:recent0 只出 14/117 结果,recent8 / random2 为 0。
  已归档为 `*_invalid_20260906`,链改为"确认容器在跑 + 真实补全请求验证"后重发(16:33Z 起,约 2.5 小时)。教训:健康检查必须验证
  服务的是目标模型,不能只看端口通。
### 2026-09-06 01:50 PT 两条口径警告 + 任务级 Step 1 发射
- **采样偏置**(用户指出,主线台账已登记):337 态全部来自 recency-2 成功的轨迹。对 harm 线的影响:伤害是在"recency-2 成功过的
  episode"里测的——是**保守**方向(这些正是 recency 够用的地方,仍测出伤害);但"救回"被系统性低估。所有步级数字加此限定。
- **动作匹配 ≠ 正确性**:底座 rollout 与标注都是 temperature 0,协议相同的 recency 上下文却只复现原动作 55.8%,说明标注器重建的
  提示与部署提示不完全一致(待查:图像分辨率 / 文本历史格式 / 图文顺序),且"偏离原动作"可能是另一条同样可行的动作。
  **仲裁者是闭环**:已发射底座四臂闭环(recent0 / recent2 已有 / recent8 / random2,各 117 题),"给图有害"若在任务成功率上
  也成立(recent0 > recent2),现象才算站稳(P1.4,追加判据)。
- 追加判据 P1.4:任务级 recent0 − recent2 的配对(按任务)差 > 0 且 bootstrap CI 不含 0。
- 发射:`base_arms_closedloop.sh`(hyper00,一张卡 + 12 台模拟器,三臂顺序,约 6 小时);`plot_harm_vs_n.py` 就位,步级数据落地即出图。
### 2026-09-05 23:35 PT Step 2 错误分类落地(`harm_error_classes.py`,337 态,零 GPU)
伤害状态(null 对、X 错)的类别占比(%):

| 条件 | 伤害数 | 早停 | 类型漂移 | **过期动作复读** | 今昔混淆代理 | **坐标漂移** | 其它 |
|---|---|---|---|---|---|---|---|
| recency-2 | 32 | 0 | 15.6 | **40.6** | 0 | 37.5 | 6.2 |
| 15 帧对均匀(状态×对) | 698 | 0.4 | 13.9 | 34.1 | 1.0 | 47.0 | 3.6 |
| 无关帧 | 69 | 0 | 15.9 | 30.4 | 1.4 | 49.3 | 2.9 |
| 裁判帧(3 个) | 58–59 | 0–1.7 | 10–12 | 24–35 | 0–1.7 | 52–61 | 3.4 |

- P2.1(某类 >50%)**未满足**:recency 下最大类是过期复读 40.6%;裁判帧下坐标漂移 52–61% 勉强过半。
- P2.2(跨条件一致)**满足**:三大类(复读 / 坐标漂移 / 类型漂移)在 recency、无关帧、裁判帧下占比接近——**伤害的形态与图的内容无关**。
- 值得追的线索:**无关任务的图也诱发"复读自己更早的动作"(30%)**——复读不是被图的内容误导,而是"多了图"本身削弱了对当前屏
  的锚定,模型退回到文本轨迹里熟悉的动作。这指向 H1/H3(稀释或长度)而非 H2(内容混淆)。Step 3 的 blank 干预可分辨。
- 局限:今昔混淆代理只比对**过去的点击点**(无 UI 树),对"点到只在历史屏存在的控件"覆盖不足;要做严格版本需要 a11y dump
  或对历史/当前屏做元素检测。
### 2026-09-05 23:20 PT 开线
- 用户:参照 ICLR *More Thought, Less Accuracy?* 的流程把"给图有害"挖成机制论文;在本 session 继续(新 session chip 未被看到,已撤)。
- 已发:Step 2 分类(零 GPU)、Step 1+3 解码链(hyper00 一张卡,约 5k 次解码)。判据先于数据写定(见 §2)。
- 依赖:标签与脚本见 `rl/harm/README.md` §2;容器纪律 `cc_container_lib.sh`。

## 4. 错误与教训
- 标注器布局(图连排在前 + 全部 conclusion)不是部署布局,同协议 T=0 只复现 55.8%;此前所有步级'伤害'与'裁判≈无关'均在该布局下测得,须在部署布局重测。
- 闭环健康检查只看端口通,服务的是别的进程;必须发真实补全验证模型名。
